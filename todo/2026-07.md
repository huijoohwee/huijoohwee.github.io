---
title: "Agentic Canvas OS Todo 2026-07"
doc_type: "Planning Ledger Shard"
schema: "todo-log/v1"
period: "2026-07"
scope: "cross-repository"
status: "append-only"
append_policy: "append-only"
date_heading_format: "YYYY-MM-DD"
source_contract: "../docs/TODO.md"
adoption_date: "2026-07-14"
historical_source: "$GITHUB_ROOT/agentic-graph/todo-log.md"
historical_policy: "preserve-row-bytes"
---

# Todo 2026-07

This shard is governed by `../docs/TODO.md`. Pre-adoption rows retain their imported source bytes; new rows append at EOF.

## 2026-07-05

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| FloatingPanel Chat Storyboard response contract | Keep no-slash plain while routed invocations use KGC plus Agentic OS directive context | Split recognized leading route tokens from remaining requests before provider payload, prompt, KGC profile, fallback, and trace relevance. | `agentic-graph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | Chat Storyboard template contract | `chatRuntimeInvocationQuery`, `buildSubmitConversationMessages`, `buildAgenticOsRuntimeInvocationSystemPrompt` | No-slash prompt or `/prd-tad.create` prompt plus remaining query/media | Plain Markdown or `response:` YAML for no slash; structured KGC with PRD/TAD defaults while the model receives query/media only | Submitted text selects the response contract; route tokens are metadata and must not become product, named terms, request scope, provider user text, or backfilled answers. | Re-run Storyboard template contract, no-slash, PRD/TAD slash, TypeScript, hygiene, doc lint, and browser smoke when the chat surface is mounted. | 2026-07-08 |
| FloatingPanel Chat clean-slate fallback | Stop no-slash image trace output from becoming a backfilled MCP/Rich Media or computing-flow KGC | Classify active-stream/no-content provider traces as no-answer evidence, sanitize markdown image runtime URLs, and keep trace-only fallback on the plain base KGC contract. | `agentic-graph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | KGC request profile + response projection | `sanitizeRequestIntent`, `isTraceOnlyAssistantText`, `buildDeterministicBaseTemplateKgcTurn`, `normalizeKgcAssistantBodyForStorage` | No-slash `what ![image](runtime-url)` prompt plus provider stream trace | Query-responsive base KGC response with neutral `[attached image]`, no signed URL, no upload id, no synthetic MCP nodes, no computing-flow scaffold | Runtime media URLs are evidence pointers, not product identity or graph backfill authority; provider trace without content cannot seed answer surfaces or computing-flow templates. | Re-run clean-slate trace regression, chat response contract tests, TypeScript, hygiene, and a live no-slash image chat smoke before deploy approval. | 2026-07-08 |
| FloatingPanel Chat slash response variants | Make `/` select chatResponseBaseContract variants only when invoked | Parse the submitted user message for `/storybuilding`, `/research-agent`, `/video-agent`, or `/care-agent`; otherwise use the plain base contract. | `agentic-graph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | Chat skill registry + submit request context | `parseChatSkillSlashInvocation`, `buildChatSkillInvocationSystemPrompt`, `buildChatSubmitRequestContext` | Leading slash command plus remaining query | Base contract with narrow variant prompt or plain vanilla contract | Submitted text is the SSOT; no selected-skill submit alias or stale skill id can activate a variant. | Re-run focused chat response contract tests, composer command tests, TypeScript, hygiene, and one live slash chat smoke before deploy approval. | 2026-07-08 |
| Probe-tree care-agent runtime | Make branching clarification runtime-ready without deploy | Route care-agent clarification through local probe-tree MCP tools, host-owned Ollama env, markdown checkpoint nodes, and memory exemplars; keep sibling demo Dev-only. | `agentic-graph/{mcp,canvas/src/features/agent-ready,docs/documents,todo-log.md}; huijoohwee/docs/agentic-graph-care-agent-demo.md` | Probe-tree MCP + care-agent demo doc | `generateProbeOptionsWithLocalModel`, `generateProbeOptions`, `selectProbeOption`, `evolveProbeTree` | Redacted care context, recalled exemplars, optional local model env, selected option | Typed probe options, `type: probe` node, `branches-to` edge, memory exemplar, care-agent proof fields | Model adapter is optional and host-owned; generate never mutates graph; markdown remains checkpoint SSOT; deploy guards stay explicit. | Re-run probe-tree MCP tests, care-agent runtime-ready test, TypeScript, hygiene, and sibling dynamic panel parse after future probe changes. | 2026-07-07 |
| FloatingPanel Chat invocation discovery | Make `/`, `@`, and `#` runtime invocations discoverable in the controlled composer | Reuse shared menu, chat, variable, media, keyword, memory, thumbnail, and vdeoxpln owners; inject exact tools and fail-closed scope rules. | `agentic-graph/{canvas/src/{features/chat,features/agent-ready,lib/{command-menu,markdown-core},components/ui,__tests__},docs/documents,todo-log.md}` | Chat composer invocation adapter | `resolveChatComposerTrigger`, `useUploadedMediaInlineCommandCandidates`, `buildChatInvocationSystemPrompt`, `buildAgenticGraphVdeoxplnRoutingPlan` | Caret context, registries, active variables, cached FloatingPanel Media inventory, provider/model | Commands, `{{key}}` media-variable chips, media embeds with mini thumbnails, scoped MCP/agent/model/media directives | Registries remain SSOT; card and Chat share one media projection, thumbnail renderer, and storage request; unavailable tools produce handoffs, not claimed execution. | Re-run composer/runtime tests, TypeScript, hygiene, doc lint, and local `@` media plus `#media` browser proof. | 2026-07-06 |
| FloatingPanel Chat pipeline visibility | Make workspace ingestion, parsing, and rendering state inspectable without a second runtime | Derive a clickable three-stage status strip from Source File lifecycle and graph state; key workspace context by exact cached text hashes. | `agentic-graph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}` | Chat surface state + bounded text hash cache | `createFloatingPanelChatPipelineStages`, `buildFloatingPanelChatSourceFilesSignature` | Active sources, document text, graph state | Semantic pipeline actions and stale-safe context cache identity | Existing Source Files and GraphData remain SSOT; chat only projects state and seeds prompts. | Re-run focused chat pipeline/cache tests, TypeScript, doc lint, hygiene, and local browser proof after future lifecycle changes. | 2026-07-06 |
| XR Mode ingest/render performance | Keep Import URL, Import local files, and XR Mode fast for `.gltf`, `.glb`, `.ply`, and `.spz` | Cache GLB/GLTF payloads, probe URL MIME/name hints, range-preview local/URL PLY rows, gate full promotion after preview paint, progressively upload full attributes, and keep validation assets env-only. | `agentic-graph/{canvas/src/{features/markdown-workspace/workspaceImport,features/three,lib/assets,__tests__,tests/runners},docs/documents,todo-log.md}` | XR import/runtime cache pipeline | `resolveSpatialCaptureUrlContentForImport`, `loadModelAssetRenderPayload`, `loadSpatialCapturePointCloudPreview`, `loadSpatialCapturePointCloud`, `buildGaussianSplatGeometry`, `updateGaussianSplatGeometrySort` | URL/local model or spatial-capture source plus optional operator-supplied validation file | XR-ready manifest, direct loader input, bounded payload/source cache, preview-ready first paint, full-fidelity promotion, deferred splat resort | Keep model and spatial-capture import source-owned; Markdown stores manifests and cache keys, not payload bytes; parser preserves validation-size source arrays while the renderer grows `instanceCount` and attribute views after range-backed first paint. | Re-run focused `workspace.import.xrSpatialCapture.*`, model payload cache, hardcode scan, TypeScript, hygiene, live Import URL fidelity check, and env-only external PLY parse before future XR ingest/render changes. | 2026-07-06 |
| AI Singapore SEA-LION MCP Sidecar | Expose SEA-LION sidecar tools through MainPanel MCP and local MCP without deploy | Add `sealionMcp.*` KTV rows and local MCP bridge tools for `detect_language_variant`, `translate_localize`, and `safety_check`; keep `AGENTIC_GRAPH_MCP_SEALION_API_KEY` server-owned and no browser persistence. | `agentic-graph/{mcp/{sealion-sidecar-runtime.js,local-tool-contract.js,server.js},canvas/src/{features/panels/views,features/agent-ready,__tests__},docs/documents,todo-log.md}` | MainPanel MCP + local stdio MCP | `callSealionSidecarTool`, `buildMcpDocEntries`, `buildAgenticGraphLocalMcpToolDefinitions` | `sealionMcp.*`, SEA-LION sidecar MCP Streamable HTTP URL, server-side env key | Searchable MCP rows, local `sealion.*` tools, hosted sidecar forwarding | Treat SEA-LION as regional specialist sidecar; forward through hosted sidecar with server auth; no copied prompt internals, browser keys, publish patches, or Cloudflare deploy. | Mirror AgenticRAG schema and publish repo only after explicit Prod/Cloudflare approval. | 2026-07-05 |
| AI Singapore SEA-LION MainPanel Integration | Add SEA-LION as a provider-neutral regional chat/specialist sidecar without deploy | Register `sealion` in shared chat/provider owners, MainPanel Integrations KTV rows, Dev proxy allowlist, and provider-family normalization; keep `AGENTIC_GRAPH_CHAT_PROXY_SEALION_API_KEY` server-managed and BYOK RAM-only. | `agentic-graph/{canvas/src/{lib,features/panels/views,features/storyboard-widget-manager,__tests__},canvas/vite.config.ts,docs/documents,todo-log.md}` | Shared chat provider + Integrations rows | `normalizeChatProviderId`, `resolveIntegrationEntryMeta`, `normalizeTextGenerationWidgetPropertiesForProviderFamily` | `sealionApi.*`, official inferencing API doc, hosted model ids, `/v1/models`, `/v1/chat/completions`, `/v1/embeddings` | Searchable MainPanel SEA-LION config, proxy headers, provider inference | Treat SEA-LION as optional OpenAI-compatible provider and MCP sidecar; follow `https://docs.sea-lion.ai/guides/inferencing/api.md`; no renderer/parser forks, source keys, fixtures, publish patches, or deployed changes. | Add publish-repo proxy parity and AgenticRAG schema mirror only after explicit Prod/Cloudflare approval. | 2026-07-06 |

## 2026-07-09

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Agentic Canvas OS command grammar + MCP readiness validation | Confirm architecture readiness and headless UI approach for integrating Agentic Canvas OS commands (/, @, #). | Validate MCP Stdio/Cloudflare HTTP readiness and confirm headless architecture where agentic-graph acts as the intelligence plane without mutating files. | `agentic-graph/{mcp/agentic-canvas-os-docs-contract.mjs,cloudflare/workers/agentic-graph-mcp/package.json,todo-log.md}` | MCP tool definitions | `AGENTIC_CANVAS_OS_DOCS_TOOL_DEFINITION` | User query on MCP readiness for lovable, vercel, cmdk | Confirmed MCP availability and SSOT grammar resolution without mutating files | Adhere to min-viable-max-value and token economics by relying on MCP SSOT rather than front-end specific cmdk npm packages. | Update cross-repo docs mapping for MCP and agentic canvas OS grammar. | 2026-07-09 |

## 2026-07-10

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Cloudflare AI Gateway high-ROI execution queue | Turn existing gateway readiness into a small, proofable operator plan that lowers spend and clarifies the current Cloudflare surface boundary. | Publish one canonical AI Gateway enhancement plan that prioritizes deterministic cache policy, intent-mapped dynamic routes, metadata-scoped spend limits, analytics proof loops, and future migration away from legacy universal-endpoint wording without touching runtime code. | `agentic-graph/{README.md,docs/documents/{agentic-graph-ai-gateway-enhancement-plan.md,agentic-graph-cloudflare-document.md},todo-log.md}` | AI Gateway optimization roadmap | Doc-owned execution queue | Existing gateway-ready architecture, Cloudflare AI Gateway docs, and current README/Cloudflare baseline | Canonical plan plus README and Cloudflare pointers for the next runtime slice | Since the gateway boundary already exists, the highest-ROI no-code move is to operationalize caching, routing, budgets, and proof while explicitly naming the modern REST-surface migration path. | When code edits open, start with metadata plus cache headers and one low-risk dynamic route because that gives the fastest measurable token-cost proof with minimal runtime churn. | 2026-07-11 |
| Publish-doc handoff to shared promotion recovery card | Let public docs point operators to the shared browser-published recovery surface instead of copying recovery semantics into another doc-only branch. | Update source MCP service owner docs plus the publish-side MCP service notices, onboarding index, and install-boundary release note so they point to the shared MCP Apps agent-ready card for template-only `#promotion.retry <path...>` semantics and keep exact retry paths browser-local. | `agentic-graph/{docs/documents/agentic-graph-mcp/{agentic-graph-mcp-service-prd-tad.md,agentic-graph-mcp-service-prd-tad.companion.md},todo-log.md},huijoohwee.github.io/docs/documents/{agentic-graph-mcp-service-prd-tad.md,agentic-graph-mcp-service-prd-tad.companion.md,agentic-graph-mcp-onboarding-index.md,agentic-graph-mcp-install-boundary-release-note-20260710.md}` | Publish docs + MCP Apps recovery surface | Shared-source publish handoff | Existing published `promotionRecovery` MCP Apps contract and prior onboarding/install docs | Public docs that route recovery readers to the shared browser-published MCP Apps card instead of inventing a parallel recovery write-up | After the shared MCP Apps card exists, the highest-ROI publish follow-up is to make the docs hub and release-note entry points point at that source owner so public readers land on one recovery contract path. | Next, consider adding a single source-owned docs reference inside the repo README only if it improves first-read operator recovery flow without crowding the current install-first opening. | 2026-07-11 |
| Published promotion recovery operator card parity | Mirror the retry contract into an existing browser-published operator surface with a named source owner and parity test. | Expose a template-only `promotionRecovery` operator contract through the shared MCP Apps readiness payload and static HTML card so published surfaces advertise `#promotion.retry <path...>` semantics without pretending to know failure-specific paths. | `agentic-graph/{canvas/src/features/agent-ready/mcpAppsReadyContract.mjs,canvas/src/__tests__/agentReadyHttpMcpParity.test.ts,todo-log.md}` | MCP Apps agent-ready surface | Published operator recovery card | Existing documented promotion retry contract plus browser-local runtime inspection contract | Shared readiness payload and browser-published MCP Apps HTML that surface the retry command template, scope, boundaries, and exact-command source with parity coverage | After browser-local inspection exposes the real runnable retry command, the next highest-ROI move is to mirror the same contract into a published operator surface so first-read external operators can understand recovery semantics without opening source docs. | Next, consider mirroring the same contract into any publish-side docs hub or release-note widget only if it reuses this shared MCP Apps source owner rather than introducing a parallel copy path. | 2026-07-11 |
| Browser-local promotion recovery inspection parity | Turn the documented retry contract into a real operator-facing browser-local inspection surface with a stable machine-readable shape. | Derive a compact `promotionRecovery` contract from finalize failure fields inside the local chat pipeline inspector, expose the runnable retry command and recovery semantics through WebMCP/browser-local inspection, and pin both failure and idle cases with focused tests. | `agentic-graph/{canvas/src/features/agent-ready/{localChatPipelineStateInspection.ts,agenticgraphAgentReadyToolContract.mjs},canvas/src/__tests__/{chatResponseContractPrompt.test.ts,webMcpRuntime.test.ts},todo-log.md}` | Browser-local chat pipeline inspection | Operator-facing recovery inspection contract | Existing finalize recovery fields, browser-local chat pipeline tool, web-MCP runtime, and focused retry tests | A browser-local/WebMCP inspection surface that exposes the exact runnable retry command plus mirror-only recovery semantics without forcing callers to parse ledger prose | Once the retry contract is documented, the next highest-ROI step is to expose that same contract in the operator inspection surface already used by browser-local and WebMCP tooling so recovery is actionable in-context. | Next, mirror the same `promotionRecovery` contract into any future browser-published operator dashboard card only after that UI surface has a named source owner and parity test. | 2026-07-11 |
| Promotion retry operator contract | Make artifact mirroring failures recoverable from the exact command already surfaced by finalize instead of relying on source-code discovery. | Publish one canonical companion that defines when `#promotion.retry <path...>` is valid, where it must appear, what it reuses, and which shared append-focus/chat boundaries it must keep honoring. | `agentic-graph/{docs/documents/{agentic-graph-chat-ai-markdown-pipeline-document.md,agentic-graph-chat-ai-markdown-pipeline-document.promotion-retry.md},todo-log.md}` | Chat pipeline promotion recovery | Operator contract documentation | Existing finalize retry command, browser-local finalize snapshot fields, warning toast, and focused retry tests | One source-owned recovery contract that names the exact command shape, operator surfaces, retry semantics, and composer guardrails without touching runtime code | The highest-ROI follow-up after install/onboarding cleanup is to make promotion failure recovery explicit because it shortens operator recovery time and protects the local-first contract without requiring new runtime breadth. | Next, surface the same retry contract from any future browser-published operator inspection page only after that surface has a named source owner and parity check. | 2026-07-11 |
| Publish-side grammar-first MCP onboarding parity | Keep public first-read docs aligned with the install-first, control-plane-executes-grammar contract after the browser-published onboarding update. | Mirror the fastest grammar path, `agentic-graph.agentic_canvas_os.docs.invoke` ownership, and live grammar examples into the published onboarding index, install-boundary release note, and published tech-stack landing note. | `agentic-graph/{todo-log.md},huijoohwee.github.io/docs/documents/{agentic-graph-mcp-onboarding-index.md,agentic-graph-mcp-install-boundary-release-note-20260710.md,agentic-graph-tech-stack-document.md}` | Publish-facing MCP grammar path | Documentation mirror alignment | Source-side grammar-first onboarding contract plus existing publish-side install docs | Public docs that make install location, grammar execution boundary, and live examples explicit without requiring readers to infer them from source-only docs | After source docs and browser-published onboarding are aligned, the next highest-ROI move is to remove the same endpoint inference from public docs so external readers hit the right live grammar path faster. | Next, mirror the same grammar-first operator path into any future publish-side docs hub or setup widget only after that surface has a named source owner and parity check. | 2026-07-11 |
| Agentic Canvas OS grammar-first onboarding | Make external operators understand exactly how live `/`, `#`, and `@` invocation works without inferring it from broader MCP prose. | Add one compact remote grammar invocation contract to the canonical agent-ready and MCP overview docs; keep `/agentic-graph/mcp` install-first and reserve live grammar resolution for `agentic-graph.agentic_canvas_os.docs.invoke` on `/agentic-graph/control-plane/mcp`. | `agentic-graph/{docs/documents/agentic-graph-agent-ready-document.md,docs/documents/agentic-graph-mcp/agentic-graph-mcp.md,todo-log.md}` | Operator-facing grammar contract | Remote grammar invocation contract + fastest grammar path | Startup-priority grammar recommendation plus existing dual-surface MCP boundary | Source-owned docs that tell operators where grammar is installed, where it executes, and which tool resolves it live | The highest-ROI doc-only implementation is to remove endpoint inference at the source, because grammar differentiation is valuable only when operators can see the exact remote call path immediately. | Next, mirror the same grammar-first operator path into any future browser-published setup widget or public docs hub only after that surface has a named source owner and parity check. | 2026-07-11 |
| Artifact promotion failure recovery | Make failed GitHub or storage mirroring immediately retryable from the chat thread, browser-local inspection, shared warning toast, one-click composer insertion, same-toast success confirmation, and shared caret-at-end placement for retry, quick-action, and queued handoff prompt insertions. | Surface an exact runnable `#promotion.retry <path...>` command in the finalize ledger, local finalize snapshot, deduped toast, shared toast action, post-click confirmation, and shared append-focus composer path whenever local artifacts already exist and promotion fails; reuse that same append-focus path for quick-action prompt seeding, surface-model append prompts, and queued live-canvas hero handoff too, normalize pure invocation-query spacing in the shared open-seed helper for both append-event and queued-handoff delivery while preserving raw seed text for path-bearing or trailing-space commands, let live-canvas hero handoff delegate that normalization to the shared owner instead of pre-normalizing locally, centralize open-only chat entrypoints behind the same shared owner for toolbar and settings surfaces, resolve append-event and queued-handoff seeds through one canonical shared seed object before dispatch or queue storage, add focused open-only and queued-handoff success regressions, and enforce the shared event boundary through registry-addressable canvas action event tests. | `agentic-graph/{canvas/src/features/{chat,agent-ready,canvas},canvas/src/lib/ui,canvas/src/__tests__,canvas/src/tests/registry,todo-log.md}` | Chat finalize artifact ledger + local inspection snapshot + shared toast + shared ui action runtime + shared append-focus composer path | `buildWorkspacePromotionRetryCommand`, `buildWorkspacePromotionRetryToast`, `buildChatPromotionRetryInsertAction`, `runChatPromotionRetryUiAction`, `retryGeneratedChatWorkspaceArtifactPromotion`, `publishLocalChatPipelineFinalizeSnapshot`, `useFloatingPanelChatSurfaceModel`, `openFloatingPanelChat`, `openFloatingPanelChatWithSeed`, `resolveFloatingPanelChatSeed`, `handoffLiveCanvasHeroQuery`, `flushFloatingPanelChatInputHandoff`, `applyFloatingPanelChatInputAppend` | Failed promotion result with one or more saved workspace artifact paths, plus standard quick-action prompt insertions, surface-model append prompts, queued live-canvas hero handoff drafts, and open-only chat entrypoints from toolbar/settings | Finalize response, retry helper, browser-local chat pipeline inspection, shared warning toast, toast action, same-toast success confirmation, retry-command composer placement, quick-action prompt insertion, surface-model append prompt dispatch, normalized pure-token append-event and queued-handoff seed text, preserved raw path-bearing and trailing-space commands, hero handoff delegation through the shared open-seed owner, open-only chat entrypoints through the shared open helper, one canonical resolved seed object for both delivery modes, queued handoff draft flush, open-only helper regression coverage, queued-handoff success regression coverage, and event-boundary regression coverage all reuse the same exact append-and-caret or open-only source owners | Retry guidance, quick-action prompt seeding, surface-model append prompts, queued handoff drafts, and open-only chat entrypoints should reuse already-saved local artifact paths or owned prompt strings instead of introducing a second composer-control channel or duplicated floating-panel open emitters, and both delivery modes should apply the same pure-token normalization before dispatch without mutating path arguments or raw command suffix spacing so focused CI can catch regressions immediately. | Next, route any future relay-owned prompt seeding through the same shared open-seed helper or append-event boundary, and avoid new direct `setInput` insertion branches. | 2026-07-11 |
| Mobile-first enhancement plan | Turn the current mobile-first recommendation into a source-owned execution queue without touching runtime code. | Publish one canonical upstream plan that prioritizes grammar reachability, heavy-runtime gating, virtual-keyboard proof, route-action evidence, and Dev -> Prod -> Cloudflare responsive parity. | `agentic-graph/{docs/documents/{agentic-graph-mobile-first-enhancement-plan.md,agentic-graph-ui-ux-design-document.md},todo-log.md},huijoohwee.github.io/schema/AgenticRAG/README.md` | Mobile-first docs and schema mirror alignment | Canonical plan + discoverability note | Existing mobile-first goal, UI/UX contract, feature-map evidence, and no-code constraint | One source-owned implementation plan plus schema-mirror note for the next mobile-first execution slice | Under a no-code boundary, the highest-ROI implementation is to convert recommendation into one canonical execution queue that future agents can ship from without re-deciding scope, proof, or release discipline. | When code edits are allowed, start with the grammar quick bar and virtual-keyboard proof because they improve mobile time-to-value fastest without broad runtime churn. | 2026-07-10 |
| Machine-readable and browser-published onboarding alignment | Keep the shortest install path, control-plane boundary, and offline proof path consistent across public machine-readable and UI entry points. | Update the published `llms.txt` onboarding copy, add a compact Fastest Path block to the source-owned MCP Apps agent-ready surface, and expose the same sequence through structured readiness data. | `huijoohwee/content/agentic-graph/llms.txt,agentic-graph/canvas/src/features/agent-ready/mcpAppsReadyContract.mjs,agentic-graph/canvas/src/__tests__/agentReadyHttpMcpParity.test.ts,agentic-graph/todo-log.md` | MCP onboarding entry points | Content + source-owned UI alignment | Startup-priority install order and offline harness guidance | Public machine-readable and browser-published onboarding surfaces that direct operators to `/agentic-graph/mcp` first, reserve control plane for session-capable grammar flows, and point evaluation to the source-side harness path | After doc-only alignment, the highest-ROI next move is to align the shortest machine-readable and browser-published entry points so external hosts, crawlers, and operators receive the same low-friction install story without reading long-form docs first. | Next, align any future setup widget or public docs hub card to the same sequence only after that surface has a named source owner and a parity check. | 2026-07-10 |
| Publish-surface startup-priority alignment | Keep the public docs mirror aligned with the new installability-first onboarding story without changing runtime code. | Mirror the fastest install path, control-plane separation, and offline harness proof-path guidance into the published install contract, onboarding index, release note, and tech-stack landing note. | `agentic-graph/todo-log.md,huijoohwee.github.io/docs/documents/{agentic-graph-mcp-install-contract.md,agentic-graph-mcp-onboarding-index.md,agentic-graph-mcp-install-boundary-release-note-20260710.md,agentic-graph-tech-stack-document.md}` | Publish-facing MCP onboarding docs | Documentation mirror update | Startup-priority roadmap plus updated source-side install, agent-ready, and harness docs | Public entry points that match the source-side installability-first story and point evaluators to the cheapest proof path before hosted complexity | The highest-ROI public follow-up after source-doc alignment is to keep first-read publish docs coherent with the same endpoint boundary and offline proof guidance, so external operators reach the right install path faster and incur less setup cost. | Next, mirror the same installability-first ordering into any future published setup widget, docs hub, or release landing page only after that surface has a named source owner and a doc-truth audit. | 2026-07-10 |
| Startup-priority alignment for install and agent-ready docs | Align operator-facing MCP onboarding and agent-ready narrative with the startup priority order while staying doc-only. | Tighten the install contract with a fastest decision path and host matrix, update the agent-ready document to lead with installability and grammar differentiation, and surface the local harness as the offline evaluation path. | `agentic-graph/{docs/documents/{agentic-graph-mcp-install-contract.md,agentic-graph-agent-ready-document.md,agentic-graph-superagent-harness.md},todo-log.md}` | Operator onboarding + agent-ready doc alignment | Canonical doc update | Startup-priority roadmap plus existing install, agent-ready, and harness docs | Sharper source-owned onboarding path that favors time-to-value, proof density, and offline evaluation before hosted complexity | The highest-ROI follow-up after publishing the roadmap is to make the first-read install and agent-ready docs reflect that same operating order so new operators reach a working evaluation path with less ambiguity and lower setup cost. | Next, mirror this same installability-first order into any future release note, setup widget, or published tech-stack landing doc only after those surfaces are source-owned and audited. | 2026-07-10 |
| Startup-priority doc-only execution queue | Turn the README-derived startup recommendations into tracked, source-owned execution guidance without touching runtime code. | Publish a canonical next-step priorities doc that ranks install boundary, Agentic Canvas OS grammar positioning, readiness proof, offline harness adoption, and Dev -> Prod -> Cloudflare discipline, then surface it from the root README. | `agentic-graph/{README.md,docs/documents/agentic-graph-next-step-priorities.md,todo-log.md}` | Startup roadmap + README feature-doc routing | Canonical doc update | Current README positioning, MCP install docs, harness docs, and Agentic Canvas OS docs boundary | One source-owned priority roadmap plus root README discoverability for the current no-code phase | The highest-ROI implementation under a no-code constraint is to convert strategy into one canonical roadmap that points contributors and future agents toward installability, differentiation, proof, offline evaluation, and release discipline before any new feature breadth. | Next, keep install-facing and agent-ready docs aligned with this priority order and only expand deeper feature docs when they improve endpoint clarity, grammar differentiation, proof density, offline demoability, or release discipline. | 2026-07-10 |
| MainPanel readiness contract cleanup | Make MainPanel Integrations and MCP readiness claims implementation-accurate without code edits. | Label MainPanel claims as `documented`, `browser-published`, or `runtime-executable`; treat external `agentic-graph.tool.*` bridge ids as planned until registry owners ship them; anchor provider coverage to the full Settings-backed universe. | `agentic-graph/{README.md,mcp/README.md,docs/documents/agentic-graph-mainpanel-readiness-rubric.md,todo-log.md}` | MainPanel readiness rubric + release gate | Canonical doc updates | Audit findings for MainPanel Integrations and MCP | Source-owned readiness rubric, README/MCP wording cleanup, and deploy-gate checklist | Fix readiness drift at the documentation contract first because it removes false-positive claims immediately, lowers operator ambiguity, and avoids spending time or tokens on unshipped runtime paths. | Mirror the same readiness labels into any future Prod or Cloudflare release notes only after runtime validation for the promoted surface exists. | 2026-07-10 |
| MainPanel readiness docs propagation | Extend the readiness rubric into the larger canonical docs set without touching runtime or test code. | Relabel MainPanel wording in agent-ready, API, and MCP overview docs so UI shells are not mistaken for executable runtime owners and setup rows are not described as live capabilities. | `agentic-graph/{docs/documents/agentic-graph-agent-ready-document.md,docs/documents/agentic-graph-api-document.md,docs/documents/agentic-graph-mcp/agentic-graph-mcp.md,todo-log.md}` | Canonical readiness wording propagation | Documentation relabel pass | New readiness rubric plus existing MainPanel audit findings | Wider canonical doc alignment with explicit `documented` / `browser-published` / `runtime-executable` language | Source-owned doc propagation yields immediate contract consistency with zero engine churn and keeps later test/runtime cleanup scoped to the remaining real drift. | When code edits are allowed, audit `canvas/src/__tests__/mainPanelMcpExternalToolServers.test.tsx` and `canvas/src/__tests__/helpers/mainPanelMcpExpectations.ts` for remaining doc-vs-runtime drift. | 2026-07-10 |
| MainPanel external MCP harness drift cleanup | Neutralize false-positive executable-readiness signals in the focused MainPanel MCP test owners. | Keep deferred external bridge ids asserted as `documented`/pattern-only contract text with deferred schema access; forbid executable-runtime wording in the focused external MCP harness checks. | `agentic-graph/{canvas/src/__tests__/mainPanelMcpExternalToolServers.test.tsx,canvas/src/__tests__/helpers/mainPanelMcpExpectations.ts,todo-log.md}` | Focused MainPanel MCP harness expectations | `assertDeferredExternalMcpBridgeIdsStayDocumentedContract`, external MCP SSOT checks | External MCP bridge-id rows and rendered MCP hub text | Focused harness proof that deferred bridge ids stay non-secret, deferred, and non-executable in test expectations | The highest-ROI source fix after doc relabeling is the harness boundary because it prevents tests from quietly preserving a stronger runtime claim than the docs and owners support. | Next, audit provider-coverage expectations so the full Settings-backed universe remains canonical and any SuperAgent/demo subset stays explicitly scoped. | 2026-07-10 |
| MCP install contract for third-party hosts | Make public install, control-plane grammar invocation, and host recipes explicit without changing runtime code. | Publish one source-owned install contract that tells hosts to install `/agentic-graph/mcp`, add `/agentic-graph/control-plane/mcp` only for session-capable grammar invocation, and mirror the same wording into the publish-docs tree. | `agentic-graph/{README.md,docs/documents/agentic-graph-mcp-install-contract.md,todo-log.md},huijoohwee.github.io/docs/documents/agentic-graph-mcp-install-contract.md` | Remote MCP install boundary | Canonical doc + mirror update | Public discovery endpoint, control-plane endpoint, host setup recipes | One install truth for ChatGPT, Claude, Vercel, Lovable, and generic MCP clients | A single install contract removes endpoint ambiguity at the source, preserves the read-only vs orchestration split, and gives app builders a fail-closed path when dual-surface MCP session support is unavailable. | Next, add the new install-contract link to any future agent-ready landing pages or setup widgets that surface MCP onboarding metadata. | 2026-07-10 |
| MCP install contract surfacing | Put the new install boundary in the first operator-facing docs people read before choosing an endpoint. | Link the install contract from the canonical agent-ready landing doc and add the same boundary note to the published tech-stack overview so remote operators see `/agentic-graph/mcp` vs `/agentic-graph/control-plane/mcp` before setup drift starts. | `agentic-graph/{docs/documents/agentic-graph-agent-ready-document.md,todo-log.md},huijoohwee.github.io/docs/documents/agentic-graph-tech-stack-document.md` | Agent-ready landing copy + publish overview | Documentation cross-link pass | New MCP install contract plus current operator-facing docs | Earlier visibility of the correct MCP setup boundary in both source and publish surfaces | Surfacing the contract from the first-read docs is the highest-ROI follow-up because it reduces wrong-endpoint installs before users ever reach deeper MCP reference material. | Next, mirror the same install-contract link into any future browser-published setup widget or agent-ready landing UI once code edits are allowed. | 2026-07-10 |
| MCP overview onboarding link | Make the dedicated MCP overview point directly to the canonical setup contract instead of making operators infer the next doc. | Add an explicit install-contract pointer inside `agentic-graph-mcp.md` so the MCP overview itself routes setup readers to one canonical onboarding answer before they compare public discovery vs control-plane details. | `agentic-graph/{docs/documents/agentic-graph-mcp/agentic-graph-mcp.md,todo-log.md}` | MCP overview onboarding note | Documentation cross-link pass | Existing MCP overview plus canonical install contract | MCP reference page that self-routes readers to the correct setup contract | This is the next highest-ROI doc follow-up because MCP-specific readers often land in the overview first; linking the install contract there closes the remaining doc-only onboarding gap without touching runtime code. | Next, expose the same canonical install-contract pointer in any browser-published MCP setup surface when code edits are allowed. | 2026-07-10 |
| MCP install boundary release note | Make the boundary change visible in recent-update scans without requiring readers to open the full install contract first. | Publish a short release note that announces `/agentic-graph/mcp` as the install/discovery URL, `/agentic-graph/control-plane/mcp` as the grammar/orchestration surface, and the install contract as the canonical onboarding source; mirror it into publish docs. | `agentic-graph/{docs/documents/agentic-graph-mcp-install-boundary-release-note-20260710.md,todo-log.md},huijoohwee.github.io/docs/documents/agentic-graph-mcp-install-boundary-release-note-20260710.md` | MCP install-boundary change log | Documentation release note | Existing install contract, README/agent-ready/MCP overview routing, publish-docs mirror | One short recent-change artifact for operators scanning release history | A release note is the highest-ROI remaining doc-only follow-up because it makes the new boundary discoverable for readers who track change logs rather than architecture docs, without implying any runtime mutation or deploy event. | Next, add this release-note link to any future docs index or changelog landing page once that index has a named source owner. | 2026-07-10 |
| MCP onboarding index | Make the onboarding trail one click deep instead of requiring readers to discover individual docs organically. | Publish a small MCP onboarding index that links the install contract, install-boundary release note, agent-ready doc, and MCP overview, then mirror it into publish docs. | `agentic-graph/{docs/documents/agentic-graph-mcp-onboarding-index.md,todo-log.md},huijoohwee.github.io/docs/documents/agentic-graph-mcp-onboarding-index.md` | MCP onboarding landing page | Documentation index | Existing MCP onboarding docs set and endpoint boundary | One small landing page for remote MCP onboarding | This is the highest-ROI remaining doc-only follow-up because it turns the scattered onboarding trail into a single landing page without inventing new runtime claims or touching product code. | Next, add a link to this onboarding index from any future docs table-of-contents or changelog hub once those surfaces have named source owners. | 2026-07-10 |
| MCP onboarding index backlink surfacing | Make the new onboarding index discoverable from the install-facing docs people already open. | Add short backlinks from README, the install contract, the install-boundary release note, and the published tech-stack overview to `agentic-graph-mcp-onboarding-index.md` so the onboarding path works in both directions. | `agentic-graph/{README.md,docs/documents/{agentic-graph-mcp-install-contract.md,agentic-graph-mcp-install-boundary-release-note-20260710.md},todo-log.md},huijoohwee.github.io/docs/documents/agentic-graph-tech-stack-document.md` | MCP onboarding backlink pass | Documentation cross-link pass | New onboarding index plus existing install-facing docs | Bidirectional onboarding trail across source and publish docs | This is the highest-ROI remaining doc-only follow-up because a hub without backlinks still depends on luck; surfacing it from the already-touched install-facing docs makes the onboarding trail materially easier to find without creating new runtime claims. | Next, add the same onboarding-index pointer to any future docs hub, changelog hub, or browser-published setup surface once those owners are explicitly named. | 2026-07-10 |
| MCP publish-doc backlink parity | Keep the mirrored publish docs as easy to navigate as the source docs for MCP onboarding. | Add the same onboarding-index backlink to the published install contract and published install-boundary release note so the mirrored docs preserve the same guided path as the source repo. | `agentic-graph/todo-log.md,huijoohwee.github.io/docs/documents/{agentic-graph-mcp-install-contract.md,agentic-graph-mcp-install-boundary-release-note-20260710.md}` | Publish-doc MCP backlink parity | Documentation cross-link pass | Existing publish MCP docs plus onboarding index | Publish-side guided onboarding parity with source docs | This is the highest-ROI remaining doc-only follow-up because publish readers should not lose the guided path that already exists upstream; mirroring the same backlinks keeps source and publish onboarding behavior aligned. | Next, add the onboarding-index pointer to any future publish-side docs hub or table-of-contents page once that surface has a named source owner. | 2026-07-10 |
| MCP source-doc backlink completion | Make the source-side agent-ready and MCP overview docs offer the same shortest guided path as the other install-facing docs. | Add the onboarding-index pointer to `agentic-graph-agent-ready-document.md` and `agentic-graph-mcp.md` so both docs surface the one-page onboarding route instead of only the install contract. | `agentic-graph/{docs/documents/agentic-graph-agent-ready-document.md,docs/documents/agentic-graph-mcp/agentic-graph-mcp.md,todo-log.md}` | Source-doc MCP backlink completion | Documentation cross-link pass | Existing source MCP docs plus onboarding index | Source-side shortest-path parity across install-facing docs | This is the highest-ROI remaining doc-only follow-up because the agent-ready and MCP overview docs are common entry points; adding the onboarding-index pointer there completes the source-side guided path without creating new runtime claims. | Next, add the onboarding-index pointer to any future docs hub or browser-published setup surface once those owners are explicitly named. | 2026-07-10 |
| MCP superseded-doc redirect note | Prevent historical publish-side MCP docs from sending readers down stale runtime/setup paths. | Add a short current-onboarding redirect note to the superseded publish-side `agentic-graph-mcp-agentic-canvas-os-prd-tad.md` so readers are sent to the onboarding index and install contract before using old topology details. | `agentic-graph/todo-log.md,huijoohwee.github.io/docs/documents/agentic-graph-mcp-agentic-canvas-os-prd-tad.md` | Superseded publish-doc redirect note | Documentation redirect copy | Historical publish-side MCP PRD/TAD plus current onboarding docs | Safer historical-doc entry path for remote MCP readers | This is the highest-ROI remaining doc-only follow-up because superseded docs still attract readers; adding an explicit redirect to the current onboarding docs reduces stale endpoint or topology interpretation without touching runtime behavior. | Next, add the same redirect note to any other superseded MCP-facing publish docs that still receive inbound references once those documents are audited. | 2026-07-10 |
| MCP publish-doc entry-point audit follow-up | Redirect realistic publish-side MCP readers from adjacent Agentic OS docs into the current onboarding path. | Add a short remote-MCP onboarding pointer to the mirrored `agentic-graph-agentic-os-follow-on-prd-tad.md` so readers landing there are sent to the onboarding index and install contract instead of inferring the endpoint boundary indirectly. | `agentic-graph/todo-log.md,huijoohwee.github.io/docs/documents/agentic-graph-agentic-os-follow-on-prd-tad.md` | Publish-doc MCP entry-point note | Documentation cross-link pass | Publish-side Agentic OS follow-on PRD/TAD plus current onboarding docs | Safer MCP onboarding from adjacent publish docs | This is the highest-ROI remaining doc-only follow-up because the follow-on PRD/TAD is still a plausible landing page for MCP readers on the publish side; a short redirect keeps the endpoint boundary explicit without rewriting the whole document. | Next, audit adjacent shared topology docs for any direct MCP endpoint guidance that should also point to the onboarding index and install contract. | 2026-07-10 |
| MCP shared-topology doc pointer | Keep broad publish-side topology docs from becoming indirect or stale MCP onboarding entry points. | Add a short remote-MCP onboarding pointer to `hjh-topology-document.md` so readers who encounter MCP in the shared topology narrative are sent to the onboarding index and install contract for the canonical endpoint boundary. | `agentic-graph/todo-log.md,huijoohwee.github.io/docs/documents/hjh-topology-document.md` | Shared-topology MCP pointer | Documentation cross-link pass | Shared publish topology doc plus current onboarding docs | Safer MCP onboarding from shared topology docs | This is the highest-ROI remaining doc-only follow-up because broad topology docs often become first-read context; adding the pointer keeps the MCP endpoint split explicit without turning the topology doc into a second onboarding spec. | Next, audit any remaining shared publish docs that mention MCP transport or endpoint surfaces and add the same pointer where it materially reduces inference. | 2026-07-10 |
| MCP source follow-on doc pointer | Keep the SSOT follow-on Agentic OS doc aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `agentic-graph-agentic-os-follow-on-prd-tad.md` so source-side readers are sent to the onboarding index and install contract instead of inferring the endpoint boundary from broader Agentic OS narrative alone. | `agentic-graph/{docs/documents/agentic-graph-agentic-os-follow-on-prd-tad.md,todo-log.md}` | Source follow-on MCP pointer | Documentation cross-link pass | Source-side Agentic OS follow-on PRD/TAD plus current onboarding docs | SSOT-side MCP onboarding parity for follow-on docs | This is the highest-ROI remaining doc-only follow-up because `agentic-graph/docs/**` is the SSOT; aligning the source follow-on PRD/TAD with the current onboarding path keeps source and publish guidance consistent without changing runtime claims. | Next, audit other source-side broad architecture docs that mention remote MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source main-architecture doc pointer | Keep the main SSOT Agentic OS architecture doc aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `agentic-graph-agentic-os-prd-tad.md` so readers who land on the primary Agentic OS topology spec are sent directly to the onboarding index and install contract for the canonical endpoint boundary. | `agentic-graph/{docs/documents/agentic-graph-agentic-os-prd-tad.md,todo-log.md}` | Source main-architecture MCP pointer | Documentation cross-link pass | Source-side Agentic OS main PRD/TAD plus current onboarding docs | SSOT-side MCP onboarding parity for the primary architecture doc | This is the highest-ROI remaining doc-only follow-up because the main Agentic OS PRD/TAD is a broad first-read SSOT entry point and already explains the four-surface MCP topology; adding the pointer keeps endpoint-boundary guidance explicit without rewriting architecture content. | Next, audit the remaining source-side infrastructure docs that mention Cloudflare or agent-ready MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source Cloudflare-baseline pointer | Keep the SSOT Cloudflare infrastructure baseline aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `agentic-graph-cloudflare-document.md` so readers entering through the Cloudflare, DNS-AID, and agent-ready infrastructure narrative are sent to the onboarding index and install contract for the canonical endpoint boundary. | `agentic-graph/{docs/documents/agentic-graph-cloudflare-document.md,todo-log.md}` | Source Cloudflare MCP pointer | Documentation cross-link pass | Source-side Cloudflare baseline plus current onboarding docs | SSOT-side MCP onboarding parity for infrastructure docs | This is the highest-ROI remaining doc-only follow-up because the Cloudflare baseline is a realistic first-read infrastructure doc and already covers HTTP MCP, WebMCP, and DNS-AID surfaces; adding the pointer keeps endpoint-boundary guidance explicit without changing the infrastructure narrative. | Next, audit the remaining source-side publish-topology and infrastructure docs and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source publish-topology pointer | Keep the SSOT cross-repo publish topology doc aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `agentic-graph-cross-repo-publish-topology.md` so readers entering through the Pages/publish topology narrative are sent to the onboarding index and install contract for the canonical endpoint boundary. | `agentic-graph/{docs/documents/agentic-graph-cross-repo-publish-topology.md,todo-log.md}` | Source publish-topology MCP pointer | Documentation cross-link pass | Source-side cross-repo publish topology doc plus current onboarding docs | SSOT-side MCP onboarding parity for publish-topology docs | This is the highest-ROI remaining doc-only follow-up because the cross-repo publish topology doc is a realistic infrastructure entry point and already references Pages/MCP reads; adding the pointer keeps endpoint-boundary guidance explicit without changing topology ownership or deployment narrative. | Next, audit any remaining source-side agent-ready or API/integration docs that mention MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source agent-ready PRD/TAD pointer | Keep the main source-side agent-ready PRD/TAD aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `agentic-graph-agent-ready-prd-tad.md` so readers entering through the shipped Pages MCP and WebMCP narrative are sent directly to the onboarding index and install contract for the canonical endpoint boundary. | `agentic-graph/{docs/documents/agentic-graph-agent-ready-prd-tad.md,todo-log.md}` | Source agent-ready MCP pointer | Documentation cross-link pass | Source-side agent-ready PRD/TAD plus current onboarding docs | SSOT-side MCP onboarding parity for agent-ready docs | This is the highest-ROI remaining doc-only follow-up because the main agent-ready PRD/TAD is a realistic first-read source doc for the shipped MCP surface; adding the pointer keeps endpoint-boundary guidance explicit without changing the read-only Pages/WebMCP contract narrative. | Next, audit the remaining source-side agent-ready runtime and API/integration docs that mention MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| MCP source agent-ready runtime pointer | Keep the source-side runtime companion for the shipped agent-ready surface aligned with the current MCP onboarding path. | Add a short remote-MCP onboarding pointer to `agentic-graph-agent-ready-prd-tad.runtime.md` so readers entering through the route-contract and deployment companion are sent directly to the onboarding index and install contract for the canonical endpoint boundary. | `agentic-graph/{docs/documents/agentic-graph-agent-ready-prd-tad.runtime.md,todo-log.md}` | Source agent-ready runtime MCP pointer | Documentation cross-link pass | Source-side agent-ready runtime companion plus current onboarding docs | SSOT-side MCP onboarding parity for runtime docs | This is the highest-ROI remaining doc-only follow-up because the runtime companion is a direct route-contract doc for the shipped Pages MCP surface; adding the pointer keeps endpoint-boundary guidance explicit without changing runtime diagrams or deployment guidance. | Next, audit the remaining source-side API and integration docs that mention MCP surfaces and add the same pointer only where it materially reduces inference. | 2026-07-10 |
| Remote MCP grammar invocation for agentic-canvas-os | Transition from local static grammar compilation to dynamic remote grammar resolution over MCP. | Update agentic-canvas-os to query agentic-graph remote MCP (/api/invoke) for grammar (/, @, #) without local mutation or static compilation. | `agentic-canvas-os/{wrangler.jsonc,web/build.mjs}` | MCP remote overlay, Cloudflare Worker route | `fetch(/api/invoke)` with AbortController | User grammar inputs | Dynamic command suggestions via headless UI | Rely on remote SSOT from agentic-graph control-plane MCP; minimize TCO and avoid legacy static artifact generation. | Review AgenticRAG schema for potential updates due to architectural shift. | 2026-07-10 |
| MCP protocol handshake fix & Dynamic SSOT Edge caching | Ensure robust remote MCP connectivity and ingest live GitHub markdown into the remote agentic-graph control plane | Implement `initialize` handshake in `agentic-graph-mcp-client.js` to capture `mcp-session-id`, and update `agentic-graph` Worker to dynamically fetch Edge-cached markdown from GitHub for CMDK payloads. | `agentic-canvas-os/src/agentic-graph-mcp-client.js`, `agentic-graph/{mcp/agentic-canvas-os-docs-core.mjs,cloudflare/workers/agentic-graph-mcp/tool-registry.mjs}` | MCP client session state, Dynamic payload builder | `ensureSession`, `buildAgenticCanvasOsDocsDynamicResolutionPayload` | Grammar token query | Stateful HTTP headers, dynamically parsed markdown from GitHub | Use native Cloudflare Edge caching for high-ROI GitHub fetch to keep token cost zero; fail-closed offline testing preserved via `fetchImpl` injection. | Wire the updated `agentic-graph-mcp-client.js` directly into the `agentic-graph` CMDK composer frontend UI. | 2026-07-10 |

## 2026-07-11

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Airvio Markdown agent discovery | Make public discovery cheap, deterministic, and free of SPA or empty-document noise. | Generate root `llms.txt` and sitemap entries upstream, redirect legacy machine paths, and filter zero-length documents in the D1 crawler query. | `agentic-graph/{cloudflare/pages/agentic-graph-agent-ready.mjs,cloudflare/workers/agentic-graph-storage/db.ts,canvas/src/__tests__,docs/documents/agentic-graph-agent-ready-document.md}` + generated `huijoohwee` artifacts | Agent-ready Pages + storage discovery SSOT | `buildAgentReadyStaticFiles`, `readCrawlerDocumentRows` | Shared route metadata and D1 document rows | Canonical text, XML, redirects, and non-empty Markdown indexes | One generated discovery owner prevents duplicated catalogs while database filtering keeps valid blank editor files out of crawler prompts. | Deploy the validated Pages and storage Worker artifacts, seed docs, then rerun live agent-ready and header checks. | 2026-07-11 |
| agentic-graph embeddability boundary | Turn the iframe-first recommendation into one source-owned contract before host integrations drift. | Publish a canonical embeddability contract that keeps `iframe` as the live embed SSOT, reserves `postMessage` for thin host control, treats host `<canvas>` as visual-only projection, and records the current CSP-owner reconciliation follow-up without touching runtime code. | `agentic-graph/{docs/documents/agentic-graph-embeddability-contract.md,README.md,todo-log.md}` | Embed boundary + README discoverability | Embeddability contract publication | Existing `/doc-view` embed helpers, `_headers` frame policy, and root Pages frame-ancestor override | Source-owned embeddability contract plus README pointer and ledger trace for the next runtime-safe embed slice | Since the repo already ships a run-scoped iframe embed path, the highest-ROI doc-only move is to freeze that boundary now so future host integrations reuse the upstream runtime instead of inventing a second interactive renderer contract. | When code edits reopen, reconcile the root `frame-ancestors` policy with the narrower `/agentic-graph*` and `/agentic-graph/doc-view` policy, then add a small postMessage contract and focused browser proof. | 2026-07-11 |
| Responsive parity release gate | Turn Dev -> Prod -> Cloudflare responsive parity from a recommendation into an explicit release blocker. | Update the mobile-first plan and cross-repo publish topology so mobile keyboard proof, route-and-action review, and publish sync become the named responsive parity gate before `pages:deploy-cloudflare`. | `agentic-graph/{docs/documents/{agentic-graph-mobile-first-enhancement-plan.md,agentic-graph-cross-repo-publish-topology.md},todo-log.md}; agentic-canvas-os/docs/kanban.md` | Responsive parity release contract | Responsive parity blocker definition | Existing mobile workflow matrix, mobile keyboard browser smoke, and publish sync gate | Source-owned release wording that blocks publish when touch-first proof and mirror parity drift apart | After route-and-action evidence and live grammar proof are in place, the highest-ROI cleanup is to make responsive parity a named release blocker so mobile proof cannot silently degrade at publish time. | Next, mirror the validated responsive parity wording into AgenticRAG notes only after the source-owned docs map is rechecked and any publish-side mirror wording is intentionally updated. | 2026-07-11 |
| Mobile route-and-action evidence slice | Turn mobile performance judgement into one source-owned workflow matrix before broader publish-parity claims advance. | Materialize the top phone workflow matrix in the feature map, align the UI/UX contract with its immediate/deferred/fallback-safe classes, and make the cross-repo publish topology treat that matrix as a release contract for heavy-runtime mobile flows. | `agentic-graph/{docs/documents/{agentic-graph-feature-map.md,agentic-graph-ui-ux-design-document.md,agentic-graph-cross-repo-publish-topology.md},todo-log.md}; agentic-canvas-os/docs/kanban.md` | Mobile workflow evidence + release contract | Route-and-action evidence matrix | Existing mobile-first enhancement plan, heavy-runtime gating policy, keyboard-proof slice, and current feature-map chunk analysis | Source-owned workflow matrix and release-contract wording that tell operators exactly which phone actions may wake Monaco, Mermaid, MapLibre, or Three and when that is allowed | After keyboard reachability is proven, the highest-ROI move is to convert mobile runtime decisions into one workflow matrix so future mobile work and publish parity claims are evidence-driven instead of intuition-driven. | Next, promote Dev -> Prod -> Cloudflare responsive parity into the release gate by linking the new workflow matrix to focused publish validation and any future AgenticRAG mirror note updates. | 2026-07-11 |
| Agentic Canvas OS next ROI slice | Prioritize the next min-viable-max-value doc-owned execution queue after the AI Gateway no-secret slice. | Record dynamic remote MCP grammar resolution and mobile virtual-keyboard proof as the next paired runtime slice, with Agentic Canvas OS docs owning the queue and `agentic-graph` mobile docs remaining the proof source. | `agentic-graph/{todo-log.md,docs/documents/agentic-graph-mobile-first-enhancement-plan.md}; agentic-canvas-os/docs/{kanban.md,PRD-TAD.md,RUNTIME-READINESS.md}` | Agentic Canvas OS roadmap + mobile-first proof queue | Next-slice prioritization | Current Agentic Canvas OS grammar direction, mobile-first enhancement plan, and no-secret AI Gateway completion state | Source-owned planning rows and runtime docs that make the next grammar-plus-mobile execution slice explicit without opening code edits yet | With the AI Gateway doc-only slice complete, the highest-ROI next move is to centralize grammar resolution on the remote MCP SSOT and promote mobile keyboard proof as the gating evidence path for touch-first execution. | When code edits reopen, start with remote grammar resolution in `agentic-canvas-os` and the mobile virtual-keyboard proof harness in `agentic-graph`, then mirror any validated contract changes into AgenticRAG only after source proof passes. | 2026-07-11 |
| AI Gateway analytics and legacy endpoint audit | Complete the no-secret execution queue by materializing analytics proof and tightening legacy API wording without code edits. | Document the `docs/reports/ai-gateway-cost-proof.md` structure and update the API document to explicitly contrast legacy `gateway.ai.cloudflare.com` with the modern `api.cloudflare.com/.../ai` REST endpoints. | `agentic-graph/{docs/documents/agentic-graph-api-document.md,docs/reports/ai-gateway-cost-proof.md,todo-log.md}` | AI Gateway analytics proof + endpoint wording | Legacy endpoint audit and cost proof creation | Existing AI Gateway enhancement plan and API document | Source-owned analytics cost proof artifact and explicit legacy vs. modern REST API endpoint boundaries in the API doc | Since the AI Gateway enhancement plan prioritizes proof density and modernization clarity before touching runtime code, materializing the cost proof and tightening the endpoint wording finishes the current no-secret ROI slice. | When live activation is reopened, add an accepted AI Gateway secret to the `joohwee` Pages project and rerun `npm run ai-gateway:readiness:check` without skip flags to unblock the live transport smoke test and dynamic route deployment. | 2026-07-11 |
| AI Gateway no-secret readiness branch | Keep the deferred-secret operator path implementation-accurate while live Pages activation is intentionally skipped. | Document that `npm run ai-gateway:readiness:check -- --skip-sync-check --skip-live` still enforces Pages secret-list verification, record that source proofs plus publish proxy smoke currently pass, and prioritize a source-owned analytics proof plus endpoint wording audit as the next no-secret ROI slice. | `agentic-graph/{docs/documents/{agentic-graph-ai-gateway-enhancement-plan.md,agentic-graph-api-document.md,agentic-graph-cloudflare-document.md,agentic-graph-cross-repo-publish-topology.md},todo-log.md}` | AI Gateway readiness docs | No-secret operator verification contract | Current readiness command output, AI Gateway enhancement plan, API contract, and publish-topology gate docs | Source-owned docs aligned with actual skip-live behavior and the next no-secret execution queue | Since the user is explicitly deferring Pages AI Gateway secret setup, the highest-ROI move is to keep the operator path fail-closed and implementation-accurate while teeing up analytics-proof and endpoint-modernization work that does not depend on live secret rollout. | When live activation is reopened, add an accepted AI Gateway secret to the `joohwee` Pages project and rerun `npm run ai-gateway:readiness:check` without skip flags; until then, materialize the small source-owned analytics proof artifact and tighten legacy endpoint wording. | 2026-07-11 |

## 2026-07-13

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| FloatingPanel prompt-preset selection, activation, and execution boundary | Let users select Video Agent, SME Care Agent, or Investment Research Agent before loading one centralized prompt, while keeping video execution source-backed and preserving generic slash-agent routing for the other two. | Replace the video-only control with a shared native selector; read labels, routes, descriptions, activation modes, and prompt text only from `agentic-canvas-os/docs/PROMPT-PRESETS.md`; fail closed on missing/duplicate/invalid entries; keep Load zero-spend and Send approval-gated. For video, defer empty host-file creation; await exact Markdown applies; reject stale editor sync; prove the runnable text/image/video plan; apply provider plus typed thinking/token-cap settings; require the eight-sheet text package; hand the committed graph to the shared Run all owner; persist and project returned media through existing owners. | `agentic-graph/{canvas/src/features/chat,canvas/src/features/canvas,canvas/src/features/workspace-fs,canvas/src/features/markdown-workspace,canvas/src/components/StoryboardWidgetCanvas,canvas/src/hooks/store/graph-data-slice,canvas/src/lib/storage,canvas/src/__tests__,docs/documents/agentic-graph-api-document.md,todo-log.md}; huijoohwee/docs/agentic-graph-agentic-video-canvas-demo.md; agentic-canvas-os/docs/{PROMPT-PRESETS.md,RUNTIME-PROOF.md}` | Central prompt-preset catalog + selector + slash-agent submit + video Run all + source projection | `loadPromptPresetCatalog`, `loadPromptPreset`, `loadVideoAgentDemoPreset`, `resolveVideoAgentPresetExecutionPreflight`, `parseGenerationInvocation`, `parseChatSkillSlashInvocation`, `requestWorkflowRunAllFromCommittedCanvas`, `resolveStoryboardWidgetWorkflowRunGraphSnapshot` | Selected centralized preset id plus active workspace context; video additionally requires its authored Canvas/script sources and credential | Selector exposes exactly the three requested agents; each loaded prompt begins with its registered slash route; prompt text has one central owner; video retains `#thinking.type.enabled`, Medium 16,384-token cap, eight sheets, committed graph activation, durable artifacts, and shared projections | Load performs zero model/provider calls. Send is explicit approval. Invalid catalog, unavailable source, missing credentials, graph commit failure, capability gap, or missing Run all owner stops visibly before provider spend; historical artifacts remain read-only. | Preserve failed historical folders unchanged; repeat provider proof only when runtime capability changes. | 2026-07-13 |
| FloatingPanel Chat thread continuity | Restore the pending assistant bubble and streaming tail follow. | Materialize one assistant row before the first token, stream into it in place, preserve pre-update tail-follow across large chunks, and let manual scrolling pause follow until the next stream. | `agentic-graph/{canvas/src/features/chat,canvas/src/__tests__,docs/documents,todo-log.md}; huijoohwee.github.io/schema/AgenticRAG` | Shared chat thread + follow state | `ChatMessageRow`, `useFloatingPanelChatThreadFollow` | Optimistic turn, cumulative stream revisions, user scroll position | One pending/live/final bubble with chronological status and bounded auto-follow | Pending/live/final identity stays keyed by assistant id; follow intent is sampled from scroll events, not post-growth geometry. | Re-run focused streaming tests, TypeScript, hygiene, AgenticRAG JSON validation, and a provider-free browser harness after future thread changes. | 2026-07-13 |
| Agentic Video Canvas default runtime recovery | Restore the source-backed demo and keep generated media visible after provider success. | Route canonical script bindings before generic chat, recover BytePlus activation failures through bounded model discovery, use registered generation forms, and project one persisted media identity into Widgets and Rich Media Panels. | `agentic-graph/{canvas/src,docs/documents,todo-log.md}; huijoohwee/docs/agentic-graph-agentic-video-canvas-demo.md` | Chat transport + workflow media projection + default seed | `executeChatSubmitTransportAttempt`, `buildRichMediaWidgetOutputPatch`, `publishMediaRunOutputToRichMediaPanel` | `/video-agent` source binding, provider response, generated artifacts | Local preset activation, bounded provider fallback, durable text/image/audio/video projections | Source binding is the route SSOT; runtime artifacts remain blank until provider return and storage read-back; no chat-log backfill or deploy. | Run an explicitly approved bounded provider smoke only after model entitlement is confirmed; then record live manifest, playback, cost, and Canvas reload proof. | 2026-07-13 |

## 2026-07-14

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Monthly TODO shard architecture | Bound planning context and reduce multi-writer conflicts without adding infrastructure. | Route the bounded TODO index to monthly cross-repository shards; keep closed months immutable, append new rows only to the active month, validate shard identity and size, and retrieve exact scope/month before broader search. | `agentic-canvas-os/{docs,todo}` | TODO index and monthly planning shards | `TODO.md`, `todo/YYYY-MM.md`, planning compliance gates | Cross-repository planning directive and UTC work date | Bounded append-only monthly ledger with on-demand retrieval | Flat Markdown remains the minimum-viable maximum-value owner while one scope stays below the shard caps; scope directories require measured pressure and a versioned migration. | Run structural and base-prefix gates before release; introduce BM25 only after exact search becomes noisy. | 2026-07-14 |
| Runtime-ready Dev to Prod Cloudflare release | Promote the merged source-backed invocation catalog through the canonical mirror and deployment workflow. | Promote only the merged Dev SHA after protected integration, canonical mirror synchronization, source and asset parity checks, one verified Cloudflare deployment, and probes for both production routes; stop immediately on any mismatch or unavailable credential. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{todo-log.md,package.json}`; `huijoohwee/content/agentic-graph` | Release workflow ledger | `RELEASE-WORKFLOW.md`, planning compliance gates, publish and deploy commands | Merged Dev SHA, canonical SSOT revision, clean Prod mirror, and deployment authorization | Auditable promotion record with immutable deployment and route verification evidence | Treat upstream source, generated mirror, and deployment as separate gates; generated provider evidence remains excluded from release inputs. | Complete preflight, validate Dev and mirror parity, deploy once through the repository-owned command, then record immutable route evidence. | 2026-07-14 |
| Production path hygiene corrective release | Remove machine-specific filesystem paths from published assets and active documentation without rewriting immutable history. | Normalize active sources, fixtures, API docs, and HackaMap manifests to repository-relative or `$GITHUB_ROOT` references; gate published output, regenerate mirrors from upstream owners, and redeploy only after protected integration and parity checks. | `agentic-canvas-os/{todo/2026-07.md,docs/RELEASE-WORKFLOW.md}`; `agentic-graph/{scripts,docs,data/test-data,todo-log.md}`; `hackamap/site/hackamap_query_runner.py` | Release path hygiene contract | `check-conflict-compliance.mjs`, `hackamap_query_runner.py` | Canonical sources, generated manifests, and promoted SHAs | Path-neutral production artifacts and live parity proof | Preserve immutable ledgers; block device paths at source and serialization boundaries; keep the operator-owned x402 address gate separate. | Run focused validation, protected integration, mirror regeneration, one deployment, and live leak probes. | 2026-07-14 |
| Current Dev to Prod Cloudflare parity release 2026-07-14 | Publish the exact protected Dev tip after preserving unrelated in-flight work. | Validate the isolated Dev candidate, merge only ledger evidence through protected integration, regenerate the Prod mirror canonically, deploy one verified snapshot, seed required storage, and prove both production routes and shipped asset parity. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{todo-log.md,package.json}`; `huijoohwee/content/agentic-graph` | Release parity ledger | `ci:integration`, `pages:build-sync`, `pages:deploy-cloudflare` | Protected Dev SHA, clean Prod mirror, operator deployment approval | Auditable Dev, Prod, and Cloudflare parity evidence | Preserve concurrent work on pushed handoff branches; release only merged source; stop on the first failed gate or partial deployment. | Complete protected integration, canonical promotion, one deploy, storage reconciliation, and live route and asset verification. | 2026-07-14 |
| Legacy todo and parity worktree cleanup | Remove completed release worktrees and eliminate duplicate agentic-graph planning owners. | Remove the two merged parity worktrees without force; delete both repository-local todo files and their organizer; route active docs, validation, conflict checks, and future task rows exclusively through Agentic Canvas OS `TODO.md` and the active monthly shard. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `agentic-graph/{docs,goal.md,agenticgraph_parser,canvas/src/__tests__,scripts}`; `huijoohwee.github.io/schema/AgenticRAG` | Central planning and worktree hygiene | `git worktree remove`, `TODO.md`, planning compliance gates | Merged parity worktrees plus duplicate local planning files | Pruned worktrees and one bounded cross-repository planning SSOT | Both tips are already merged; monthly shards provide the strict row, lifecycle, retrieval, and byte-prefix guarantees required for planning history. | Keep release branches until separately authorized for deletion; append future work only to the active shard. | 2026-07-14 |
| Config path and shared export failure repair | Restore the full configuration-path suite without masking generated artifacts or package modules. | Exclude ignored generated preview roots from canonical reference scans, include `.tsx` modules in shared-package source discovery, and export the existing RAF runtime through the `grph-shared` package map. | `agentic-graph/{agenticgraph_parser/config_paths_test.py,grph-shared/package.json}`; `agentic-canvas-os/todo/2026-07.md` | Path integrity and shared package surface | `prune_ignored_ref_scan_dirs`, `test_grph_shared_package_exports_match_source_modules` | Ignored preview artifacts plus TypeScript and TSX source modules | Green canonical fixture-reference and package export coverage | Generated previews are not source authority; every authored TS or TSX shared module must match one public package export. | Keep ignored outputs outside source scans and rerun the 14-test suite after shared-module additions. | 2026-07-14 |
| Native image to Three.js skill | Convert supported image sources into one reusable Three.js projection without adding an agent or external plugin dependency. | Add the model-free `image.to-threejs` skill, typed conversion manifest, native raster and SVG loaders, and one shared render mode across Cards, Widgets, Storyboard overlays, and Rich Media Panels; reuse existing skill, semantic, image, workflow, and proof owners. | `agentic-canvas-os/docs/{SKILLS.md,IMAGE-TO-THREEJS-SKILL.md}`; `agentic-graph/{canvas/src,docs/documents/agentic-graph-api-document.md}` | Image to Three.js skill contract and shared media projection | `buildImageToThreeJsConversion`, `ImageThreeJsSurface`, `runStoryboardWidgetMediaWorkflowNode` | One PNG, JPG, JPEG, or SVG source from local or connected widget data | Typed zero-cost manifest and native Three.js Card, Widget, overlay, and Rich Media Panel render | Vector sources become bounded shape and stroke geometry; raster sources become color-correct textured planes; typed errors fail before spend; external Object Sculptor code and dependencies remain forbidden. | Add focused browser proof for raster and SVG Rich Media rendering, then mirror only the source-backed contract into AgenticRAG after Dev integration. | 2026-07-14 |
| SME growth-stage risk and coverage copilot runtime | Implement the PRD's minimum viable local risk copilot while preserving regulated-action and deployment boundaries. | Reuse the typed SME and probe-tree owners; add redaction, declared trigger evaluation, draft-only nudges, approval-gated category matching, multilingual fallback, read-only status views, and focused exact-zero-cost proof without Prod or Cloudflare mutation. | `agentic-graph/{contracts,mcp,canvas/src/__tests__,docs}`; `agentic-canvas-os/todo/2026-07.md` | SME risk-copilot runtime and status surface | `sme_care_agent_status`, source normalization, trigger evaluation, nudge drafting, marketplace matching, multilingual adaptation | Synthetic or operator-redacted SME profile, REG delta, requested language, and optional canonical approval | Typed exposure, declared trigger, draft nudge, category handoff, fallback metadata, status views, and cost logs | Pure validation and deterministic rules own the default path; unsafe input fails before persistence or spend, unmatched deltas no-op, and regulated outputs remain approval-gated and non-bindable. | Run focused contract, MCP, property, Canvas, hygiene, and affected-scope checks; retain Dev-only boundaries and record runtime proof. | 2026-07-14 |
| Agentic Canvas OS multi-device collaboration protected release 2026-07-14 | Release the collaboration runtime and serialized multi-device delivery lane through protected integration. | Fix room authorization and offline replay, validate Durable Object persistence and CI contracts, publish one scoped branch, merge only after required checks, deploy the merged SHA once, and verify the live readiness and collaboration routes. | `agentic-canvas-os/{agent-api,src,worker,web,.github,scripts,docs,todo}` | Collaboration runtime and protected delivery pipeline | `createAuthSessionHandler`, `CanvasRoom`, device publish and deployment workflows | Capability-strength room identifier, queued operations, protected branch, and operator deployment approval | Scoped session tokens, durable idempotent operations, merged Dev SHA, serialized Cloudflare deployment, and live verification evidence | Room identifiers are shareable capabilities; anonymous callers cannot select identity or obtain unscoped room access; offline operations persist until acknowledged; production uses only the protected merged SHA. | Stop on authorization, append-only history, CI, credential, deployment, or live verification failure and report the exact blocked stage. | 2026-07-14 |
| Agentic Canvas OS single-worktree consolidation and protected synchronization 2026-07-14 | Consolidate concurrent repository work into one canonical checkout and synchronize it with protected `origin/main`. | Preserve every unique commit, resolve the collaboration contract at its source, remove the clean redundant worktree, validate the combined branch, publish it through one protected pull request, and fast-forward the canonical checkout after merge. | `agentic-canvas-os/{.github,COLLABORATION.md,__tests__,docs,scripts,src,todo,worker}` | Single-worktree delivery and synchronization contract | `device:publish`, `sync:live`, protected auto-merge | Two clean branch histories, one in-progress merge, fetched `origin/main`, and operator consolidation authority | One validated task branch, one registered canonical worktree, protected merge commit, and clean local `main` parity | Unique commits remain immutable inputs; direct `main` pushes, force operations, secondary worktrees, unexplained dirt, and non-fast-forward pulls fail closed. | Monitor required checks, merge through the protected gate, then run `sync:live` and prove local `HEAD` equals fetched `origin/main`. | 2026-07-14 |
| Canonical Dev explicit fast-forward startup | Prevent recurring clean-main startup failures after protected merges without weakening source identity. | Add one explicit two-phase command that preflights every canonical source, rejects dirt, task branches, divergence, and redundant worktrees, then applies only fast-forward merges before the unchanged Dev gate. | `agentic-canvas-os/{docs/FACTS.md,docs/START-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{package.json,scripts,docs/collaboration-runtime-contract.md}` | Canonical Dev source synchronization | `syncDevCanonicalSources`, `npm run dev:latest` | Clean canonical checkouts whose fetched remote refs may have advanced | Safely refreshed canonical sources followed by ordinary fail-closed Dev startup | A complete preflight precedes every mutation, so one invalid source prevents partial updates; task branches retain their explicit task-mode workflow. | Keep normal `npm run dev` non-mutating and use `dev:latest` only for clean canonical branch refreshes. | 2026-07-14 |
| agentic-graph collaboration report source-revision binding 2026-07-14 | Prevent replay of a valid collaboration report and result pair from another source commit. | Record the merged `sourceRevision` and exact-byte report binding; keep schemas and verifier code upstream; mirror only consumer guidance; preserve unrelated work in its named stash; forbid Prod or Cloudflare mutation. | `agentic-graph/{schemas,scripts,.github/workflows,docs}`; `huijoohwee.github.io/schema/AgenticRAG`; `agentic-canvas-os/todo/2026-07.md` | Collaboration report provenance contract | `resolveCollaborationRuntimeSourceRevision`, `validateCollaborationRuntimeValidationPair`, `collaboration:report:check-result` | Canonical report bytes, validation envelope, pull-request head SHA, and fetched repository state | Schema-valid report and result artifacts bound to exact bytes and one expected source commit | Local runs derive repository `HEAD`; CI supplies the pull-request head or push SHA and rejects digest, pair-revision, or expected-revision mismatch before the canonical gate. | Add canonical `sourceRepository` to the report and envelope, then compare it with `github.repository` so a valid pair from a fork with the same commit SHA cannot be replayed. | 2026-07-14 |
| Image to Three.js runtime-ready Dev promotion | Promote the validated native image projection without widening its owners or deployment scope. | Record focused and browser proof for PNG, runtime-generated JPEG, SVG, fallback, source replacement, and disposal; promote the canonical skill contract to runtime-ready Dev; mirror only the source-backed consumer contract into AgenticRAG. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs/documents/agentic-graph-image-to-threejs-api.md}`; `huijoohwee.github.io/schema/AgenticRAG` | Image to Three.js proof and schema mirror | `buildImageToThreeJsConversion`, `ImageThreeJsSurface`, `RichMediaBrowserSmokePage` | Validated agentic-graph Dev diff and PNG, JPEG, or SVG source | Runtime-ready Dev skill contract plus bilingual Card, Widget, Storyboard, and Rich Media Panel consumer guidance | The source runtime remains canonical; downstream docs describe only proven behavior; no alias, copied external implementation, additional dependency, Prod mirror, or Cloudflare claim is allowed. | Integrate the validated agentic-graph source branch through its protected gate, then reconcile the exact merged source SHA before any separately approved release. | 2026-07-14 |

## 2026-07-15

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Storyboard media projection and Rich Media resize release 2026-07-15 | Promote the validated shared inline-media, preview, graph-materialization, and Rich Media resize repairs through protected Dev and one production deployment. | Preserve generated workspace notes outside the release; commit the shared owners; validate focused Canvas, TypeScript, hygiene, and integration gates; merge only after the protected gate; synchronize the canonical mirror; deploy the exact merged SHA once; then verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,package.json,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Shared media projection and release ledger | `CardInlineTextProjectedMediaChip`, `StoryboardCardMediaDropSlot2d`, `FlowCanvasMediaOverlays`, `resolveFlowCanvasMediaOverlayInteractionPolicy`, `Production Release` | Validated agentic-graph task branch, Agentic Canvas OS planning row, protected Dev SHA, and operator deployment authorization | Merged source fix, generated mirror parity, one Cloudflare deployment, and verified `airvio.co` plus `/agentic-graph` routes | Cards, Widgets, and Rich Media Panels share canonical media and resize owners; no direct main push, duplicate projection, manual mirror patch, or deployment before protected integration. | Record the merge SHA, deployment run, route probes, and any environment-only blocker; leave unrelated generated notes in their named stash. | 2026-07-15 |
| Widget Probe Tree Rich Media multi-turn release 2026-07-15 | Promote the source-backed Widget Card probe invocation, multi-turn Rich Media response, retained edge, and responsive viewport layout. | Preserve unrelated work on named branches; rebase onto the shared media owner; validate probe, text generation, Canvas, type, hygiene, and integration gates; merge through protected Dev; regenerate the canonical mirror; deploy the merged SHA once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Widget probe workflow and release ledger | `runStoryboardProbeTreeInvocation`, `publishStoryboardWidgetRichMediaOutput`, `fitStoryboardWidgetWorkflowViewport`, `Production Release` | Rebasing task branch, SME Care Agent validation fixtures, protected Dev SHA, clean Prod mirror, and deployment authorization | Retained Widget Card and edge, Rich Media-only streamed output, multi-select and multi-turn continuation, auto-fit layout, exact promoted SHA, and verified production routes | Shared probe, widget, graph, Rich Media, and viewport utilities remain authoritative; provider output never replaces the input Widget Card; no direct main push, manual mirror edit, duplicate text widget, or deployment before protected integration. | Merge the planning row and validated Dev branch, promote canonically, deploy once, and record immutable route and SHA parity evidence. | 2026-07-15 |
| FloatingPanel prompt preset alias contract 2026-07-15 | Synchronize Agentic Canvas OS with the first-class agentic-graph Prompt Presets surface. | Define catalog-owned `/*-prompt-preset` selection aliases separately from executable `/...-agent` routes; add Crawler Agent; preserve zero-spend load, no-submit selection, source-backed prompt ownership, and Dev-only boundaries. | `agentic-canvas-os/{docs/FACTS.md,docs/PROMPT-PRESETS.md,docs/RUNTIME-PROOF.md,todo/2026-07.md}` | Prompt preset catalog and proof contract | `prompt_presets`, `slash_command`, `runtime_command` | Four source-backed FloatingPanel presets and their executable runtime routes | Four alias-to-runtime mappings with centralized prompts and documented execution boundary | The catalog owns selection aliases; skills and the command dictionary continue to own execution routes, preventing preset labels from silently redefining runtime commands. | Keep future preset additions source-backed, suffix-consistent, independently validated, and non-submitting until the operator explicitly sends. | 2026-07-15 |
| Native image to Three.js and GLB protected release 2026-07-15 | Promote the reusable image conversion, procedural GLB, shared Rich Media interaction, and Widget Card invocation owners through protected integration. | Validate canonical invocation docs, procedural construction and exporter fidelity, Canvas interaction ownership, TypeScript, hygiene, and integration gates; merge Agentic Canvas OS first, merge agentic-graph second, then deploy the exact protected Dev SHA once and verify production parity. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `agentic-graph/{canvas/src,grph-shared/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Image conversion and protected release ledger | `image.to-threejs`, `image.to-glb`, `ImageToGlbSurface`, `Production Release` | Authorized task branches, one supported image source, validated procedural artifacts, protected Dev SHA, and production environment approval | Canonical invocation catalog, separate Three.js and GLB Rich Media outputs, local orbit and pan controls, exact promoted SHA, and verified production routes | Source media remains unchanged; shared resolvers and interaction guards stay authoritative; procedural code replaces baked geometry; direct main pushes, external copied dependencies, manual mirror edits, and deployment before protected integration remain forbidden. | Merge the Agentic contract PR, validate and merge the agentic-graph runtime PR, dispatch the manual production workflow once, and record route, asset, deployment, and residual-risk evidence. | 2026-07-15 |
| Prompt invocation and workflow output repairs protected release 2026-07-15 | Promote the accumulated browser-comment repairs for prompt invocation and source-backed workflow outputs through protected Dev and production gates. | Publish the prompt preset, invocation token, native crawler, Widget input-topology, Rich Media output, thinking-budget, and Probe-Tree repairs through protected integration; preserve workspace notes; promote only the merged SHA; deploy once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,package.json,scripts,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Prompt and workflow release ledger | `resolveChatComposerTrigger`, `runStoryboardWidgetNativeCrawlerInvocation`, `preserveStoryboardWidgetWorkflowInputTopology`, `materializeStoryboardWidgetProbeTreeInvocation`, `Production Release` | Attributed agentic-graph task diff, appended planning row, protected merge SHA, clean generated mirror, and operator release authorization | Invocable slash, at, and hash tokens; durable Widget and edge topology; bounded source-backed output; Probe-Tree branches; exact promoted SHA; verified production routes | Shared invocation, crawler, graph, Rich Media, and Probe-Tree owners remain authoritative; no backfill, direct main push, manual mirror edit, paid proof call, or deployment before protected integration. | Merge this planning row first, validate and merge agentic-graph through the Integration Gate, regenerate the mirror canonically, deploy once, and record immutable SHA and route evidence. | 2026-07-15 |

## 2026-07-16

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| XR and Camera shared runtime consolidation 2026-07-16 | Keep Canvas XR Mode, FloatingPanel Camera, and FloatingPanel XR synchronized through one camera-framing authority. | Consolidate board selection, camera draft, graph persistence, live Three camera application, reverse orbit sync, axis recenter, and XR control profiles into shared owners; delete BottomPanel XR and stale duplicate projections. | `agentic-graph/{canvas/src,docs}`; `agentic-canvas-os/todo/2026-07.md`; `huijoohwee.github.io/schema/AgenticRAG` | Shared Camera Framing Owner | `StrybldrCameraFramingSection`, `cameraFramingRuntime`, `cameraFramingPose`, `cameraFramingControlsRuntime`, `cameraControlsProfile` | Camera panel changes, XR axis commands, manual OrbitControls motion, document changes, and Reframe requests | One synchronized camera snapshot across Canvas XR Mode and both FloatingPanel projections, with graph history written only by Reframe | Controls remains the sole live camera writer; thin panels project shared state; repeated axis commands recenter; reverse sync waits for damping; stale BottomPanel and duplicate selector paths are removed. | Merge the protected agentic-graph PR, regenerate the AgenticRAG docs map from the merged source revision, and keep Prod and Cloudflare unchanged until separately authorized. | 2026-07-16 |
| Cross-device runtime revision identity and catalog freshness enforcement 2026-07-16 | Prevent same-branch devices from claiming parity while serving different app, docs, or catalog revisions. | Require exact agentic-graph and Agentic Canvas OS SHAs across devices, expose revision-bearing runtime identity, bind catalog hydration to the docs SHA, and allow at most two explicit refresh attempts before a visible stale or blocked result. | `agentic-canvas-os/{docs/{AGENTS.md,FACTS.md,START-WORKFLOW.md,VALIDATION-RUNBOOK.md,RELEASE-WORKFLOW.md,RUNTIME-PROOF.md},todo/2026-07.md}` | Cross-device Runtime Identity Contract | `cross-device runtime identity gate` | Visible device runtime identity records and catalog hydration state | Exact app/docs SHA parity, catalog/docs revision equality, counts, and bounded freshness status | Branch names are informational only; missing identity, revision mismatch, stale hydration, or retry exhaustion fails closed and blocks parity or release. | Implement the shared agentic-graph runtime identity projection and revision-keyed catalog hydration owner, then capture two-device evidence before protected release. | 2026-07-16 |
| XR motion-reference and canonical Timeline media protected release 2026-07-16 | Promote the validated grey-box stage, cast and camera choreography, Gaussian splat editor, single Camera owner, and canonical Timeline media bars through protected Dev and one production deployment. | Preserve external projects as inspiration only; validate XR staging, Gaussian splat tooling, one Camera owner, and canonical Timeline video/audio bars; integrate through protected Dev; synchronize the generated Prod mirror; deploy the exact merged SHA once; verify both Cloudflare routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | XR stage, Gaussian splat surface, shared Camera owner, canonical Timeline media clips, and release ledger | `buildXrMotionReferencePackage`, `serializeOptimizedGaussianPly`, `StrybldrCameraFramingSection`, `VideoSequenceTimelineRuler`, `Production Release` | Attributed agentic-graph task branch, PR #94 runtime-identity handoff, strict planning row, protected Dev SHA, clean generated mirror, and deployment authorization | Deterministic motion-reference package, optimized Gaussian PLY, one Camera surface, canonical thumbnail and waveform bars, exact promoted SHA, and verified production routes | Blockout and SuperSplat remain reference-only with no copied code or external runtime dependency; shared XR, Camera, and Timeline owners stay authoritative; no direct main push, manual mirror edit, deployment before protected integration, or release without exact runtime identity. | Complete independent validation and reconciliation while PR #94 runs; after its handoff, prove two-device SHA parity, merge through the Integration Gate, promote canonically, deploy once, and record immutable route evidence. | 2026-07-16 |
| Probe Tree Widget Card BYOK output protected release 2026-07-16 | Promote slash-only Probe-Tree invocation and visible Widget Card branch output through protected Dev and production gates. | Validate prompt isolation, typed branch materialization, Rich Media publication, output recovery, TypeScript, hygiene, and integration; merge through protected Dev; synchronize the canonical Prod mirror; deploy the merged SHA once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,mcp}`; `huijoohwee/content/agentic-graph` | Probe-Tree Widget Card release ledger | `runStoryboardWidgetWorkflowProbeTree`, `publishStoryboardWidgetRichMediaOutput`, `useTextWidgetOutputArtifactRecovery`, `Production Release` | Attributed Probe-Tree task branch, BYOK configuration, protected merge SHA, clean generated mirror, and deployment authorization | Slash-only invocation, visible bounded branch cards and Rich Media output, exact promoted SHA, and verified production routes | Shared prompt, graph, Storyboard, publication, and recovery owners remain authoritative; direct main pushes, duplicate token insertion, manual mirror edits, paid proof calls, and deployment before protected integration remain forbidden. | Pass append-only, focused runtime, protected integration, mirror parity, deployment, and live route gates; stop at the first mismatch. | 2026-07-16 |

## 2026-07-17

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Probe Tree balanced layout release continuation 2026-07-17 | Resume the protected release after reconciling the owned Probe-Tree branch with the latest Dev source. | Preserve the attributed Probe-Tree commits; merge the fetched Dev base; validate append-only ledgers, balanced branching, pinning, output publication, TypeScript, hygiene, and runtime identity; promote only the protected merge SHA; synchronize canonically; deploy once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,mcp}`; `huijoohwee/content/agentic-graph` | Probe-Tree balanced release ledger | `runStoryboardWidgetWorkflowProbeTree`, `resolveStoryboardWidgetProbeTreeBalancedPositions`, `resetStoryboardWidgetWorkflowLayout`, `Production Release` | Reconciled Probe-Tree branch, recorded planning base, protected Dev SHA, clean generated mirror, and production authorization | Balanced left-to-right cascading branches, persistent output, exact promoted SHA, immutable deployment evidence, and verified production routes | Shared graph, Storyboard, layout, pinning, publication, and release owners remain authoritative; concurrent workspace notes stay outside the release snapshot; any ledger, identity, integration, mirror, credential, or route mismatch stops downstream mutation. | Merge this planning row first, complete protected Dev integration, regenerate the Prod mirror, deploy the exact snapshot once, and record route and SHA parity. | 2026-07-17 |
| Repository-owned one-command collaboration gate 2026-07-17 | Replace physical-device and manual runtime-identity export procedures with one deterministic repository command. | Own one Agentic Canvas OS command that delegates to agentic-graph's canonical readiness harness; automatically boot isolated owner/guest browsers and worker, verify active peers, document sync, identity parity, and digest; clean up; forbid physical-device and JSON-export requirements. | `agentic-canvas-os/{package.json,scripts,docs,__tests__,todo/2026-07.md}`; `agentic-graph/{scripts,canvas}` | Automated collaboration compliance owner | `collaboration:gate`, `runCollaborationGate`, `collaboration:readiness:check` | Canonical sibling repositories and local loopback ports | One exit code plus machine-readable two-peer runtime proof and failure screenshots | Agentic Canvas OS owns orchestration; agentic-graph owns runtime implementation; no duplicated verifier, physical hardware prerequisite, clipboard transfer, or committed identity artifact is allowed. | Keep the wrapper thin, validate its owner contract, run the complete gate, and integrate through the existing protected runtime-identity PR. | 2026-07-17 |
| XR Camera MCP invocation protected release 2026-07-17 | Promote one always-visible Camera owner with source-backed framing and motion invocation across every surface mode. | Define Camera slash, binding, and semantic tokens upstream; remove the downstream catalog copy; validate shared framing, MCP control, XR choreography, canonical BottomPanel Timeline transport, protected integration, mirror parity, one deployment, and both production routes. | `agentic-canvas-os/{docs/{FACTS.md,DICTIONARY-COMMAND.md,DICTIONARY-SEMANTIC.md,DICTIONARY-BINDING.md},todo/2026-07.md}`; `agentic-graph/canvas/src`; `huijoohwee/content/agentic-graph` | Shared Camera invocation and release ledger | `/camera.frame`, `/camera.animate`, `/camera.play`, `/camera.scrub`, `control_local_camera`, `Production Release` | Attributed XR task branch, canonical dictionaries, selected actor, stage marks, protected Dev SHA, clean generated mirror, and release authorization | One upstream invocation catalog, always-visible Camera controls, canonical BottomPanel motion track, exact promoted SHA, and verified production routes | Agentic Canvas OS owns token metadata; agentic-graph owns runtime execution; shared Camera and Timeline utilities remain sole state owners; no copied catalog, FloatingPanel timeline, direct main push, manual mirror edit, external runtime dependency, or premature deployment is allowed. | Merge the upstream dictionary row first, validate and merge agentic-graph through the Integration Gate, synchronize the exact Dev SHA canonically, deploy once, and record route and revision evidence. | 2026-07-17 |
| XR Animation native choreography runtime 2026-07-17 | Add source-backed XR character motion and action-path control without duplicating Camera or Timeline state. | Define one animation command and two semantic routes upstream; implement native presets, shared XR persistence, deterministic sampling, WebMCP control, FloatingPanel projection, and package export; remove stale local invocation and hydration owners; preserve the external reference-only boundary. | `agentic-canvas-os/{docs/{FACTS.md,DICTIONARY-COMMAND.md,DICTIONARY-SEMANTIC.md},todo/2026-07.md}`; `agentic-graph/canvas/src` | Shared XR Animation Contract | `/animation.control`, `#character-motion`, `#action-path`, `control_local_animation`, `XrAnimationFloatingPanelView` | Selected actor or canvas, typed operation, native preset id, shared XR plan, and Dev runtime identity | Persistent cast performance or action path, canonical Timeline playback, deterministic motion-reference export, and Browser WebMCP result | Agentic Canvas OS owns tokens; agentic-graph owns execution; presets remain typed values; Camera and BottomPanel Timeline stay sole owners; no copied external code, assets, schemas, runtime, or downstream token catalog is allowed. | Merge this upstream catalog first, then validate and merge the agentic-graph runtime through the protected Integration Gate; keep Prod and Cloudflare unchanged without fresh authorization. | 2026-07-17 |
| Multi-chat writer lease and fencing runtime 2026-07-17 | Support same-device and cross-device asynchronous Codex tasks without weakening the single canonical checkout rule. | Add atomic session leases, bounded heartbeat, monotonic epochs, claim-commit fencing, scope-aware draft PR ownership, lifecycle guards, focused tests, and runtime-readiness proof; keep same-scope mutation serialized and allow different scopes to progress independently. | `agentic-canvas-os/{scripts,package.json,.githooks,docs,__tests__,todo/2026-07.md}` | Session-bound Writer Coordination Runtime | `device:start`, `device:resume`, `device:heartbeat`, `createWriterLeaseStore`, `assertNoCompetingScopePullRequests` | Device, session id, semantic scope, base SHA, TTL, canonical checkout, and open pull-request metadata | Atomic local lease, draft ownership PR, increasing fence epoch, bounded expiry, explicit parked or delivery state, and scope-aware parallelism | Local Git metadata owns same-device exclusion; draft PR metadata owns distributed scope visibility; claim ancestry fences stale writers; duplicate scope, expired lease, wrong session, or secondary worktree fails closed. | Integrate through protected Dev; retain explicit parked or expired exact-SHA handoff and fast-forward fencing as the only same-scope takeover path. | 2026-07-17 |
| Local hostname branch contract and checkout-state guard 2026-07-17 | Unblock `.local` device branches without widening semantic scope or leaving checkout mutations after invalid input. | Centralize the device-branch grammar, preserve bounded hostname separators, normalize scope separately, validate lease identity before mutation, align workflow contracts, and add focused parser, sanitizer, metadata, and checkout-state regression proof. | `agentic-canvas-os/{scripts,docs,__tests__,todo/2026-07.md}`; `agentic-graph/{docs,schema,scripts}` | Device Branch Identity Contract | `parseDeviceBranch`, `sanitizeDevice`, `sanitizeScope`, `createWriterLeaseStore`, `device:start` | Hostname-derived device, semantic scope, branch, and writer lease metadata | Contract-valid `.local` task branch or typed pre-mutation rejection | Device accepts interior dot, underscore, and hyphen with alphanumeric boundaries; scope remains hyphen-only; lease fields must equal parsed identity. | Run focused tests, docs checks, branch lifecycle verification, and protected integration without Prod or Cloudflare mutation. | 2026-07-17 |
| Probe-Tree active Chat semantic response release 2026-07-17 | Promote genuinely query-grounded Probe-Tree clarification cards without hardcoded or generic fallback responses. | Route Probe-Tree LLM generation through the active Chat tuple, reconcile stale typed card metadata, separate transport failure from semantic rejection, preserve query-grounded authored choices, validate protected Dev, promote the exact merged SHA, deploy once, and verify both routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,mcp}`; `huijoohwee/content/agentic-graph` | Probe-Tree LLM semantic response release ledger | `resolveStoryboardWidgetProbeTreeChatRoute`, `inferTextGenerationProviderFamily`, `readGraphNodeProperties`, `Production Release` | Fenced Probe-Tree source commit, active Chat configuration, strict planning base, protected Dev SHA, clean generated mirror, and operator release invocation | Query-specific multi-choice Probe-Tree cards, explicit provider transport errors, exact promoted SHA, immutable deployment evidence, and verified production routes | Active Chat owns provider routing; typed graph fields remain canonical; provider output must pass semantic validation; zero-model fallback synthesis, hardcoded answers, direct main push, manual mirror edits, and pre-integration deployment remain forbidden. | Merge this planning row, fence the source commit onto current Dev, pass collaboration and Integration Gates, synchronize the exact merge SHA, deploy once, and record route plus revision parity. | 2026-07-17 |
| Agentic Canvas OS cache context runtime readiness 2026-07-17 | Reuse stable prompt prefixes across changing requests without overstating provider cache behavior. | Compile stable prompt prefixes once per namespace and revision; append dynamic tails after them; bound volatile entries; invalidate stale revisions; expose local reuse separately from provider cache reads and writes; keep live provider proof gated to the downstream model owner. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Stable Prefix Cache Context Registry | `createCacheContextRegistry`, `normalizeCacheUsage`, `createAgentApiApp` | Stable JSON-compatible prefix, namespace, revision, and changing dynamic tails | Opaque handle, exact prefix-first prompt packets, bounded reuse counters, revision invalidation, and honest provider telemetry | Agentic Canvas OS owns local deterministic assembly; the downstream model adapter owns provider request fields and returned usage; no latency inference, secret persistence, unbounded cache, copied implementation, paid call, Prod mirror, or Cloudflare mutation is allowed. | Integrate the Dev runtime and wire the downstream model owner only in a separately claimed scope with focused provider-usage proof and approved spend. | 2026-07-17 |
| Agentic Canvas OS reasoning continuity runtime readiness 2026-07-17 | Preserve compatible reasoning across stable turns without exposing reasoning content or overstating provider behavior. | Compare bounded goals, assumptions, and priorities; chain the last completed response only through declared capabilities; request all-turn continuity for stable invariants; reset rendered reasoning on drift; serialize active turns; confirm effective context only from returned response metadata. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Reasoning Continuity Registry | `createReasoningContinuityRegistry`, `begin`, `complete`, `createAgentApiApp` | Thread id, stable invariant arrays, declared capabilities, completed response id, and returned effective context | Bounded request patch, current-turn fallback, prior-response chaining, active-turn fence, and honest provider-confirmation counters | Agentic Canvas OS owns volatile deterministic planning; the downstream adapter owns model capability truth, provider calls, tokens, cost, and returned response metadata; raw reasoning storage, copied implementation, silent capability guesses, unbounded turns, paid calls, Prod mirror, and Cloudflare mutation are forbidden. | Wire the downstream Responses adapter in a separately claimed agentic-graph scope and run one bounded provider-confirmation proof only after explicit spend approval. | 2026-07-17 |

## 2026-07-18

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| XR subject-bound camera motion-reference production release 2026-07-18 | Promote the protected XR camera-move implementation through the canonical production workflow. | Preserve unrelated workspace notes; bind agentic-graph to this exact docs revision; require protected integration, append-only ledgers, collaboration proof, immutable manifest parity, canonical mirror generation, one authorized deployment, and verification of both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | XR subject-bound camera release ledger | `buildXrCameraMoveRuntime`, `buildXrMotionReferencePackage`, `control_local_camera`, `Production Release` | Protected XR feature branch, selected actor and marks, exact docs revision, operator release authorization | Merged subject-bound camera moves, deterministic motion-reference package, exact promoted SHA, immutable deployment evidence, and verified production routes | Agentic Canvas OS remains invocation and planning authority; agentic-graph remains runtime authority; FloatingPanel Camera and BottomPanel Timeline retain separate ownership; Blockout remains inspiration-only; direct main pushes, manual mirror edits, copied dependencies, and pre-integration deployment are forbidden. | Merge this ledger, update the agentic-graph docs pin, rerun protected integration, complete Dev, dispatch one production release, and record route plus revision parity. | 2026-07-18 |
| Agentic Canvas OS programmatic tool calling runtime readiness 2026-07-18 | Turn open-ended tool use into predictable bounded stages without executing generated JavaScript in the application process. | Require provider-attested hosted execution, response continuation, caller lineage, schema validators, read-only idempotent tools, bounded parallelism, compact final evidence, and honest cost and isolation status; keep high-impact work direct and live provider proof gated. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Programmatic Tool Calling Runtime | `createProgrammaticToolCallingRuntime`, `run`, `createAgentApiApp` | Run identity, JSON task input, capability flags, tool declarations, validators, hosted response items, and cost logs | Final output, caller-linked tool results, bounded evidence and cost, or typed blocked reason | Agentic Canvas OS owns deterministic policy and client-tool dispatch; the downstream adapter owns exact model eligibility and hosted execution; local eval, copied external artifacts, unsafe programmatic mutation, fabricated isolation, paid calls, Prod mirror, and Cloudflare mutation are forbidden. | Wire one downstream hosted adapter in a separately claimed scope and run a bounded provider proof only after explicit spend approval. | 2026-07-18 |
| Agentic Canvas OS LLM instruction audit runtime readiness 2026-07-18 | Preserve repository workflow intent while reducing repeated always-on instruction context. | Keep AGENTS.md limited to durable rules; keep SKILLS.md metadata-first; add deterministic intent, budget, duplication, owner-leakage, baseline, zero-cost, and deploy-boundary checks; route details to canonical owners; preserve exact workflow behavior. | `agentic-canvas-os/{scripts,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Instruction Audit Runtime | `auditInstructionDocuments`, `instruction-audit:check` | Durable guidance, skill catalog, optional exact Git baseline, and bounded audit policy | Typed audit metrics, violations, baseline reduction, zero-cost log, and unchanged deploy state | Structural checks own context discipline; canonical documents retain detailed workflow intent; the audit never invokes a model, rewrites source, copies external guidance, or authorizes Prod or Cloudflare mutation. | Evaluate task-quality outcomes separately, and add new durable instructions only after repeated error evidence while keeping workflow mechanics in selected skills or owner docs. | 2026-07-18 |
| Agentic Canvas OS instruction task-quality evaluation 2026-07-18 | Evaluate whether slimmed instructions preserve observable task behavior without grading private reasoning or overstating model quality. | Add four bounded final-answer scenarios, typed candidate provenance, required and forbidden concept checks, concision limits, fail-closed case validation, human review, and explicit zero evaluator model and deploy claims. | `agentic-canvas-os/{evals,scripts,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Instruction Task-Quality Evaluation | `evaluateInstructionTaskQuality`, `instruction-quality:check`, `instruction-quality:evaluate` | Repository suite plus complete recorded or live candidate answers for one exact instruction revision | Per-case findings, aggregate score, validation errors, provenance, and unchanged deployment state | The evaluator owns deterministic final-answer screening only; candidate generation, model usage, semantic judgment, promotion, Prod mirror, and Cloudflare remain external and explicitly gated. | Run a named candidate packet from the intended host, review qualitative behavior, and compare revisions before making a model-specific quality claim. | 2026-07-18 |
| Same-device multi-worktree mutation governance 2026-07-18 | Allow independent Codex tasks to mutate different scopes concurrently without disturbing the canonical runtime checkout. | Replace global single-worktree exclusion with one clean main runtime owner, registered branch-exclusive task worktrees, an atomic per-branch lease registry, scope fencing, protected pull requests, focused lifecycle tests, and explicit downstream runtime gates. | `agentic-canvas-os/{scripts,.githooks,__tests__,docs,COLLABORATION.md,README.md,todo/2026-07.md}` | Multi-worktree Writer Coordination Runtime | `createWriterLeaseStore`, `assertRegisteredWorktree`, `device:start`, `device:park`, `device:complete`, `sync:live` | Fetched main ref, detached registered task path, stable session id, device identity, and unique semantic scope | Concurrent isolated task branches with worktree-bound leases while the clean main worktree remains the only normal runtime and synchronization source | Git-common-directory metadata coordinates local worktrees; branch exclusivity and one PR per scope prevent overlap; same-worktree, duplicate-scope, stale-fence, unregistered-path, and runtime-source violations fail closed. | Align agentic-graph predev with the v2 registered-worktree policy in a separate clean scope, then prove concurrent task mutation plus canonical-main runtime acceptance. | 2026-07-18 |
| Probe-Tree action-topic semantic production release 2026-07-18 | Promote action-verb clarification topics through the active Chat route while preserving child-owned terminal continuation and explicit output wiring. | Align the source-backed prompt with the merged runtime; require semantic 2-4-card output for explicit action topics, active provider routing, user-authored output edges, append-only proof, protected integration, exact mirror promotion, one deployment, and both route checks. | `agentic-canvas-os/{docs/PROMPT-PRESETS.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Probe-Tree prompt and release SSOT | `prompt_presets`, `runStoryboardWidgetWorkflowProbeTree`, `Production Release` | Merged agentic-graph SHA, exact docs revision, active Chat tuple, explicit target edges, operator release authorization | Query-specific clarification cards, child-owned terminal generation, preserved multi-edge topology, exact promoted SHA, immutable deployment evidence, verified production routes | Agentic Canvas OS owns prompt metadata; agentic-graph owns runtime execution; action verbs do not imply terminal intent; selected-child output remains authoritative; no stale routing, hardcoded or zero-model cards, inferred edge, direct main push, manual mirror edit, or premature deployment. | Merge this SSOT row, pin its exact revision in agentic-graph, pass collaboration and Integration Gates, promote canonically, deploy once, and record route plus revision parity. | 2026-07-18 |
| Agentic Canvas OS programmatic tool calling API parity 2026-07-18 | Make the hosted-program controller compatible with current caller lineage and both Responses continuation modes without copying provider artifacts. | Replace legacy item vocabulary; preserve exact function caller identity; support stored and full stateless replay; keep source, fingerprints, reasoning, and intermediate outputs transient; select programs only for predictable structured reductions; fail closed on approval, mutation, semantic judgment, evidence validation, or missing capability. | `agentic-canvas-os/{agent-api/src,__tests__,docs,todo/2026-07.md}` | Programmatic Tool Calling API Parity | `createProgrammaticToolCallingRuntime`, `selectProgrammaticToolRoute` | Task shape, continuation mode, provider-normalized response items, tool declarations, caller-linked function requests, and capability evidence | Final output with compact cost and execution evidence, exact caller-preserving continuation, deterministic direct route, or typed blocked result | Provider documentation informs behavior only; Agentic Canvas OS owns an independent provider-neutral controller, application policy, validation, and proof. Generated JavaScript remains hosted; local evaluation, copied provider artifacts, fabricated live proof, paid calls, Prod mirror, and Cloudflare mutation are forbidden. | In a separately claimed agentic-graph scope, implement the downstream adapter and compare direct versus programmatic runs on one representative bounded suite only after explicit provider-spend approval. | 2026-07-18 |
| Agentic Canvas OS Tool Search deferred loading runtime 2026-07-18 | Load only optional tool definitions selected for an active session while keeping required direct tools available. | Register revision-bound grants; expose deferred metadata; resolve bounded client or hosted searches; append exact canonical definitions; require top-level loading before programs; preserve real gateway policy, costs, and honest provider evidence. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Tool Search Runtime | `createToolSearchRuntime`, `open`, `resolveClient`, `acceptHosted`, `authorize` | Session identity, catalog revision, capabilities, direct and deferred tools, metadata query, provider-normalized definitions, and cost log | Immutable initial context, exact loaded subset, typed authorization, sanitized readiness, or bounded blocked result | Agentic Canvas OS owns catalog validation and session availability; adapters own search and provider truth; agentic-graph owns execution policy. Global scans, copied artifacts, unselected schemas, program-initiated search, fabricated reduction, paid calls, Prod, and Cloudflare are forbidden. | Wire one downstream adapter in a separately claimed scope and prove actual context reduction only with approved provider spend and returned usage. | 2026-07-18 |
| Agentic Canvas OS Function Calling runtime readiness 2026-07-18 | Give models bounded access to application-owned functionality and data while preserving exact policy and evidence boundaries. | Validate strict schemas; enforce explicit tool selection and bounded parallelism; preserve response and call identity; replay active reasoning items; execute only through the real gateway; separate model and tool costs; keep live proof gated. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Function Calling Runtime | `createFunctionCallingRuntime`, `run`, `createAgentApiApp` | Run identity, JSON input, strict functions, capabilities, tool choice, approvals, normalized response items, and cost logs | Final output, exact same-id function outputs, compact evidence, separate model and gateway costs, or typed bounded block | Agentic Canvas OS owns provider-neutral orchestration and validation; adapters own protocol and capability truth; agentic-graph owns tool permission and execution. Copied artifacts, silent schema downgrade, inferred approval, unbounded calls, fabricated live proof, paid calls, Prod, and Cloudflare are forbidden. | Accepted for the bounded Dev lane with returned usage, signed review, durable continuation, matching receipt evidence, and exact persisted read-back; keep additional spend and Prod separately gated. | 2026-07-18 |
| OpenAI Responses and agentic-graph function gateway runtime 2026-07-18 | Connect the direct function controller to one concrete model protocol and existing policy owner. | Add authenticated Responses translation, explicit server model and pricing, application-owned strict schemas, an exact agentic-graph tool allowlist, policy revalidation, same-id continuation, usage cost, secret redaction, and fail-closed live proof gates. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | OpenAI Responses Function Adapter and agentic-graph Gateway | `createOpenAiResponsesFunctionAdapter`, `createAgenticGraphFunctionGateway`, `createFunctionCallingHandler` | Authenticated bounded prompt, explicit selection, server configuration, returned provider items, and agentic-graph MCP status result | Final response with exact continuation and separated costs, or typed pre-spend block | Callers cannot submit schemas, MCP names, credentials, or policy; only the explicit read-only status mapping is enabled; missing key, model, pricing, endpoint, allowlist, usage, approval, or strict output fails closed. | Accepted for one reviewed run-note mutation through the deployed Dev adapter and gateway; retain fail-closed configuration and require new approval for any further provider run. | 2026-07-18 |
| OpenAI function gateway CodeQL origin guard 2026-07-18 | Close the incomplete URL substring sanitization alert in the offline adapter fixture without changing runtime behavior. | Parse the mocked transport URL; require the exact OpenAI HTTPS origin and Responses path; rerun the function gateway suite and protected security analysis. | `agentic-canvas-os/{__tests__/openai-function-gateway.test.mjs,todo/2026-07.md}` | Function Gateway Test Transport | `fetchImpl` | Provider request URL and offline fixture bodies | Exact-origin OpenAI fixture dispatch or existing MCP fixture dispatch | A hostname substring never establishes provider identity; this change is test-only and grants no network, provider, deployment, or gateway authority. | Keep the exact-origin regression in the shared suite and require the CodeQL check to pass before protected integration. | 2026-07-18 |
| OpenAI forced function continuation readiness 2026-07-18 | Make one bounded forced status call able to finish after its required function succeeds. | Preserve the original run-level selection for validation and evidence, but send automatic provider selection on continuation after the first required gateway call; reject any repeated forced call. | `agentic-canvas-os/{agent-api/src/function-calling.js,__tests__,docs,todo/2026-07.md}` | Direct Function Calling Controller | `providerChoiceForTurn`, `run` | Forced or otherwise required selection, valid provider function call, strict gateway output, and prior response identity | One exact function call followed by provider final output, with original policy retained in compact evidence | Provider selection is turn-scoped while application policy is run-scoped; relaxing only after a completed requirement avoids a forced-call loop without weakening allowlists, schemas, call bounds, or evidence. | Accepted: the forced reviewed call completed once, the continuation returned final output, and evidence retained one function call across two provider requests; keep Prod unchanged. | 2026-07-18 |
| Agentic Canvas OS Running Agents runtime readiness 2026-07-18 | Drive complete application turns through bounded agent transitions, exclusive continuation, pause and resume, and incremental settlement. | Add one provider-neutral turn controller; lock one continuation strategy per conversation; share one loop across run and stream; retain opaque pause state; serialize active turns; bound state, events, steps, and time; report actual, partial, unreported, or not-run cost honestly. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Running Agents Runtime | `createRunningAgentRuntime`, `run`, `stream`, `resume`, `resumeStream` | Run and conversation identity, agent, JSON input, one continuation strategy, optional resume resolution, and injected agent-step adapter | Completed, paused, or blocked settlement with continuation state, compact transition evidence, bounded events, and honest aggregate cost | Agentic Canvas OS owns application-turn lifecycle only; adapters own provider translation and existing controllers retain tool, hosted-program, gateway, and durable-state policy. Copied artifacts, mixed continuation, duplicate execution, fabricated live proof, paid calls, Prod, and Cloudflare are forbidden. | Wire one downstream agent-step adapter in a separately claimed scope, then run a bounded provider proof only after explicit spend approval. | 2026-07-18 |
| Agentic Canvas OS Agent Definitions runtime readiness 2026-07-18 | Make one revision-fenced specialist definition the stable configuration unit consumed by the existing agent runtime. | Register exact model and ordered instructions; keep optional behavior reference-only; authorize capabilities externally; verify handoff targets; validate bounded outputs; expose sanitized readiness; keep live provider proof gated. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Agent Definition Registry | `createAgentDefinitionRegistry`, `register`, `prepare`, `validateOutput` | Agent identity, revision, model route, instruction blocks, optional capability references, handoff targets, and output contract | Immutable prepared packet, valid bounded output, or typed registration, preparation, authorization, handoff, revision, capacity, or validation block | Agentic Canvas OS owns definition integrity only; Running Agents owns lifecycle and existing application owners retain capability policy, execution, credentials, approvals, and cost. Copied artifacts, embedded executors, fabricated provider proof, paid calls, Prod, and Cloudflare are forbidden. | In a separately claimed agentic-graph scope, register one source-backed specialist and connect its prepared packet to the existing Running Agents adapter before any approved live-provider proof. | 2026-07-18 |
| Agentic Canvas OS Models and Providers runtime readiness 2026-07-18 | Resolve explicit model defaults and transport strategy for SDK-based agent runs without baking a provider catalog into the runtime. | Register exact provider revisions; prefer agent selection over run and process defaults; match model features and transport behavior; keep credentials and execution adapter-owned; expose sanitized readiness; gate live proof. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,wrangler.jsonc,todo/2026-07.md}` | Model Provider Registry | `createModelProviderRuntime`, `resolveModelProviderEnvironment`, `resolve` | Provider revision, model features, agent and run selections, process default, transport metadata, server environment, and exact requirements | Immutable provider, model, and transport packet or typed bounded block | Agentic Canvas OS owns deterministic selection; Agent Definitions own specialist choice; Running Agents adapters own transport execution and returned evidence. Copied artifacts, hardcoded providers, secret return, fabricated capability proof, paid calls, Prod, and Cloudflare are forbidden. | In a separately claimed agentic-graph scope, pass one prepared specialist through this resolver into a registered Running Agents adapter, then seek explicit approval for one bounded live-provider proof. | 2026-07-18 |
| Agentic Canvas OS Sandbox Agents runtime readiness 2026-07-18 | Run approved agent work in a provider-neutral container workspace with bounded state and honest containment evidence. | Separate application control from injected container compute; require exact capabilities and policy; serialize workspace operations; keep bindings secret; persist opaque snapshots and resume checkpoints externally; expose provider attestation separately from independent containment proof. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Sandbox Agents Runtime | `createSandboxAgentRuntime`, `open`, `execute`, `snapshot`, `pause`, `resume`, `close` | Run and agent identity, fresh workspace or snapshot, capability set, application authorization, container adapter, external state store, and bounded operation | Ready, completed, paused, resumed, closed, or typed blocked result with sanitized evidence and honest cost | Agentic Canvas OS owns lifecycle and policy enforcement; the injected provider owns actual container compute; the state store owns durable opaque references. Offline adapter proof cannot establish real isolation, paid execution, Prod, or Cloudflare readiness. | Wire one real container provider, authorizer, and durable state store in a separately claimed scope, then prove containment and cleanup before promoting beyond provider-attested Dev readiness. | 2026-07-18 |
| Agentic Canvas OS Probe-Tree clarification contract 2026-07-18 | Make action-topic clarification behavior explicit, source-backed, and fail-closed. | Classify RECOMMEND, COMPARE, ASSESS, and PLAN semantically; require 2-4 cards through the active Chat tuple; allow only runtime-recognized selected-child terminal bypass; forbid query hardcoding and zero-model fallback. | `agentic-canvas-os/{scripts,__tests__,docs,package.json,todo/2026-07.md}` | Probe-Tree Clarification Contract | `validateProbeTreeContractDocuments`, `probe-tree-contract:check` | Source-backed preset and `/`, `#`, `@` dictionary contracts | Typed contract validation or actionable failure before publication | The preset owns semantic classification and bounds; dictionaries project it; agentic-graph owns runtime execution. Action verbs and root aliases never imply terminal intent. | Merge through protected integration, sync canonical main, and let agentic-graph consume the exact Agentic Canvas OS docs revision. | 2026-07-18 |
| Agentic Canvas OS Docker Sandbox Provider runtime readiness 2026-07-18 | Execute the generic Sandbox Agents contract through a real local container boundary with durable state and independent proof. | Add an immutable-image Docker adapter, deny-first authorizer, atomic file checkpoint claims, internal agent networking, hardened loopback preview proxies, workspace snapshots, cross-controller resume, separate configuration and behavioral verification, and cleanup proof. | `agentic-canvas-os/{agent-api/src,scripts,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Docker Sandbox Provider | `createDockerSandboxAdapter`, `createSandboxApplicationAuthorizer`, `createSandboxFileStateStore`, `createDockerContainmentVerifier` | Approved workspace, immutable image digest, bounded operations, local Docker Engine, private state and snapshot roots | Provider-attested and independently verified local run with snapshots, resume, preview response, zero cost, and zero residual resources | Agent containers are non-root, read-only, capability-dropped, resource-bounded, and internal-only; a separate hardened proxy owns loopback preview ingress. Environment bindings, online packages, public ports, host mounts, fabricated proof, Prod, and Cloudflare remain forbidden. | Integrate a monitored hosted adapter only after defining approved image retention, multi-tenant isolation, durable storage lifecycle, and repeated independent verification. | 2026-07-18 |
| Agentic Canvas OS orchestration and handoffs runtime readiness 2026-07-18 | Make manager delegation and specialist conversation transfer explicit and auditable. | Register exact agent topology; authorize every branch; keep delegate specialists behind the source manager; transfer conversation and final-answer ownership only on handoff; enforce current-owner continuation, revision fencing, bounds, cost evidence, redaction, and provider-honest readiness. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Agent Orchestration Runtime | `createAgentOrchestrationRuntime`, `register`, `run`, `owner`, `clearConversation` | Exact workflow revision, branch, current conversation owner, bounded input, application authorizer, agent resolver, and Running Agents adapter | Source-owned delegated answer, target-owned handoff answer, explicit owner identities, compact evidence, honest aggregate cost, or typed block | Agentic Canvas OS owns topology and answer ownership only; Agent Definitions, Running Agents, gateways, model adapters, durable state, credentials, approvals, and spend retain their authority. Copied artifacts, inferred ownership, hidden routing, fabricated live proof, paid calls, Prod, and Cloudflare are forbidden. | Wire one source-backed specialist through the existing definition and running-agent owners, then seek explicit approval for one bounded live delegate and handoff proof. | 2026-07-18 |
| Agentic Canvas OS prompt preset Chat and MCP expansion 2026-07-18 | Increase the centralized prompt catalog while preserving canonical runtime ownership and provider-honest invocation. | Add four generic SME and investment variants; declare LLM or native Chat response mode plus read-only MCP token resolution; register missing runtime commands; fail closed on stale routes, unresolved tokens, query hardcoding, and zero-model LLM fallback. | `agentic-canvas-os/{scripts,__tests__,docs,package.json,todo/2026-07.md}` | Prompt Preset Invocation Contract | `validatePromptPresetContractDocuments`, `prompt-preset-contract:check` | Source-backed preset catalog, canonical command dictionary, skill variants, active Chat tuple, and MCP docs tool identity | Eleven validated presets with exact Chat mode and matching MCP runtime token, or actionable pre-publication failure | Prompt selection loads only; Chat owns response generation; MCP resolves metadata only; existing agent and native owners execute. No query fixture, duplicate runtime, inferred approval, paid call, Prod, or Cloudflare mutation. | Merge through protected integration, sync canonical main, and update the agentic-graph consumer only if its focused source-backed catalog tests expose a contract gap. | 2026-07-18 |
| Agentic Canvas OS integrated agent runtime composition 2026-07-18 | Connect one source-backed definition to exact model selection, Running Agents lifecycle, and explicit orchestration ownership. | Require exact source URI and digest verification; compose definition preparation, provider selection, lifecycle continuation, output validation, delegation, and handoff; keep live provider execution separately gated. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Agent Runtime Composition | `createAgentRuntimeComposition`, `resolveAgent`, `runAgent` | Exact agent and source revision, registered provider and transport, orchestration role and branch, bounded input, and injected step adapter | Validated manager-owned delegation or specialist-owned handoff output with compatible continuation and honest cost, or bounded block | Existing definition, provider, lifecycle, and orchestration owners remain authoritative; source or model drift, invalid output, missing adapter, copied artifacts, paid calls, Prod, and Cloudflare fail closed. | With separate spend approval, configure one bounded live delegate and handoff proof and record returned usage without deploying. | 2026-07-18 |
| Agentic Canvas OS bounded live agent provider proof 2026-07-18 | Prove one composed manager delegation and specialist handoff against a real provider without changing Worker defaults. | Require explicit key, model, price, approval, three-call ceiling, source digests, stable instructions, stored continuation, effective context, usage, redaction, and no deploy. | `agentic-canvas-os/{agent-api/src,__tests__,scripts,docs,README.md,package.json,todo/2026-07.md}` | Live Agent Provider Proof | `createOpenAiResponsesAgentAdapter`, `runLiveAgentProviderProof` | Approved OpenAI Responses configuration plus source-backed manager and specialist definitions | Manager-owned delegation, specialist-owned handoff, hash-linked continuation, returned usage and cost, or bounded redacted block | The Node proof owns provider translation only; existing definition, provider, lifecycle, composition, and orchestration owners retain policy. Default Worker, raw response data, repeated paid runs, Prod, and Cloudflare remain forbidden. | Keep the proof opt-in; expand only with separate approval for a distinct capability such as live streaming or tool execution. | 2026-07-18 |
| Agentic Canvas OS Probe-Tree semantic owner 2026-07-18 | Give semantic clarification behavior one dedicated source-backed owner. | Add `PROBE-TREE.md`; classify RECOMMEND, COMPARE, ASSESS, and PLAN semantically and case-insensitively; validate preset and dictionary projections; preserve 2-4 cards, selected-child terminal ownership, active Chat routing, and fail-closed fallback policy. | `agentic-canvas-os/{docs,scripts,__tests__,todo/2026-07.md}` | Probe-Tree Semantic Clarification Contract | `validateProbeTreeContractDocuments`, `probe-tree-contract:check` | Canonical Probe-Tree document plus prompt-preset and dictionary projections | One validated semantic contract or actionable zero-spend failure before publication | Meaning and missing decision variables own classification; keyword case, action verbs, root aliases, query fixtures, stale routes, and zero-model cards cannot establish completion. | Keep agentic-graph consuming the validated preset projection and rerun focused contract checks whenever topic or continuation semantics change. | 2026-07-18 |
| Agentic Canvas OS progressive Agents runtime readiness 2026-07-18 | Expose one bounded application path that starts with an exact agent, adds tools through existing owners, and adds specialists only through explicit orchestration. | Add a provider-neutral facade over Agent Definitions, Agent Runtime Composition, Function Calling, and Agent Orchestration; validate strict requests, exact final-answer ownership, sanitized readiness, and no-copy boundaries. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Progressive Agents Runtime | `createProgressiveAgentsRuntime`, `registerAgent`, `executeAgent`, `registerWorkflow`, `executeWorkflow` | Source-backed definition, exact run identity and revision, optional authorized tool references, or exact specialist workflow branch | Agent-owned direct output, manager-owned delegation, specialist-owned handoff, or bounded block | Existing owners retain source, model, tool, lifecycle, provider, approval, and answer policy; copied SDK artifacts, inferred routing, paid calls, Prod, and Cloudflare remain forbidden. | Add a live tool-bearing provider proof only under separate spend approval; keep the default Worker unconfigured. | 2026-07-18 |
| Agentic Canvas OS guardrails and human review runtime readiness 2026-07-18 | Add automatic validation and review-gated sensitive actions to source-backed agent workflows. | Run ordered input and output checks around composition; keep tool checks beside gateways; pause through Running Agents; consume one approve, reject, or edit decision; revalidate edits; enforce expiry, replay, capacity, evidence, and provider-honest boundaries. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Guardrails and Human Review Runtime | `createGuardrailsHumanReviewRuntime`, `validate`, `requestReview`, `resolveReview` | Exact run, conversation, agent revision, guardrail references, bounded values, proposed action, and typed decision | Validated or transformed value, approval interruption, approved, rejected, edited, audit, or bounded block | Agentic Canvas OS owns validation and review state only; Running Agents owns turn lifecycle and real gateways retain authorization, side effects, cost, and audit. Copied artifacts, inferred approval, replay, paid calls, Prod, and Cloudflare are forbidden. | Inject durable atomic review and paused-turn stores before claiming restart-safe recovery; prove one live tool approval only under separate provider-spend authorization. | 2026-07-18 |

## 2026-07-19
| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Agentic Canvas OS durable human review and guarded function gateway runtime readiness 2026-07-19 | Make delayed human review survive Worker isolate changes without accepting caller-authored approval claims. | Add per-identity atomic state, exact signed reviewer evidence, durable paused-turn claims, guarded status-tool execution, strict HTTP fields, sanitized readiness, and offline recovery proof. | `agentic-canvas-os/{agent-api/src,worker,__tests__,docs,README.md,package.json,wrangler.jsonc,todo/2026-07.md}` | Durable Human Review Gateway Runtime | `AgentState`, `createDurableObjectHumanReviewStore`, `createDurableObjectPausedTurnStore`, `mintReviewerToken`, `createAgenticGraphFunctionGateway` | Exact review, run, conversation, action digest, reviewer token, paused-turn snapshot, and allowlisted status call | Atomic pause, authenticated decision, guarded MCP output, cross-isolate resume, durable settlement, or typed zero-spend block | Session auth never authorizes review; one Durable Object owns each state atom; invalid evidence is non-consuming; guardrails bracket execution; provider calls, reviewed mutations, Prod, and Cloudflare remain unverified and unchanged. | Add a manager-owned Function Calling continuation surface that durably preserves the provider response chain across a long human-review delay before attempting any reviewed live side effect. | 2026-07-19 |
| Agentic Canvas OS durable Function Calling review continuation runtime readiness 2026-07-19 | Resume the exact provider tool-call chain after delayed human review without replaying model or gateway work. | Add serializable Function Calling checkpoints, an atomic per-run continuation manager, exact tool-revision fencing, authenticated resume input, response-id and reasoning replay, bounded expiry, retry-safe reviewer failures, readiness evidence, and cross-isolate offline proof. | `agentic-canvas-os/{agent-api/src,worker,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Durable Function Calling Continuation Runtime | `createFunctionCallingRuntime`, `createFunctionCallingManager`, `createDurableObjectFunctionContinuationStore`, `createFunctionCallingResumeHandler` | Paused provider response, exact function call, reasoning items, durable resume token, signed reviewer resolution, and current tool registry | Atomic run reservation, sanitized review pause, claimed resume, same-call-id gateway completion, previous-response continuation, final output, or typed bounded block | The manager owns continuation state only; provider adapters retain response truth, gateways retain authorization and review, invalid evidence remains retryable, schema drift fails closed, and no paid call, mutation, Prod, or Cloudflare action is authorized. | Add native gateway execution receipts and idempotency fencing before enabling any reviewed mutating side effect, then seek separate approval for one bounded live provider proof. | 2026-07-19 |
| Agentic Canvas OS reviewed function execution receipt runtime readiness 2026-07-19 | Fence every reviewed side effect with durable authorization, stable idempotency, and replayable terminal evidence. | Reserve a durable receipt before review; persist authorized arguments before MCP; atomically claim execution; forward one stable key; require native mutation receipt echo; replay completed output; expose sanitized readiness; prove uncertain retry without duplicate mutation. | `agentic-canvas-os/{agent-api/src,src,worker,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Function Execution Receipt Runtime | `createFunctionExecutionReceiptRuntime`, `createDurableObjectFunctionExecutionReceiptStore`, `createAgenticGraphFunctionGateway`, `callTool` | Exact run, call, tool revision, reviewed arguments, receipt state, execution claim, and downstream receipt evidence | Reserved or authorized receipt, one fenced MCP attempt, stable-key retry, durable completed output, terminal replay, or typed fail-closed block | Agentic Canvas OS owns pre-execution fencing and local replay; native agentic-graph mutations must deduplicate the forwarded key and echo its digest. Synthetic offline mutation proof cannot authorize a real mutation, paid provider call, Prod, or Cloudflare action. | In a separate agentic-graph scope, implement native receipt echo and dedupe storage for one approved mutating tool, then add cross-repository recovery proof before live provider use or deployment. | 2026-07-19 |
| Cross-repository run-note execution receipt recovery 2026-07-19 | Complete one reviewed real mutation without duplicate side effects after an uncertain result. | Register immutable reviewed `update_agent_run_note`; atomically update the native per-run manifest and receipt; echo exact execution identity; retry with the same key; prove terminal replay across fresh Agentic gateways. | `agentic-canvas-os/{agent-api/src,scripts,__tests__,docs,README.md,package.json,todo/2026-07.md}` and `agentic-graph/cloudflare/workers/agentic-graph-mcp` | Reviewed Run Note Recovery | `createAgenticGraphFunctionGateway`, `dispatchAgenticGraphMcpToolCall`, `RunNoteExecutionStore.execute` | Signed review, exact run and call identity, bounded run note, stable idempotency key, native Durable Object state | One revision, native applied or replayed receipt, completed Agentic receipt, or typed retryable block | Review cannot be disabled; metadata stays outside model arguments; manifest and receipt commit atomically; payload drift conflicts; offline proof makes zero provider calls and does not authorize deployment. | Accepted in Dev: one completed application receipt and matching applied native receipt produced exactly one persisted note revision; retain replay regression and keep Prod separately gated. | 2026-07-19 |
| Agentic Canvas OS bounded reviewed Function Calling Dev proof lane 2026-07-19 | Execute one provider-driven run-note workflow with durable human review and native receipt evidence while isolating production. | Add route-free Dev Workers, fail-closed deployment inputs, exact review-state derivation, authenticated paused-envelope recovery, a two-turn and one-call ceiling, response-id and usage evidence, application plus native receipts, and persisted read-back validation. | `agentic-canvas-os/{agent-api/src,scripts,__tests__,docs,README.md,package.json,wrangler.jsonc,todo/2026-07.md}` and `agentic-graph/cloudflare/workers/agentic-graph-mcp` | Live Reviewed Function Proof | `buildDevFunctionGatewayDeployArgs`, `runLiveFunctionRunNoteProof`, `recover` | Explicit Dev approval, current model and prices, Dev-only secrets and URLs, one dry-run manifest or one known paused run id | One logical provider run, two Responses requests, one reviewed mutation, matching durable receipts, exact revision 1, sanitized evidence, or fail-closed block | Secrets never enter CLI vars or output; only `*-dev.workers.dev` URLs pass; recovery cannot restart the model or expose private continuation state; native evidence must match; production routes and Prod deployment remain forbidden. | Accepted: the recovered run completed with 546 input and 55 output tokens, USD 0.000876 estimated cost, one completed application receipt, one applied native receipt, and persisted revision 1. Keep another paid run and all Prod, Pages, or custom-domain deployment separately gated. | 2026-07-19 |
| agentic-graph Docs Sheets Slides live-provider export runtime | Implement source-backed spreadsheet and presentation export from generated Rich Media without overwriting the authored Widget input. | Implement deterministic Markdown table and slide conversion, register `export.publish`, preserve Widget input topology, create separate Rich Media outputs, enforce provider OAuth and idempotent ledger identity, and run bounded Google and Microsoft account acceptance without Prod or Cloudflare. | `agentic-canvas-os/{docs/{START-WORKFLOW.md,TODO.md,VALIDATION-RUNBOOK.md},todo/2026-07.md}` and `agentic-graph/{canvas/src,grph-shared/src,mcp,scripts,docs/documents,package.json}` | Docs Sheets Slides Export Runtime | `runExportPublishTool`, `buildSpreadsheetArtifactFromMarkdown`, `buildPresentationArtifactFromMarkdown` | Source-bound Markdown Rich Media, artifact kind, provider preference, environment-only credentials, and exact source revision | Separate connected Rich Media outputs, deterministic XLSX or PPTX bytes, stable provider document identity and URL, hash-linked export ledger, or typed fail-closed result | agentic-graph owns conversion and provider execution; Agentic Canvas OS owns planning. Source drift, missing credentials, duplicate identity, unsafe path, or provider verification failure blocks readiness; fallback is bounded; Prod and Cloudflare remain unchanged. | Complete protected agentic-graph integration, then run one bounded spreadsheet and slide export twice per configured real account, verify stable identities and read-back, and record credentials or account access as explicit blockers when unavailable. | 2026-07-19 |
| agentic-graph multilingual TextGeneration output-version protected release 2026-07-19 | Promote the verified multilingual clarification and selectable Rich Media output-version runtime without changing authored Widget input. | Bind the release to exact app, docs, and catalog revisions; rerun paired runtime and immutable-manifest gates; publish only through protected integration; deploy once; verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs/runtime-readiness-contract.md,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Multilingual Output Version Release Ledger | `collaboration:gate`, `release:manifest:create`, `ci:integration`, `pages:sync`, `release.yml` | Exact protected agentic-graph SHA, matching Agentic Canvas OS and catalog revisions, clean generated mirror scope, and operator deployment approval | Round-tripped immutable manifest, one promoted mirror, one Cloudflare deployment, and verified root and `/agentic-graph` route evidence | Stop on ownership ambiguity, ledger failure, revision mismatch, test failure, mirror drift, absent credentials, partial deployment, or live evidence mismatch; never patch generated output manually. | Complete protected planning integration, refresh the agentic-graph docs pin to that exact revision, validate the final candidate, then dispatch the guarded production workflow once. | 2026-07-19 |
| agentic-graph Cloudflare D1 canonical-path seed reconciliation production release 2026-07-19 | Restore canonical Source Files seeding when a legacy D1 row already owns the workspace path, then complete the authorized production promotion. | Preserve the existing canonical-path owner, resolve chunk writes through that identity, prove legacy and new-row behavior, require protected integration and collaboration evidence, regenerate the mirror canonically, deploy once, verify D1 parity, and verify both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{scripts,canvas/src/__tests__,package.json}`; `huijoohwee/content/agentic-graph` | D1 Canonical Seed Release Ledger | `buildDirectD1DocumentStatements`, `storage:d1:seed:docs`, `collaboration:gate`, `pages:deploy-cloudflare` | Exact protected app and docs revisions, legacy conflicting D1 row, deterministic seed records, runtime proof, and operator deployment authorization | Preserved canonical document identity, correctly attached chunks, complete Source Files seed count, exact promoted mirror, one Cloudflare deployment, and verified production routes | agentic-graph owns source and D1 reconciliation; Agentic Canvas OS owns planning; generated mirrors remain command-owned; external 8th Wall material remains inspiration-only; copied dependencies, hardcoded fixtures, direct main pushes, manual production patches, and pre-integration deployment are forbidden. | Merge this ledger, bind validation to its exact revision, publish the protected agentic-graph patch, promote canonically, deploy once, and record D1 plus route parity. | 2026-07-19 |

## 2026-07-20

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| agentic-graph Source Files verified cloud sync production release 2026-07-20 | Promote the merged per-file cloud status and canonical upload verification path through the authorized production workflow. | Bind the release to exact app, docs, and catalog revisions; prove authenticated two-peer runtime parity, canonical GitHub-first upload and D1 read-back contracts, command-owned mirror regeneration, one Pages deployment, D1 reconciliation, and both production routes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src/features/source-files,canvas/src/features/markdown-workspace,docs}`; `huijoohwee/content/agentic-graph` | Source Files Cloud Sync Release Ledger | `syncSourceFileToCanonicalCloud`, `collaboration:readiness:check`, `pages:deploy-cloudflare`, `storage:d1:seed:docs` | Protected agentic-graph merge `ead920e8`, exact Agentic Canvas OS revision, fresh catalog, generated mirror, and operator deployment authorization | Visible local, checking, uploading, cloud, unavailable, and retry states; byte-equal canonical read-back; exact promoted assets; one production deployment; verified routes | agentic-graph owns runtime and upload verification; Agentic Canvas OS owns planning; GitHub remains canonical and D1 remains a reconciled projection; no caller credentials, direct main pushes, manual mirror edits, invented aliases, or deployment before protected evidence. | Merge this ledger, validate the exact app/docs pair, regenerate the mirror canonically, deploy once, verify D1 and HTTP evidence, and report residual generated-mirror state. | 2026-07-20 |
| agentic-graph Source Files legacy-root reconciliation production release 2026-07-20 | Include the protected removal of obsolete Agentic OS and video-run Source Files roots in the authorized cloud-sync production release. | Rebind the release to final app revision `eea1a6ae`; preserve canonical workspace reconciliation; rerun two-peer, immutable-manifest, integration, mirror, D1, asset, and route gates; deploy exactly once only after protected planning integration. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src/features/workspace-fs,canvas/src/features/markdown,canvas/src/features/markdown-workspace,docs}`; `huijoohwee/content/agentic-graph` | Source Files Legacy Root Release Ledger | `reconcileLegacyWorkspaceSourceRoots`, `collaboration:readiness:check`, `release:manifest:create`, `pages:deploy-cloudflare` | Protected agentic-graph merges `6cdff504` and `eea1a6ae`, exact Agentic Canvas OS revision, canonical workspace contents, generated mirror, and deployment authorization | Obsolete roots removed, canonical files retained, exact collaboration digest and manifest, one production deployment, D1 parity, and verified assets and routes | agentic-graph owns workspace reconciliation; Agentic Canvas OS owns planning; any revision drift, duplicate root, validation failure, generated-mirror mutation, partial deployment, or live mismatch stops release without manual patching. | Merge this ledger, bind final validation to its merge SHA, deploy the command-generated candidate once, and record Cloudflare, D1, asset, and route evidence. | 2026-07-20 |
| Agentic Canvas OS Motion Control invocation catalog 2026-07-20 | Assign canonical invocation metadata without conflating catalog truth with runtime proof. | Add `/motion.control` and `#pose`; reuse `@canvas`; cross-link FACTS; keep Agentic Canvas OS source-catalog-only; assign camera, inference, XR, MCP execution, runtime proof, and deployment behavior to protected agentic-graph implementation. | `agentic-canvas-os/{docs/{DICTIONARY-COMMAND.md,DICTIONARY-SEMANTIC.md,FACTS.md},todo/2026-07.md}` and `agentic-graph/{canvas/src,mcp,docs}` | Motion Control Invocation Catalog | `/motion.control`, `#pose`, `@canvas` | Motion Control source intent, Canvas binding, pose semantic, and exact Agentic Canvas OS revision | Revision-bound catalog metadata and agentic-graph implementation handoff without runtime or deploy proof | Agentic Canvas OS owns token definitions and truth resolution; agentic-graph owns UI, camera permission, LiteRT inference, XR projection, MCP execution, validation, and runtime proof; no catalog entry is live evidence. | Merge the upstream catalog through protected Dev integration, pin its exact revision in agentic-graph, then validate the agentic-graph implementation without Prod or Cloudflare. | 2026-07-20 |
| Agentic Canvas OS canonical XR physics share seed 2026-07-20 | Make the approved Motion Control physics document durable in the default public Source Files workspace. | Promote the byte-identical workspace seed into the canonical storage inventory; preserve its local-only runtime claim, invocation, and no-copy boundaries; advance agentic-graph's exact docs pin; reseed D1 only after both protected merges; verify byte identity and the supplied share route. | `agentic-canvas-os/{docs/workspace-seeds,todo/2026-07.md,memory/2026-07.md}` and `agentic-graph/{docs/runtime-readiness-contract.md,scripts}` | Canonical XR Physics Workspace Seed | `storage:d1:seed:docs` | Approved merged agentic-graph seed, protected Agentic Canvas OS merge, exact docs pin, and explicit D1 publication authority | One canonical default-storage projection, 45 active default documents, exact read-back hash, and browser-verified share identity | Agentic Canvas OS owns the default-storage projection inventory; agentic-graph owns runtime and D1 reconciliation; the share resolver remains generic. Absent source, pin drift, copied external artifacts, validation failure, or non-exact read-back blocks completion. | Merge both protected PRs, seed from the pinned Agentic Canvas OS checkout, repeat direct document and share browser proof, and leave Pages undeployed. | 2026-07-20 |
| agentic-graph production Dev fidelity restoration | Restore the production apex and `/agentic-graph` routes from the exact protected Dev source after generated mirror drift. | Preserve Dev as source of truth; prove route ownership and cross-device identity; merge this ledger; pin its revision; regenerate the mirror canonically; deploy one exact protected SHA; verify apex Home and `/agentic-graph` workspace fidelity. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,cloudflare/pages,scripts,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Production Dev Fidelity Release Ledger | `collaboration:gate`, `pages:sync`, `conflict:mirror:check`, `Production Release` | Exact Dev app and docs revisions, stale generated mirror evidence, protected integration, and explicit Cloudflare deployment authorization | One command-generated mirror, one protected Pages deployment, apex Live Canvas Home, `/agentic-graph` workspace, and exact revision evidence | agentic-graph owns authored runtime and release automation; Agentic Canvas OS owns planning; generated output stays command-owned; any ledger, identity, parity, deployment, or route mismatch stops without manual production patching. | Merge this ledger first, pin its exact SHA in agentic-graph, complete Integration Gate, dispatch the guarded release once, and record immutable deployment and route evidence. | 2026-07-20 |
| Agentic ECS invocation catalog 2026-07-20 | Publish exact source-backed grammar for the native agentic-graph entity-component-system workflow without turning catalog metadata into runtime proof. | Register `/ecs.session-start`, `/ecs.world-tick`, `/ecs.decision-persist`, `#agentic-ecs`, and `@ecs-session`; bind them to the three exact agentic-graph MCP tools; preserve the private bounded session lifecycle, real cost evidence, and Dev-only boundary. | `agentic-canvas-os/{docs/{DICTIONARY-COMMAND.md,DICTIONARY-SEMANTIC.md,DICTIONARY-BINDING.md,FACTS.md},todo/2026-07.md}`; `agentic-graph/{ecs,contracts,mcp,canvas/src,docs}` | Agentic ECS Invocation Catalog | `agentic-graph.ecs.session_start`, `agentic-graph.ecs.world_tick`, `agentic-graph.ecs.decision_persist` | Validated KGC Markdown source, exact catalog revision, and one private ECS session binding | Revision-bound invocation truth plus protected agentic-graph implementation and focused Dev proof | Agentic Canvas OS owns token definitions and truth resolution; agentic-graph owns hydration, execution, decision persistence, Canvas projection, and MCP transport; neither catalog metadata nor a local check authorizes Prod or Cloudflare. | Merge the catalog through protected integration, pin its exact SHA in agentic-graph, run focused ECS and collaboration proof, then merge the runtime without deployment. | 2026-07-20 |
| Agent Swarm horizontal scaling runtime readiness 2026-07-20 | Add native horizontal agent scaling without predefined roles or handcrafted workflows. | Add one goal-only dynamic planner, atomic durable run ledger, ephemeral worker claims, bounded parallel execution, recovery, cancellation, receipts, centralized base-agent synthesis, sanitized readiness, invocation metadata, and focused proof; preserve no-copy, no-spend-default, Dev-only boundaries. | `agentic-canvas-os/{agent-api/src,worker,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Agent Swarm Runtime | `createAgentSwarmRuntime`, `start`, `work`, `settle`, `status`, `cancel`, `run` | Exact base-agent revision, bounded goal and context, optional concurrency ceiling, injected planner, worker, synthesizer, authorizer, and atomic store | Durable dynamic task ledger, secret-free trace, receipts, honest cost, one base-agent final output, or typed bounded block | Authorization precedes planning; caller roles, tasks, and workflows fail closed; dependency-ready tasks claim atomically; execution occurs outside the lock; stale output is fenced; effects require exact receipts; only the base agent synthesizes. | Keep default Worker adapters unconfigured; add live provider or mutating execution only under separate approval, exact owner policy, spend bounds, and post-integration proof. | 2026-07-20 |
| Agent Toolkit intelligence runtime readiness 2026-07-20 | Instrument, profile, evaluate, compare, optimize, and propose reviewed improvements for heterogeneous agent teams without replacing their frameworks. | Add framework-neutral lifecycle adapters, redacted bounded telemetry, trust-labelled deterministic metrics, digest-bound candidate comparison, per-principal abuse controls, percentile/bottleneck profiling, deterministic quality/latency/cost recommendations, and review-gated learning proposals; preserve existing runtime ownership; forbid raw content capture, automatic promotion, vendor dependencies, copied artifacts, and unapproved deployment. | `agentic-canvas-os/{agent-api/src,worker,__tests__,docs,README.md,package.json,todo/2026-07.md}` and `agentic-graph/{docs/runtime-readiness-contract.md,mcp}` | Agent Toolkit Runtime | `createAgentToolkitRuntime`, `instrument`, `profile`, `evaluate`, `compare`, `optimize`, `propose` | Application-authorized target, candidate, adapter, evaluator, dataset, and metric revision digests, sanitized lifecycle signals, unique evidence references, candidate policies, budgets, and a stable authenticated principal | Durable metadata-only trust-labelled traces, bounded structured exporter events, latency distributions and bottlenecks, immutable evaluation packets, deterministic compare and optimization decisions, review-pending learning proposals, or typed bounded blocks | Existing runtimes keep execution, authorization, effects, scheduling, and final-answer ownership; the Toolkit excludes remote-unverified comparison evidence, binds trusted evidence to one target/adapter/operation/profile cohort, enforces atomic request/run/cohort quotas, and never promotes or applies automatically. | Merge Agentic runtime proof first; advance agentic-graph's exact docs pin and proof tokens in a separate protected scope; rerun current-revision catalog hydration and two-peer identity proof without Prod or Cloudflare. | 2026-07-20 |
| Agentic Canvas OS Camera and XR scene invocation catalog 2026-07-20 | Publish exact source-backed Camera source-selection and XR scene grammar without creating a second runtime owner. | Register the 15 command, semantic, and binding tokens; cross-resolve FACTS; add focused parity and compacted-value checks; keep browser-local agentic-graph as execution/persistence owner and Agentic ECS as the separate stdio/KGC lane. | `agentic-canvas-os/{docs/{DICTIONARY-COMMAND.md,DICTIONARY-SEMANTIC.md,DICTIONARY-BINDING.md,FACTS.md},scripts,__tests__,package.json,todo/2026-07.md}`; `agentic-graph/{canvas/src/features/three,canvas/src/features/strybldr}` | Camera and XR Scene Invocation Catalog | `validateXrInvocationContractDocuments`, `agentic-graph.control_local_camera`, `agentic-graph.control_local_xr_scene` | `/camera.select`; seven `/xr.*` commands; six `#` semantics; `@scene`; exact source-owner boundaries | Revision-bound invocation truth, focused drift failures, and protected ACOS integration without runtime or deployment claims | Agentic Canvas OS owns token definitions and direct resolution; browser-local agentic-graph owns Camera/XR execution and persistence; Agentic ECS remains a separate three-tool composition lane; Prod and Cloudflare stay closed. | Merge ACOS through protected Dev integration, send the exact integrated SHA to the existing agentic-graph owner, then refresh downstream catalog hydration and retire local fallbacks there. | 2026-07-20 |
| Agentic Canvas OS Camera and XR Markdown table shape contract 2026-07-20 | Preserve complete rendered Camera and XR invocation rows after the protected catalog integration. | Replace ambiguous inline pipes with prose, validate every owned dictionary row's exact Markdown column count, and prove malformed rows fail before protected publication. | `agentic-canvas-os/{docs/DICTIONARY-COMMAND.md,scripts/xr-invocation-contract.mjs,__tests__/xr-invocation-contract.test.mjs,todo/2026-07.md}` | Camera and XR Table Shape Contract | `validateXrInvocationContractDocuments`, `splitMarkdownTableRow` | Integrated XR invocation rows and GitHub-Flavored Markdown table delimiters | Complete five-column command rows, complete four-column semantic and binding rows, focused malformed-row failure, and protected ACOS integration | Agentic Canvas OS owns rendered dictionary shape; agentic-graph runtime ownership and ECS separation remain unchanged; no downstream mask, direct main push, Prod, or Cloudflare action is allowed. | Merge the smallest protected follow-up, provide its exact integrated SHA to the existing agentic-graph owner, then resume downstream catalog pinning and browser proof. | 2026-07-20 |
| agentic-graph worktree cleanup production release 2026-07-20 | Remove stale linked checkout storage and promote the exact protected Dev and docs pair through the authorized production workflow. | Remove only audited registered worktrees; preserve branch history and unrelated canonical files; bind promotion to exact protected revisions; require collaboration, immutable-manifest, mirror, D1, Cloudflare, and live-route proof. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{docs/runtime-readiness-contract.md,scripts,.github/workflows/release.yml}`; `$GITHUB_ROOT/.worktrees` | Worktree Cleanup Release Ledger | `git worktree remove`, `git worktree prune`, `collaboration:gate`, `Production Release` | Audited clean or generated-residue worktrees, preserved named branches, exact app and docs revisions, and explicit deployment authorization | Empty worktree registry, protected integrated docs pin, one verified Pages deployment, D1 parity, exact asset SHA, and passing production routes | Git owns worktree registration and branch retention; Agentic Canvas OS owns planning; agentic-graph owns authored release automation; generated mirrors stay command-owned; ambiguous dirt, unpreserved commits, failed gates, or live drift stop release. | Merge this ledger, pin its exact revision in agentic-graph, complete protected integration, dispatch one guarded release, and report cleanup plus production evidence. | 2026-07-20 |
| Cross-repository session worktree lifecycle runtime readiness 2026-07-20 | Prevent completed task checkouts from accumulating without risking unfinished or ambiguous work. | Record merged completion in the writer lease; classify every registered worktree; retain canonical, active, delivery, and parked lanes; fail closed on dirt or ambiguity; remove only an explicitly targeted clean detached exact-main completed checkout without force or branch deletion. | `agentic-canvas-os/{scripts,__tests__,docs,package.json,todo/2026-07.md}`; `agentic-graph/{scripts,package.json,docs}`; `huijoohwee/{scripts,package.json}` | Session Worktree Lifecycle Runtime | `completeSession`, `buildLifecycleReport`, `cleanupCompletedWorktree`, `worktree:lifecycle:check`, `worktree:lifecycle:cleanup` | Registered worktree registry, exact fetched main SHA, writer-lease state, checkout dirt, and explicit cleanup target | Typed ready or attention-required report, preserved active or parked lane, safely removed completed checkout with preserved branch, or actionable refusal | Completion proof creates the cleanup fence; canonical and unfinished worktrees are never removal candidates; cleanup uses non-force Git removal and preserves branches; no session-end action authorizes Prod, Cloudflare, or branch deletion. | Expose the centralized command in each participating repository, keep focused lifecycle tests in the source owner, and run cleanup only after the task reaches completed state. | 2026-07-20 |

## 2026-07-21

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canonical lifecycle and production runtime-readiness enforcement 2026-07-21 | Keep Dev, Cloudflare, apex, `/agentic-graph`, and the generated mirror bound to one exact protected source chain. | Prevalidate remote revisions, retain last-known-good state, hash dirty evidence, enforce one production owner, publish strict v2 attestations, automate docs promotion, and require browser fidelity before mirror publication. | `agentic-canvas-os/{scripts,__tests__,docs,.github,todo}`; `agentic-graph/{scripts,.github,cloudflare,docs}`; `huijoohwee/{scripts,.github}` | Canonical Lifecycle And Runtime Attestation | `createCanonicalWorkspaceSynchronizer`, `validateProductionRuntimeReadiness`, `Production Release` | Protected app and docs revisions, required check runs, build artifact, immutable manifest, public surfaces, and mirror candidate | Prevalidated fast-forward, exact v2 readiness JSON, browser-proven apex and subpath fidelity, automatic rollback, and verified mirror publication | Any dirty checkout, failed check, revision drift, HTML fallback, missing marker, browser mismatch, or deployment failure preserves the prior runtime and blocks publication. | Merge the Agentic schema first, pin it automatically in agentic-graph, merge the release enforcement, then validate the generated mirror policy against the published v2 record. | 2026-07-21 |
| Physics Playground default startup production release 2026-07-21 | Start Home, the full workspace, and MainPanel Settings from one source-backed XR physics document across Dev and Cloudflare. | Bind Home, `/agentic-graph` cold initialization, and MainPanel Settings to the canonical physics seed; preserve explicit document and imported-embed ownership; validate focused startup contracts, protected integration, exact-SHA mirror generation, one Cloudflare deployment, and public browser fidelity. | `agentic-canvas-os/{todo/2026-07.md,docs/workspace-seeds}`; `agentic-graph/{canvas/src,docs,.github/workflows/release.yml}`; `huijoohwee/content/agentic-graph` | Physics Playground Startup Release Ledger | `resolveCanonicalStartupCanvasEmbedRuntimeUrl`, `resolveWorkspaceStartupActivePath`, `Production Release` | Canonical physics seed, protected docs and app revisions, clean generated mirror, and explicit Cloudflare release authority | Physics Playground on apex Home, cold `/agentic-graph`, and the Settings preset with exact source, renderer, mirror, and deployment parity | Agentic Canvas OS owns the published seed and planning record; agentic-graph owns initialization and release automation; generated mirrors remain command-owned; explicit deep links, imported embeds, or non-initialization persisted documents win; any gate or fidelity mismatch blocks deployment. | Merge this ledger, advance agentic-graph's exact docs pin, integrate the application change, release the verified SHA once, and record Dev plus public browser proof. | 2026-07-21 |
| agentic-graph Home Apex overlay ownership fidelity release 2026-07-21 | Keep the editorial Home overlay visible when its source-backed Physics Playground background selects XR or 3D after bootstrap. | Make Home overlay viewport ownership renderer-independent; suppress parent renderer, HUD, and workspace inset while Home owns the canvas; preserve ordinary `/agentic-graph` renderer ownership; validate focused source contracts, production build, protected integration, exact-SHA release, and public browser fidelity. | `agentic-canvas-os/todo/2026-07.md`; `agentic-graph/{canvas/src,docs,.github/workflows/release.yml}` | Home Apex Overlay Ownership | `CanvasViewport`, `LiveCanvasHeroLazy`, `Production Release` | Apex route ownership, resolved render mode, canonical Physics Playground share route, protected app and docs revisions | One full-viewport Home overlay over its isolated background, no parent renderer or HUD collision, unchanged `/agentic-graph`, or a blocked release | Route ownership outranks renderer mode for Home presentation; the isolated iframe owns its background renderer while the parent owns editorial overlay and controls. Any residual inset, duplicate renderer, failed gate, revision drift, or public mismatch blocks release. | Merge this ledger, advance agentic-graph's exact docs pin, merge the focused owner fix through protected integration, then accept only an automatic release whose apex and `/agentic-graph` browser proofs match Dev. | 2026-07-21 |
| Home Apex single Physics source authority cleanup 2026-07-21 | Make the canonical XR Physics Playground the only authored world behind Home and its optional Game Mode projection. | Delete fallback, renamed, conditional, duplicate, stale, and legacy arena/environment producers at their upstream owners; bind Home and Game Mode to the Physics seed; add static source guards and lifecycle proof; forbid downstream visibility or layering patches. | `agentic-canvas-os/{docs/workspace-seeds/agentic-graph-physics-playground-demo.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs}` | Home Physics Scene Authority | `run_ready_demo.id=xr-physics`, `GameFpsMissionStage`, `XrPhysicsRunReadyDemoRuntime` | Canonical Physics source identity, Home share bootstrap, optional Game Mode activation, and protected app/docs revisions | One authored XR world with an optional actor/camera/control overlay, or a fail-closed unsupported state; no alternate arena, terrain, environment, renderer, or standalone Home game source | The applied Physics document owns Home scene identity and composition; Game Mode may add only its mission overlay. Any source capable of materializing another world, surviving a source handoff, or reactivating through a stale identity is a conflict to remove, not suppress. | Merge this source contract, pin its exact docs revision in agentic-graph, integrate the upstream owner deletion with focused static and lifecycle tests, then require Dev and public Home browser proof before release acceptance. | 2026-07-21 |

## 2026-07-22

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Workspace seed single-source authority cleanup 2026-07-22 | Keep one authored workspace-seed owner while retaining only validated runtime projections. | Author seeds only in agentic-graph docs; require the Agentic default-storage projection to remain byte-identical; delete publish-repository copies; reject divergent, stale, fallback, conflicting, and legacy variants through source validation. | `agentic-graph/{docs/workspace-seeds,scripts}`; `agentic-canvas-os/{docs/FACTS.md,todo/2026-07.md}`; `huijoohwee/docs`; `huijoohwee.github.io/schema/AgenticRAG` | Workspace Seed Authority Contract | `verifyWorkspaceSeedAuthority`, `workspace-seeds:authority` | Canonical Physics seed, pinned Agentic projection, publish repository tree, and downstream schema map | One authored agentic-graph seed, one exact storage projection, zero publish copies, and a source-backed downstream ownership statement | agentic-graph owns authoring; Agentic Canvas OS projects exact bytes for default storage; the publish repository owns only generated release output; any independently editable copy fails closed. | Merge the docs contract, delete the publish duplicate, integrate the agentic-graph guard, regenerate AgenticRAG, and retain active unrelated PR scopes without takeover. | 2026-07-22 |
| agentic-graph procedural image-to-3D quality contract 2026-07-22 | Record the bounded cross-repository contract for procedural, action-ready image reconstruction without importing an external generator. | Validate connected contour volumes, compact budgets, observed-front and inferred-hidden provenance, rigid pivots and sockets, one four-second +/-12-degree loop clip, separated quality gates, and GLB plus editable external-buffer glTF export; forbid external runtime, model, dependency, live-provider, browser, skinning, Prod, or Cloudflare claims. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs}`; `huijoohwee.github.io/schema/AgenticRAG` | Procedural Image-to-3D Quality Contract | `image.to-glb`, `buildImageToGlbScene`, `validateImageToGlbBundle` | One supported reference image, procedural source, compact reconstruction budgets, and Dev validation evidence | Connected contour model with provenance, rigid action hierarchy, bounded loop clip, GLB, editable external-buffer glTF, and separated gate results | agentic-graph owns reconstruction, validation, export, and runtime evidence; Agentic Canvas OS owns command, semantic, proof, and planning projections; AgenticRAG remains the downstream schema projection. Any missing gate, copied or external dependency, or evidence overclaim fails closed. | Merge the agentic-graph runtime through protected integration, then advance the Agentic docs pin and regenerate AgenticRAG; require separate live-provider, browser, skinning, Prod, and Cloudflare proof before promoting those claims. | 2026-07-22 |
| Autonomous implementation run management | Turn project work into isolated autonomous implementation runs that teams manage through durable state, evidence, and explicit controls. | Define canonical invocation, safe provisioning, stable JSON lifecycle, review-ready handoff, durable MCP supervision, bounded configured execution, controls, evidence, and a no-copy/no-dependency boundary; stop managed runs delivery-ready without automatic merge or deploy. | `agentic-canvas-os/{docs,scripts,__tests__,package.json,todo/2026-07.md}`; `agentic-graph/{mcp,contracts,canvas/src,docs,__tests__}` | Managed Implementation Run Runtime | `implementation.run`, `device:start`, `device:review`, `device:resume`, `agentic-graph.implementation_run.plan`, `agentic-graph.implementation_run.start`, `agentic-graph.implementation_run.list`, `agentic-graph.implementation_run.control` | Durable work item, repository, semantic scope, configured runner id, allowed paths, verification argv, bounds, idempotency key, and explicit control | Fenced task worktree, durable supervised attempts, bounded verification evidence, team-visible state, ACOS review-ready lease, and managed `delivery_ready` handoff | Agentic Canvas OS owns invocation and Git lifecycle; agentic-graph owns the durable local MCP supervisor and projection; exact fences and state versions serialize mutation; unknown tokens, arbitrary commands, unsafe paths, missing containment evidence, merge, deploy, and external dependency fail closed. | Integrate ACOS first, advance agentic-graph's pinned docs revision, run focused and full repository gates, then accept the combined Dev runtime only with exact catalog, MCP, lifecycle, recovery, and no-deploy proof. | 2026-07-22 |
| Source-bound application component-pack interoperability 2026-07-22 | Let teams extend the application catalog with independently owned components without coupling composition to a framework, package manager, or network registry. | Admit bounded exact-revision pure-JSON packs only at host initialization; bind canonical pack and composite catalog digests; inject adapters and owner resolvers privately; preserve three MCP tools and canonical invocation; reject discovery, downloads, mutable aliases, drift, and copied or external framework dependencies. | `agentic-canvas-os/{docs/APPLICATION-COMPOSITION.md,__tests__,memory,todo}`; `agentic-graph/{mcp,docs,scripts,__tests__}` | Application Component Pack Registry | `createApplicationComponentPackRegistry`, `digestApplicationComponentPackSource`, `/application.compose` | Exact inert pack sources, normalized component definitions, private adapters and owner resolvers, and caller runtime proof | Deterministic composite catalog and policy digests, bounded offline execution, typed startup rejection, or pre-invocation drift block | Component content digests remain independent of unrelated pack members; catalog proof separately binds selected packs; MCP cannot register code or grant authority; existing optional host integrations remain outside this clean-room subsystem boundary. | Integrate the ACOS contract first, update agentic-graph's exact docs pin, run focused, broad, security, stdio, and collaboration gates, then merge Dev while canceling and verifying zero-step production deployment. | 2026-07-22 |
| Agentic Canvas OS skill optimization training runtime readiness 2026-07-22 | Make skill-text improvement trainable through neural-network-like schedules without changing model weights or importing an external optimizer. | Define one canonical resumable Skill Evolution contract with source revision fences, epochs, batches, mini-batches, text learning-rate schedules, disjoint validation gates, strict budgets, and review-pending-only handoff; keep agentic-graph as the executable MCP owner. | `agentic-canvas-os/{docs/SKILL-EVOLUTION.md,src/agentic-graph-mcp-client.js,scripts,__tests__,todo,memory}`; `agentic-graph/{mcp,canvas/src,docs,__tests__}` | Skill Evolution Optimization Contract | `/skill.evolve`, `evolveSkill`, `agentic-graph.skill.evolve` | Exact source revision, baseline and adapter digests, disjoint dataset references, schedule, validation policy, bounds, idempotency, and revision fences | Resumable typed run state, aggregate evidence, bounded cost, or one review-pending candidate diff with all apply, model-weight, and deployment flags false | Agentic Canvas OS owns invocation and clean-room semantics; agentic-graph owns durable execution; validation never reaches the candidate adapter; any drift, overlap, failed gate, budget breach, stale revision, copied dependency, or automatic write fails closed. | Integrate the ACOS contract, advance agentic-graph's exact docs pin, prove the local MCP harness and client transport together, then retain operator review and `/skill.manage` as the separate persistence boundary. | 2026-07-22 |
| Home Apex Airvio identity and prompt preset production release 2026-07-22 | Promote the reviewed Home Apex identity, prompt preset layout, and deterministic runtime handoff through the protected release chain. | Preserve source-backed prompt ownership; publish Airvio agentic canvas OS browser and hero identity; integrate the exact app and docs pair; generate Prod canonically; deploy once; verify apex, subpath, assets, metadata, and runtime revisions. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas,docs,scripts,.github/workflows}`; `huijoohwee/content/agentic-graph` | Home Apex Airvio Release Ledger | `LiveCanvasHero`, `device:complete`, `Production Release` | Reviewed agentic-graph head, pinned Agentic Canvas OS revision, immutable manifest, explicit release authority, and audited worktree registry | Protected Dev merge, exact generated Prod mirror, one Cloudflare deployment, verified Airvio identity, prompt preset behavior, and public revision parity | Protected integration is the only release trigger; generated mirrors remain controller-owned; divergent canonical work, dirty Prod files, failed checks, competing deployments, or public drift block mutation and preserve prior production. | Merge this ledger first, rerun the agentic-graph Integration Gate on current main, complete the protected merge, then accept only the automatic release whose public evidence matches the promoted SHA. | 2026-07-22 |
| agentic-graph flight simulation and MMORPG workspace seed production release 2026-07-22 | Promote the preserved flight-simulation and MMORPG workspace seeds through the protected release chain without absorbing unrelated canonical or Prod work. | Preserve the exact local seed commit; reconcile it in a leased task worktree; validate document and projection contracts; require protected integration, canonical mirror generation, one controller-owned Cloudflare deployment, and matching public runtime evidence. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{docs/documents,docs/workspace-seeds,scripts,.github/workflows}`; `huijoohwee/content/agentic-graph` | Flight And MMORPG Seed Release Ledger | `device:review`, `device:publish`, `device:complete`, `Production Release` | Preserved agentic-graph commit, exact current main, pinned Agentic Canvas OS revision, immutable manifest, explicit release authority, and audited worktree registry | Protected Dev merge containing both seed families, exact generated Prod mirror, one Cloudflare deployment, and verified public revision parity | agentic-graph remains the authored seed owner; Agentic Canvas OS owns planning and release policy; generated Prod remains controller-owned. Unexplained dirt, overlapping scope, failed validation, revision drift, or public mismatch blocks downstream mutation. | Merge this ledger, import the preserved seed commit into the leased agentic-graph lane, pass protected integration, complete Dev, and accept only the automatic release whose public marker names the promoted SHA. | 2026-07-22 |

## 2026-07-23

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Agentic Canvas OS opt-in autonomous runtime readiness 2026-07-23 | Close the default composed-agent wiring gaps without enabling autonomous spend or mutation by default. | Require explicit runtime and spend gates, re-hash one server-side definition source, align provider and Responses configuration, inject the bounded adapter, expose an authenticated strict route, scope continuation per principal, and preserve all tool and mutation review owners. | `agentic-canvas-os/{agent-api/src,worker/index.js,__tests__,docs,README.md,package.json,todo/2026-07.md}` | Opt-In Autonomous Runtime | `resolveAutonomousRuntimeEnvironment`, `createAutonomousAgentDefinitionRegistry`, `createAgentRuntimeHandler`, `POST /api/agent/run` | Server-side enablement, spend approval, exact source and digest, aligned model and adapter, session token, run id, conversation id, and bounded input | Validated composed text and reported cost, typed pre-spend unconfigured refusal, strict request rejection, or bounded execution block | The shipped default remains off; the caller cannot select agents, models, tools, MCP routes, policy, or review evidence; Function Calling retains signed review and native receipts; missing gates, drift, paid proof, Prod, and Cloudflare fail closed. | Integrate through protected Dev, keep provider status unverified, and seek separate explicit approval before any new bounded paid proof or deployment. | 2026-07-23 |

## 2026-07-24

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Role-agent orchestration MCP and invocation runtime | Make source-backed role-playing agent teams canonically invocable while preserving every existing runtime owner. | Define one `/agent.team #role-based-agent-team @agent-team` contract; fence exact revisions and state; bound turns, depth, fanout, retries, time, tokens, and cost; preserve private delegation, explicit handoff ownership, durable review and cancellation; forbid CrewAI copying or dependency. | `agentic-canvas-os/{docs,scripts,__tests__,package.json,todo/2026-07.md}`; `agentic-graph/{mcp,contracts,__tests__}` | Role-Based Agent Team Runtime | `agentic-graph.agent_team.plan`, `agentic-graph.agent_team.start`, `agentic-graph.agent_team.list`, `agentic-graph.agent_team.control` | Exact team source, Agent Definition and workflow revisions, task, bounds, review policy, plan digest, idempotency key, and expected state version | Immutable plan, durable bounded run, sanitized list projection, serialized control transition, exact final-answer owner, or typed fail-closed result | Agentic Canvas OS owns invocation and semantics; agentic-graph owns durable MCP supervision; existing agent owners retain execution and authority; roles and personas grant nothing; Agent Swarm remains unchanged; drift, replay, stale state, missing review, budget breach, copied source, dependency, Prod, or Cloudflare fails closed. | Integrate the Agentic contract, advance agentic-graph's exact docs pin, then require focused model-free catalog proof and matching durable stdio MCP lifecycle proof before any runtime-ready promotion. | 2026-07-24 |
| repository-packing-ai-friendly-single-file-runtime | Pack one complete local codebase into a deterministic AI-friendly artifact without external runtime reliance. | Define `/repository.pack #repository-packing @repository-root @runtime-proof`; implement one local stdio MCP tool with Git discovery, deterministic Markdown, typed omissions, containment, hard bounds, secret blocking, atomic content-addressed output, focused proof, and zero model, network, cost, Prod, or Cloudflare activity. | `agentic-canvas-os/{docs,scripts,__tests__,package.json,todo/2026-07.md}`; `agentic-graph/{mcp,canvas/src,scripts,docs}` | Repository Packing Runtime | `/repository.pack`, `repository.pack`, `agentic-graph.repository.pack`, `runRepositoryPackTool` | Exact local Git worktree root, repository-relative output directory, optional path policies, and bounded file and byte ceilings | Verified content-addressed Markdown artifact metadata, typed omissions and failures, stable digests, and zero-cost evidence | Agentic Canvas OS owns invocation and clean-room truth; agentic-graph owns local execution; source remains unchanged; traversal, symlink escape, drift, secrets, overflow, copied artifacts, external dependency, remote fallback, or deploy fails before publication. | Integrate the invocation contract, pin its exact revision in agentic-graph, prove tool discovery and real stdio packing, then accept combined Dev readiness only after protected integration and collaboration parity. | 2026-07-24 |
| AI voice studio MCP invocation and clean-room runtime | Make authorized voice cloning, dictation, and disclosed speech creation canonically invocable without importing an external studio. | Define one `/voice.studio` command with clone, dictate, and create semantic routes; require speaker consent, recording rights, revocation, disclosure, provenance, approval, cost, bounds, one MCP tool, deterministic proof, and no copied Voicebox artifact or dependency. | `agentic-canvas-os/{docs,scripts,__tests__,package.json,todo/2026-07.md}`; `agentic-graph/{mcp,contracts,canvas/src,__tests__}` | AI Voice Studio Contract | `validateVoiceStudioContractDocuments`, `agentic-graph.voice.studio` | Exact operation, immutable audio or text artifact reference, active voice-profile revision, authorization receipts, disclosure, bounds, idempotency key, and runtime proof | Canonical metadata route, typed profile, transcript, or audio result, complete provenance and cost evidence, or a pre-execution block | Agentic Canvas OS owns invocation and safety semantics; agentic-graph owns MCP execution and media persistence; consent never follows from approval or a binding; unsupported rights, revoked profiles, copied artifacts, dependency, spend gap, Prod, or Cloudflare fail closed. | Integrate the Agentic contract, advance agentic-graph's exact docs pin, and require deterministic local stdio clone, dictate, create, replay, cancellation, revocation, read-back, and no-spend failure proof before runtime-ready promotion. | 2026-07-24 |

## 2026-07-25

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Expired pending resume recovery | Restore an interrupted same-session parked stash without weakening branch fencing. | Renew only exact active local and remote lease markers plus matching pending stash evidence; restore and record it before any new park cycle. | `agentic-canvas-os/{scripts/device-branch-lib.mjs,__tests__/device-park-stash.test.mjs,memory/2026-07.md,todo/2026-07.md}` | Writer Lease Resume Recovery | `reconcileResumeReplay`, `completeParkedStashRestore` | Exact session, task worktree, branch, fence, draft pull request, and immutable stash metadata | Renewed fenced lease and restored stash, or a typed refusal | The pending-stash exception is narrower than ordinary expired recovery and requires exact remote parity before heartbeat or restoration. | Integrate through protected delivery, then use the repaired canonical controller to resume the blocked agentic-graph memory-runtime lane. | 2026-07-25 |
| agentic-graph responsibility flow runtime-ready release | Promote the registered-settings responsibility index through the protected release chain with exact source, docs, mirror, and public evidence. | Record registry-only scope; require deterministic bounded Markdown shards, retryable Settings projection loading, protected Dev integration, canonical Prod generation, one controller-owned deployment, and exact public revision parity. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,scripts,.github/workflows}`; `huijoohwee/content/agentic-graph` | Responsibility Flow Release Ledger | `device:integrate`, `responsibility-flow:check`, `Production Release` | Reviewed agentic-graph head, exact Agentic Canvas OS revision, immutable manifest, registry-derived projections, and explicit release authority | Protected Dev merge, bounded ownership artifacts, resilient Settings projection loading, exact generated Prod mirror, one Cloudflare deployment, and matching public runtime evidence | agentic-graph owns registered-setting provenance and runtime consumption; Agentic Canvas OS owns planning and release policy; generated Prod remains controller-owned. Missing planning evidence, stale projections, failed checks, revision drift, deployment conflict, or public mismatch blocks downstream mutation. | Merge this ledger, advance the agentic-graph docs pin, rerun its protected Integration Gate, integrate Dev, and accept only the automatic release whose public evidence matches the promoted SHA. | 2026-07-25 |

## 2026-07-26

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Interrupted integration recovery | Unblock an expired same-session integration that committed locally before protected publication. | Verify the recorded integration tree, exact remote fence ancestry, clean registered worktree, and no more than one protected-main merge; issue one new fenced claim and preserve commit evidence. | `agentic-canvas-os/{scripts/device-branch-lib.mjs,__tests__/device-resume.test.mjs,memory/2026-07.md,todo/2026-07.md}` | Writer Lease Integration Recovery | `resume`, `resolveExpiredIntegrationContinuation` | Exact same-session expired lease, matching task branch and worktree, recorded commit/tree, remote fence, and optional one protected-main merge | Renewed fenced lease with preserved integration evidence, or typed refusal before publication | The exception accepts no arbitrary local edits, no extra authored commits, no ambiguous branch, no mismatched tree, and no remote advance beyond the exact fence. | Merge the controller repair through protected delivery, then replay the blocked agentic-graph `device:resume` and `device:integrate` path without a direct push. | 2026-07-26 |
| Role-agent runtime-ready Dev promotion | Promote the exact team contract after matching local MCP execution proof. | Catalog the collaboration manager, evidence scout, and risk reviewer; record host-owned verification, control, review, local-model, and effect-replay evidence; retain clean-room and deployment gates. | `agentic-canvas-os/{docs,scripts,memory/2026-07.md,todo/2026-07.md}`; `agentic-graph/{contracts,data/config/agents,mcp,docs}` | Role-Based Agent Team Runtime | `agent.team`, `agentic-graph.agent_team.plan`, `agentic-graph.agent_team.start`, `agentic-graph.agent_team.list`, `agentic-graph.agent_team.control` | Exact invocation tuple, built-in team source, owner revisions, explicit local model, durable receipts, and focused proof | Runtime-ready local Dev contract with manager-owned synthesis, private delegation, zero-cost test settlement, and fail-closed replay | Roles remain metadata; all owners stay host-configured; real provider availability, remote Worker parity, Prod, Cloudflare, and copied external artifacts remain unclaimed. | Integrate this contract, advance agentic-graph's immutable docs pin, rerun its complete runtime gate, and integrate the matching source lane. | 2026-07-26 |
| Invocation catalog digest parity | Keep `/`, `#`, and `@` source metadata identical across Agentic Canvas OS, MCP, FloatingPanel Skills & Commands, and runtime identity. | Produce one deterministic full-catalog SHA-256 digest; return it with exact counts on every sigil query; replace stale slices; recompute in-browser; prefer source metadata on token collisions; attest digest parity across peers. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `agentic-graph/{mcp,canvas/src}` | Invocation Catalog Runtime Identity | `agentic-graph.agentic_canvas_os.docs.invoke`, `refreshAgenticOsRemoteGrammarCatalog`, `buildAgenticGraphRuntimeIdentity` | Exact docs revision and source-backed command, semantic, and binding dictionaries | Digest-verified fresh catalog, source-backed FloatingPanel rows, and peer-comparable runtime identity or visible stale/blocked state | Revision, digest, counts, provenance, and slice membership must agree; removed tokens are replaced, not retained; local behavior may enrich but never shadow source metadata; drift fails closed. | Integrate the docs contract first, advance agentic-graph's exact revision pin, rerun MCP, browser catalog, runtime identity, and collaboration gates, and keep deployment outside this Dev-only task. | 2026-07-26 |

## 2026-07-27

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Terminal-turn auto-delivery recommendation | Make automatic protected delivery a terminal implementation-completion policy rather than a generic conversational-turn side effect. | Require immutable opt-in, genuinely complete work, no remaining required action, clean exact reviewed head, protected checks, canonical convergence, and runtime proof; exclude ordinary messages, chats, sessions, threads, questions, status, read-only, waiting, partial, dirty, parked, and blocked turns. | `agentic-canvas-os/{README.md,docs,scripts,__tests__,todo/2026-07.md}` | Terminal Auto-Delivery Contract | `device:start --auto-delivery`, `device:review`, `device:integrate` | Pre-authorized implementation lease, terminal completion classification, exact review head, protected checks, and canonical runtime policy | Exact-head protected auto-merge followed by canonical runtime readiness, or a typed non-delivery outcome | Conversation ending is never authority; only the terminal reviewed implementation lane may wake protected auto-merge, and any incomplete, dirty, blocked, changed, forked, conflicted, or unproven state fails closed without deploy. | Review the existing auto-delivery lane, run focused and full checks, then integrate only through protected delivery while retaining ordinary conversational turns as non-mutating. | 2026-07-27 |

## 2026-07-28

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Agentic Canvas OS GitHub Actions and Cloudflare Workers free-tier economics | Reduce routine automation and Worker quota consumption without weakening required checks, authorization, persistence correctness, observability, or release ownership. | Filter no-op automation, remove dependency installs and caches from zero-dependency CI, use Node 22 slim runners, split dependency security, preflight and rate-limit room access, defer empty-room persistence, bound retention and CPU, cache alarm state and reconnect tokens, sample logs, disable preview surfaces, and prove the source contract without deployment. | `agentic-canvas-os/{.github,worker,wrangler.jsonc,__tests__,README.md,docs/RUNTIME-PROOF.md,todo/2026-07.md}` | Free-Tier Economics Contract | `dispatchCloudflareRequest`, `reconcileState`, `scheduleExpiry`, GitHub workflow triggers | Current workflow events, package lock, canvas requests, Durable Object state, alarm state, and Wrangler configuration | Required protected checks with less repeated setup; bounded admission before Durable Objects; empty-room zero-write behavior; change-only persistence; sampled telemetry; deployment-free bundle evidence | Preserve exact required check names and merge-group coverage; favor cached reads over writes; retain the earliest valid alarm; keep production deployment under the protected controller; never infer zero cost from source configuration. | Review measured GitHub and Cloudflare usage periodically, add authenticated creation or edge admission before public production scale, adjust filters or sampling only with provider evidence, and integrate through protected Dev before any separately authorized deployment. | 2026-07-28 |
| Canonical worktree lifecycle resolution | Make protected integration, runtime reconciliation, and task provisioning work when canonical main lives under the shared worktree root. | Select the single registered main worktree, derive task roots from Git ownership, clear hook-local Git environment, preserve workspace runtime-state ownership, and fail closed on ambiguity. | `agentic-canvas-os/{scripts,docs/CANONICAL-LIFECYCLE.md,__tests__,todo/2026-07.md}` | Canonical Worktree Resolver | `resolveCanonicalMainWorktree`, `deriveTaskWorktreeRoot`, `clearLocalGitEnvironment` | Registered worktree records, Git common directory, canonical source paths, and hook environment | Exact canonical roots and safe task root, or a typed refusal before mutation | Branch names, filesystem adjacency, and inherited hook Git variables cannot establish canonical ownership; exactly one registered main worktree is required. | Integrate through protected delivery, replay the fenced agentic-graph completion, remove only cleanup-ready worktrees, and verify exact production parity. | 2026-07-28 |
| guideline-runtime-alignment-audit-runtime-ready | Implement a deterministic non-mutating audit from guideline rules to runtime evidence. | Build content-derived models, evidence-bound readiness, bounded checkers, one output sink, deterministic reports, exact property proof, and protected local integration; keep source, production, edge, and deployment surfaces unchanged. | `agentic-canvas-os/{scripts/alignment-audit,__tests__,package.json,package-lock.json,todo/2026-07.md}` | Alignment Auditor Runtime | `runAudit`, `alignment-audit:check` | Configured guideline and runtime roots, exact revisions, and a null deploy instruction | Versioned report and digests, typed findings, a closed deploy boundary, and verification proof | Use content-only identifiers, explicit evidence closure, strict output containment, and deterministic reduction; fail closed on configuration or integrity errors. | Integrate through the protected pull request, then rerun canonical runtime and collaboration proof; do not deploy to production or Cloudflare. | 2026-07-28 |
| MainPanel Settings Z.AI Quick Start reference rows preserving shared KTV layout | Add a source-linked Z.AI Quick Start reference to MainPanel Settings while retaining the established Key-Type-Value presentation. | Publish static Z.AI API reference rows and the official Quick Start action through the shared Settings KTV owner; keep credentials, proxy allowlists, and provider execution out of scope. | `agentic-graph/canvas/src/features/panels/views/{zAiApiDocs.ts,useSettingsView.ts,settingsView.constants.ts}`; `agentic-canvas-os/todo/2026-07.md` | MainPanel Z.AI Settings Reference | `buildZaiApiDocEntries`, `useSettingsView` | Official Z.AI Quick Start endpoint and existing shared Settings row contracts | Static searchable KTV rows, exact documentation action, and an explicit reference-only runtime boundary | Settings documentation must reuse the shared KTV row path; a provider runtime requires separate authorization and source-backed endpoint, credential, and proxy changes. | Review the isolated agentic-graph Settings pull request; scope a separate provider-integration task only if executable Z.AI chat support is requested. | 2026-07-28 |
| Flight Sim Geo+XR geospatial production release 2026-07-28 | Promote the exact native MapLibre aircraft overlay through protected Dev, generated Prod, and Cloudflare without absorbing unrelated worktrees. | Bind the release to the reviewed Flight head, exact docs and catalog revisions, immutable manifest, canonical mirror generation, single controller-owned Pages deployment, D1 reconciliation, and public route proof; preserve all unrelated dirty, active, review-ready, and parked lanes. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,gympgrph/src,docs,.github/workflows}`; `huijoohwee/content/agentic-graph` | Flight Sim Geo+XR Release Ledger | `device:integrate`, `release:manifest:create`, `Production Release` | Reviewed Flight head `85974da1`, exact Agentic Canvas OS revision, clean generated mirror artifact, and explicit production authority | Protected Dev merge, exact generated Prod mirror, one Cloudflare deployment, D1 reconciliation, and matching public revision evidence | agentic-graph owns the Flight and MapLibre runtime; Agentic Canvas OS owns release planning; generated Prod stays controller-owned; any ownership, revision, test, deployment, or public-evidence mismatch stops downstream mutation. | Integrate this ledger, advance the Flight branch docs pin to the merged revision, rerun its exact-head gate, integrate Dev, and accept only the automatic release whose public evidence names the promoted SHA. | 2026-07-28 |
| Agentic SDLC guideline runtime production release 2026-07-28 | Publish the pinned guideline sources and promote their deterministic conformance runtime through protected Dev, generated Prod, and Cloudflare. | Preserve source commit `e313da3`; integrate ACOS PR `#153`; advance agentic-graph's exact docs pin; require immutable manifest, canonical mirror generation, one controller-owned Pages deployment, D1 reconciliation, and matching public route evidence. | `huijoohwee.github.io/guidelines`; `agentic-canvas-os/{scripts/agentic-sdlc,docs,__tests__,todo/2026-07.md}`; `agentic-graph/{docs,.github/workflows}`; `huijoohwee/content/agentic-graph` | Agentic SDLC Release Ledger | `device:review`, `device:integrate`, `runtime:docs-dependency:promote`, `Production Release` | Published guideline revision, reviewed ACOS head, exact agentic-graph docs pin, immutable manifest, generated mirror artifact, and explicit production authority | Protected ACOS merge, protected agentic-graph promotion, exact Prod mirror, one Cloudflare Pages deployment, D1 reconciliation, and verified root plus `/agentic-graph` evidence | Guideline source owns authored rules; Agentic Canvas OS owns conformance and release planning; agentic-graph owns production; generated Prod stays controller-owned; any provenance, ownership, revision, validation, deployment, or public-evidence mismatch stops downstream mutation. | Complete the fenced ACOS integration, promote its exact main SHA through a protected agentic-graph pull request, and accept only the automatic release whose public marker names the promoted agentic-graph and Agentic Canvas OS revisions. | 2026-07-28 |

## 2026-07-29

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| agentic-graph clean-room immersive media runtime readiness | Make immersive media behavior available across the existing FloatingPanel scene family without adding another renderer or authority owner. | Define `/media.immersive @canvas #canvas-media`, close Flight Sim and Motion Control catalog drift, project bounded source, view, navigation, transition, annotation, layer, overlay, and capture controls, and forbid copied artifacts or external runtime dependencies. | `agentic-canvas-os/{docs,todo/2026-07.md}`; `agentic-graph/{canvas/src,docs,__tests__}` | Immersive Media Runtime | `agentic-graph.inspect_local_immersive_media`, `agentic-graph.control_local_immersive_media`, `/flight.sim`, `/motion.control` | Optional approved media source, bounded view and interaction settings, typed annotations and layers, exact Canvas binding, and local runtime state | One zero-config local scene, shared-Camera interaction, deterministic applied or blocked results, capture proof, and panel projections backed by one runtime | Agentic Canvas OS owns invocation truth and planning; agentic-graph owns media, Canvas, Camera, input, persistence, MCP execution, and runtime proof. Any copied artifact, external dependency, renderer fork, panel-local authority, source drift, Prod, or Cloudflare mutation fails closed. | Integrate this catalog through protected Dev, advance agentic-graph's exact docs pin, then require focused source tests and neutral browser proof from the protected application SHA. | 2026-07-29 |
| Agentic SDLC end-to-end observability catalog 2026-07-29 | Make a complete agentic execution run inspectable on the existing Canvas without another store, dashboard, renderer, model, network, or paid dependency. | Define one read-only observation composition and require immutable receipts, stable projection order, existing KGC, GraphData, and Canvas owners, typed state claims, zero economics, and a closed Dev boundary. | `agentic-canvas-os/{docs,__tests__,todo/2026-07.md}`; `agentic-graph/{mcp,contracts,canvas/src}` | Agentic SDLC Observability Contract | `/sdlc.observe`, `agentic-graph.agentic_sdlc.observe` | Exact invocation, run id, bounded view, expected revision, expected ledger digest, optional cursor and limit, and immutable `agentic-sdlc-ledger-receipt/v1` | `agentic-graph-agentic-sdlc-observation/v1` with source, typed status and conformance, deterministic GraphData and KGC Markdown, cache identity, and zero economics | Source receipt and bytes must agree before projection; `verified`, `delivery_ready`, and `deployed` remain separate evidence-backed claims; observation never mutates or promotes state. | Run the independent ACOS contract test and docs check, then require matching protected agentic-graph tests and exact revision integration before claiming live Canvas or guideline-runtime parity. | 2026-07-29 |
| agentic-graph agentic payments runtime production release 2026-07-29 | Promote the fail-closed Singapore agentic payments lifecycle through protected Dev, generated Prod, and Cloudflare with exact source and revision evidence. | Bind the release to reconciled payments source, deterministic local VCC, exact protected app and docs revisions, immutable manifest, generated mirror, one controller-owned Pages deployment, D1 migrations, and public route evidence; preserve unrelated dirty lanes and keep unresolved provider operations fail-closed. | `agentic-canvas-os/{docs/RELEASE-WORKFLOW.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,cloudflare,mcp,grph-shared,scripts,docs}`; `huijoohwee/content/agentic-graph` | Agentic Payments Release Ledger | `device:integrate`, `payment:runtime:local-vcc`, `Production Release` | Reconciled Stripe, StraitsX, XSGD, Avalanche, paywall, MCP, Worker, migration, and documentation changes; exact app and docs revisions; explicit production authority | Protected Dev merge, deterministic local contracts, generated Prod parity, one Cloudflare deployment, D1 reconciliation, and matching public revision evidence with provider execution still blocked until configured | agentic-graph owns payment execution and fail-closed readiness; Agentic Canvas OS owns release planning; generated Prod stays controller-owned; any ownership, revision, test, secret, provider, deployment, or public-evidence mismatch stops downstream mutation. | Integrate this ledger, transfer the preserved payments candidate into a fenced agentic-graph lane, reconcile the canonical PRD, run exact gates, merge protected Dev, and accept only the authorized controller release whose public evidence names the promoted SHA. | 2026-07-29 |

## 2026-07-30

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| agentic-graph PRD TAD ADR documentation alignment and legacy cleanup 2026-07-30 | Align agentic-graph product and architecture documentation with the canonical PRD/TAD/ADR guideline while removing confirmed stale, duplicate, and conflicting sources. | Audit claims against the protected repository, update one canonical PRD, TAD, ADR, and design index, remove obsolete planning and architecture duplicates, and validate frontmatter, links, documentation checks, and Dev-only boundaries. | `agentic-graph/{README.md,docs}`; `agentic-canvas-os/todo/2026-07.md` | Documentation Contract Alignment | `agentic-graph PR #532`, documentation validation | agentic-graph PR #532, guideline version 1.6.0, actual protected repository implementation, and confirmed legacy documentation corpus | In-progress protected agentic-graph documentation candidate with one planning authority, source-backed PRD/TAD/ADR contracts, removed duplicates, and validation evidence | Actual implementation is authoritative; canonical owners replace duplicates; evidence bounds readiness; source-only planning precedes its agentic-graph consumer; Production and Cloudflare remain closed. | Integrate this planning row first; then refresh PR #532 against the new frontier, complete documentation checks, and integrate only with clean source-backed proof. | 2026-07-30 |
| Repository runtime readiness source audit for Builders Hub 2026-07-30 | Add a bounded clean-room repository audit that turns exact source evidence into honest layer-specific readiness for a solo AI-native operator. | Extend the existing runtime-ready invocation; inspect exact Git metadata under caps; emit typed source findings, zero-cost evidence, and closed external boundaries; keep target, local runtime, browser, integration, Prod, and Cloudflare mutation outside the task. | `agentic-canvas-os/{scripts/repository-runtime-readiness.mjs,__tests__,docs,package.json,todo/2026-07.md}`; `huijoohwee.github.io/guidelines` | Repository Runtime Readiness Evaluator | `auditRepositoryRuntimeReadiness`, `/runtime-ready.check` | Exact local Git worktree, expected revision, requested source layer, policy limits, and clean-room reference metadata | Typed source evidence, stable findings, layer verdicts, zero-cost ledger, and minimum-value recommendations | Exact revision and bounded complete evidence are mandatory; missing identities, drift, mutable inputs, or unjoined proof block only the affected layer; external mutation and deployment remain closed. | Integrate the universal guideline first, replace its candidate pin with protected source, rerun Agentic Canvas OS checks, then advance agentic-graph's docs pin in a separately leased downstream scope. | 2026-07-30 |
| Native deterministic parser generator and explained knowledge graph 2026-07-30 | Turn one explicit local codebase plus docs, SQL, configs, and PDFs into a queryable source-auditable graph across MCP, `/`, `#`, and `@`. | Compile bounded workspaces with inert data grammars and native source adapters; publish canonical digest-fenced snapshots; expose exactly three local MCP tools; keep dictionary resolution metadata-only; require explanation and evidence for every edge; forbid model, network, embedding, vector, external runtime, source mutation, Prod, and Cloudflare paths. | `agentic-canvas-os/{src/knowledge-graph,scripts/knowledge-graph-mcp.mjs,__tests__,docs,memory/2026-07.md,todo/2026-07.md}` | Native Knowledge Graph Runtime | `generateParser`, `ingest`, `query`, `explain`, `agentic_canvas_os.knowledge_graph.*` | Explicit symlink-safe workspace, graph id, optional bounded grammar data or artifact digests, expected snapshot digest, bounded query, or exact edge id | Immutable parser artifact, content-addressed graph snapshot, deterministic subgraph, stored edge evidence, or typed fail-closed diagnostic | Stable identities derive from canonical local bytes and parser rules; every edge must pass one evidence factory; stale digests and unsafe or unsupported inputs fail closed; metadata lookup never implies execution or approval. | Integrate through the protected pull request, rerun exact-head collaboration and canonical runtime proof, and keep production publication outside this local Dev feature. | 2026-07-30 |
| Deterministic knowledge graph executable-owner correction 2026-07-30 | Preserve one canonical runtime while retaining Agentic Canvas OS invocation vocabulary. | Supersede the unintegrated native-runtime candidate: retire the duplicate parser, store, query engine, MCP server, and runtime tests from Agentic Canvas OS; keep `/knowledge.graph.*`, `#knowledge-graph`, `@knowledge-graph`, and a typed client for the three agentic-graph-owned MCP tools. | `agentic-canvas-os/{src/agentic-graph-mcp-client.js,__tests__/knowledge-graph-{client,contract}.test.mjs,docs,memory/2026-07.md,todo/2026-07.md}`; `agentic-graph/{mcp,canvas/src,docs}` | Knowledge Graph Invocation Contract | `ingestKnowledgeGraph`, `queryKnowledgeGraph`, `explainKnowledgeGraphEdge`, `agentic-graph.knowledge_graph.*` | Explicit source selection, opaque graph id, exact expected snapshot digest, bounded query or edge id | Typed forwarding and bounded read-only projection identity, with executable parsing and persistence remaining in agentic-graph | Agentic Canvas OS validates stable request and result identity but never exposes a store path or creates another executable graph owner; exact protected agentic-graph proof is required before combined runtime readiness. | Integrate the source contract first, advance the exact agentic-graph dependency pin, run protected parity and workspace-scale proof, and keep Prod and Cloudflare outside this Dev task. | 2026-07-30 |
| Knowledge graph Launch Skills and Commands reconciliation 2026-07-30 | Preserve GitHub repository graph ingestion while moving its visible invocation authority to the shared catalog. | Open FloatingPanel Skills and Commands from Launch Import URL; resolve the source-backed slash, semantic, and binding tuple through the docs MCP; pass the verified audit packet to the local agentic-graph ingestion owner. | `agentic-canvas-os/{docs,memory/2026-07.md,todo/2026-07.md}`; `agentic-graph/{canvas/src,mcp,docs}` | Knowledge Graph Launch Invocation | `agentic-graph.agentic_canvas_os.docs.invoke`, `agentic-graph.knowledge_graph.ingest`, `runLaunchImportUrl` | Canonical repository URL, fresh source-backed invocation catalog, exact tuple, and bounded local MCP host | Observable Skills and Commands activation followed by immutable repository acquisition and read-only graph projection, or a typed failure before ingestion | Launch may know the command capability but cannot own a duplicate catalog or semantic and binding list; URL acquisition may use network only before local deterministic parsing. | Validate menu-to-panel activation, tuple resolution, repository ingestion, workspace-scale queries, stored edge explanation, and isolated browser behavior before protected integration. | 2026-07-30 |
| Builders Hub protected guideline revision synchronization 2026-07-30 | Bind the repository readiness evaluator to the protected guideline source before release. | Replace candidate-only provenance with the exact protected guideline revision, validate the squash result and source contract, rerun focused checks, integrate Agentic Canvas OS through protected delivery, then reseal agentic-graph runtime review before any production authorization. | `agentic-canvas-os/{docs/REPOSITORY-RUNTIME-READINESS.md,__tests__/repository-runtime-readiness.test.mjs,todo/2026-07.md}`; `huijoohwee.github.io/guidelines` | Repository Runtime Readiness Provenance | `/runtime-ready.check`, `repository-runtime-readiness:check` | Guideline pull request `#68`, protected guideline revision `5b79529a5c791cdfceed70548543f82358fa100c`, and Agentic Canvas OS base `e94363dc1041de6aa8d023daf88ed06f58486ab2` | Verified protected guideline binding, regression coverage, and append-only release planning evidence | The protected squash revision is authority; a pre-merge candidate, branch name, or equivalent-looking content cannot replace exact protected provenance. | Integrate Agentic Canvas OS, advance agentic-graph's exact docs pin in a separate protected scope, rerun collaboration and runtime review, and authorize only the resulting immutable production candidate. | 2026-07-30 |

## 2026-07-31

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| universal-parser-generator-v2-catalog-contract | Make the generic built-in local parser registry usable through the shared knowledge-graph catalog without duplicating its descriptors. | Accept exactly one generic built-in profile or bounded custom descriptors; validate identity and matcher safety before transport; retain invocation proofs; forbid executable, network, model, embedding, vector, source-write, deployment, and provider behavior. | `agentic-canvas-os/{src/agentic-graph-mcp-contract.js,src/agentic-graph-mcp-client.js,docs/KNOWLEDGE-GRAPH.md,__tests__,memory/2026-07.md,todo/2026-07.md}`; `agentic-graph/{mcp,docs,README.md,__tests__}` | Knowledge Graph Parser Profile Contract | `validateKnowledgeGraphRequest`, `generateKnowledgeGraphParser` | One `default-source` profile or one non-empty inert descriptor array, plus an optional source-resolved invocation proof | Digest-fenced parser registry or typed pre-transport rejection without parser execution, source acquisition, or artifact writes | Exactly-one selection keeps the built-in source registry neutral and portable while custom parsers remain explicit; unknown or combined choices fail closed before MCP dispatch. | Run both repository parser-contract tests and local stdio proof; keep cross-repository runtime readiness, protected integration, and production authorization separately evidence-gated. | 2026-07-31 |
