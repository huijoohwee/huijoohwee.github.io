---
title: "Agentic Canvas OS Todo 2026-03"
doc_type: "Planning Ledger Shard"
schema: "todo-log/v1"
period: "2026-03"
scope: "cross-repository"
status: "append-only"
append_policy: "append-only"
date_heading_format: "YYYY-MM-DD"
source_contract: "../docs/TODO.md"
adoption_date: "2026-07-14"
historical_source: "$GITHUB_ROOT/agentic-graph/docs/reports/todo-log"
historical_policy: "preserve-row-bytes"
---

# Todo 2026-03

This shard is governed by `../docs/TODO.md`. Pre-adoption rows retain their imported source bytes; new rows append at EOF.

## 2026-03-01

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Webpage→Design wireframe pipeline | Keep cross-repo docs+schema aligned with URL-agnostic webpage DOM export, layout snapshot, and Design wireframe rendering | Update docs/schema: document hidden-iframe DOM export (networkIdle+domQuiet, sandboxed proxy, viewport-aware), layout snapshot caching, neutral layout-to-graph pruning/synthesis, DesignCanvas webpage wireframe rendering (no legacy grid/empty fallback), and fidelity-based settings; forbid URL-specific rules and undocumented recompute/loop paths. | `agentic-graph/docs/documents/{agentic-graph-webpage-proxy-and-srcdoc-iframe-document.md,agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-wireframe-enhanced-document.md}`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld,pipeline.jsonld}` | Cross-repo documentation + AgenticRAG canvas/pipeline contract | Update Markdown + JSON-LD contract text | Webpage URL + DOM snapshot + layout graph + Design wireframe settings | Updated docs + schema text | Treat DOM export/layout/Design wireframe as URL-agnostic SSOT; keep caching and viewport keys neutral; forbid host-specific CSS/DOM branches and infinite recompute or duplicate layout/render loops. | Run `npm -C agentic-graph/canvas run doc:lint`, `npm -C agentic-graph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-01 |

## 2026-03-10

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Rich Media | Keep Rich Media docs aligned with 2D/3D overlays and viewer behavior | Update cross-repo docs+schema to describe shared Rich Media behavior (2D/3D overlays, auto Script/Imgs/Fid heuristics, export HTML Canvas hi-DPI snapshots), and forbid duplicate preview surfaces, per-mode-only heuristics, or docs claiming non-existent recomputation/loop behavior. | `agentic-graph/docs/documents/{agentic-graph-canvas-document.md,agentic-graph-webpage-proxy-and-srcdoc-iframe-document.md,agentic-graph-markdown-editor-mode-contract-document.md}`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/README.md` | Cross-repo documentation + AgenticRAG schema | Update Markdown + README text | Implemented Rich Media overlay scheduling, flicker fixes, and hi-DPI HTML export | Updated docs + schema text | Document only code-backed behavior; treat Script/Imgs/Fid defaults as auto; keep Rich Media pools bounded and overlays single-surface; forbid duplicate previews and background recompute loops. | Run `npm -C agentic-graph/canvas run doc:lint`, `npm -C agentic-graph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-10 |

## 2026-03-14

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 2D+3D renderer matrix / Rich Media / Frontmatter | Align renderer+export docs with implemented 2D/3D, Rich Media, and Frontmatter behavior | Update cross-repo docs+schema to describe 2D D3/Flow vs 3D renderer matrix, Rich Media On/Off overlay+export behavior, and Frontmatter Mode On/Off derivation (Flow frontmatter graph vs Markdown JSON-LD), ensuring all toggles are view-only and share SSOT GraphData/fit/zoom semantics across workspace and HTML Canvas exports. | `agentic-graph/docs/documents/agentic-graph-renderer-document.md`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld` | Cross-repo documentation + AgenticRAG canvas contract | Update Markdown + JSON-LD contract text | Implemented 2D/3D renderer parity, Rich Media overlays/export, and frontmatter mode toggles | Updated docs + schemaVersion bump + renderer matrix section | Document only code-backed behavior; treat renderer/Rich Media/Frontmatter toggles as view-only; reuse SSOT visibility/fit semantics; forbid per-surface derivations or export-only behavior. | Run `npm -C agentic-graph/canvas run doc:lint`, `npm -C agentic-graph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-14 |
| Docs / Markdown / Frontmatter+Mermaid+Rich Media linking | Keep YAML/Mermaid frontmatter↔Markdown body↔Rich Media linking contract SSOT | Update cross-repo docs+schema to describe how YAML/Mermaid frontmatter graphs link to Markdown body anchors and Rich Media via `[[wikilinks]]`, `{{templates}}`, `frontmatterMermaidAnchors`, and node anchor properties; forbid absolute in-repo paths and duplicate, export-only graph derivations. | `agentic-graph/docs/documents/{agentic-graph-markdown-ssot-ui-contract-document.md,agentic-graph-renderer-document.md}`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{markdown.jsonld,canvas.jsonld}` | Cross-repo documentation + AgenticRAG markdown/canvas contract | Update Markdown + JSON-LD text | Implemented frontmatter Flow graph + article anchors + Rich Media overlays | Updated docs + schemaVersion bumps | Document only code-backed linking patterns; keep anchors/links schema-neutral; forbid hardcoded repo paths and per-surface link semantics. | Run `npm -C agentic-graph/canvas run doc:lint`, `npm -C agentic-graph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-14 |
| Docs / Canvas / HTML Canvas export viewer HUD | Keep export viewer toggles and centering aligned with renderer SSOT | Document HTML Canvas export viewer HUD (3D/Rich/Media/Frontmatter), centroid-based fit, and mobile pan/zoom parity with Canvas; toggles remain view-only and must not re-derive GraphData or layout, only gate renderer, overlays, and media interaction. | `agentic-graph/docs/documents/{agentic-graph-renderer-document.md,agentic-graph-canvas-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld`, `agentic-graph/todo-log.md` | Cross-repo documentation + AgenticRAG canvas export contract | Update Markdown + JSON-LD contract text | Implemented HTML Canvas export centering + viewer HUD toggles | Updated docs + schemaVersion bump + HUD/mode parity text | Document only code-backed behavior; treat HUD toggles as view-only; reuse SSOT GraphData/fit semantics; forbid export-only graphs or per-mode derivations. | Run `npm -C agentic-graph/canvas run doc:lint`, `npm -C agentic-graph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-14 |

## 2026-03-23

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Export / HTML Canvas / Standalone fidelity | Make exported `.html` visually match in-app Canvas without server routes | Rewrite SVG and overlay asset URLs for standalone HTML export: unwrap local proxy endpoints (`/__fetch_remote`, `/__webpage_proxy`, `/__webpage_asset_path`) to absolute URLs; inline `/__repo_file` assets to `data:*;base64` up to a bounded per-asset max; add missing CSS vars (media header bg); forbid exported HTML containing stale proxy-only paths. | Historical path snapshot: `canvas/src/components/BottomPanel/markdownWorkspace/main/exports/exportHtmlCanvas.ts`, `canvas/src/lib/graph/htmlViewer/{buildGraphHtmlViewerMarkup.ts,rewriteSvgMarkupForStandaloneHtmlExport.ts,standaloneAssetRewrite.ts}` | HTML Canvas export pipeline + standalone viewer | `exportHtmlCanvasFromWorkspace`, `rewriteSvgMarkupForStandaloneHtmlExport`, `buildGraphHtmlViewerMarkup` | GraphData + Schema + SVG snapshot | Standalone `.canvas-2d.html/.canvas-3d.html` with correct media and styles | For each SVG `href/src`: unwrap proxies; inline repo files if fetchable and within size cap; otherwise keep absolute URL; avoid per-frame work by doing all rewriting at export-time. | Smoke: export a doc that contains repo-file images + remote images + rich media panels; open exported HTML with no dev server and confirm media renders and pan/zoom works. | 2026-03-23 |
| Export / HTML Canvas / Fidelity and Animation | Ensure exported HTML matches 2D D3 styles and animations | Apply `useGraphCanvasStyles` visual fidelity (strokes, colors, `kgNodeBob` animations) to the generated SVG elements before capturing the snapshot; refactor style application into a shared standalone function. | `canvas/src/components/GraphCanvas/useGraphCanvasStyles.ts`, `canvas/src/lib/graph/htmlCanvasSvgExport.ts` | GraphCanvas styles + SVG export | `applyGraphCanvasStyles2d`, `renderGraphCanvasSvgForHtmlExport` | SVG D3 selections + Schema | SVG snapshot with CSS variables, animations, and accurate strokes | Call the shared style applicator after simulation ticks so exported DOM is fully styled and parity with workspace is maintained. | Export a 2D canvas with node animations enabled and confirm bobbing and strokes appear in the HTML file. | 2026-03-23 |
| Export / HTML Canvas / Full Parity Elements | Ensure exported HTML contains all DOM overlays like Markdown blocks, tables, blockquotes | Inject `[data-kg-markdown-design-block]` DOM elements as `<foreignObject>` inside the SVG snapshot `<g data-kg-layer="root">` during capture; apply `HTML_STYLE_PROPS` computed styles to preserve visual parity without needing React at runtime. | `canvas/src/lib/graph/svgSnapshot.ts`, `canvas/src/features/markdown-edgeless/MarkdownDesignOverlay.tsx` | SVG Snapshot + Markdown Overlay | `buildViewportSvgMarkupFromElement`, `MarkdownDesignOverlay` | DOM elements + bounding boxes | SVG snapshot containing cloned HTML elements inside `<foreignObject>` | Before serializing the cloned SVG, query for markdown overlay blocks in the DOM, clone them, attach computed HTML styles, and append them as `<foreignObject>`s using world coordinates so they pan/zoom natively in the standalone viewer. | Smoke test: open a markdown doc with a table or blockquote, export to HTML, and confirm the table is visible and pans/zooms correctly. | 2026-03-23 |
| Docs / Performance patterns / Keyword Mode | Keep docs+schema aligned with bounded performance surfaces | Update cross-repo docs+schema to reflect existing debounce, bounded worker parsing, LRU caching, idle yielding, and opt-in perf metrics; forbid docs claiming non-existent behavior; keep all metrics structural-only and optional. | `agentic-graph/docs/documents/{agentic-graph-pipeline-deep-dive-document.md,agentic-graph-parser-document.md}`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{metadata.jsonld,keyword-mode.jsonld,parser.jsonld}` | Cross-repo documentation + AgenticRAG schema | Update Markdown + JSON-LD contract text | Existing Canvas implementations | Updated docs + schemaVersion bumps | Prefer code-derived SSOT; record stable keys and caching surfaces; only document behavior already implemented; preserve schema neutrality (no domain semantics). | Run `npm -C agentic-graph/canvas run doc:lint`, `npm -C agentic-graph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-20 |

## 2026-03-25

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas / 2D D3 / Force-directed layout stability | Stop post-settle layout snaps and malformed jumps | Document and enforce bounded force-directed layout: preserve simulation positions across scene rebuilds, disable strict-overlap relax once alpha is near-settled, and ensure overlay/panel changes never restart the simulation or mutate settled node positions. | `agentic-graph/canvas/src/components/GraphCanvas/{scene.ts,simulation.ts}`, `agentic-graph/canvas/src/components/GraphCanvasRoot/hooks/{useD3GraphScene2d.ts,useD3PresentationUpdates2d.ts}`, `agentic-graph/canvas/src/lib/graph/physics2dTuning.ts`, `agentic-graph/docs/documents/agentic-graph-graph-canvas-layout-ssot.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | Force layout + overlap relax SSOT | `buildSimulation`, `computeStrictOverlapTuning2d`, `useD3GraphScene2d`, `useD3PresentationUpdates2d` | GraphData + schema + overlay membership + interaction mode | Stable force-directed layout with no post-settle snaps | Preserve positions on scene rebuilds, gate strict-overlap passes by alpha, and route overlay membership changes through presentation updates instead of restarting D3 or mutating layout caches. | Run `npm -C agentic-graph/canvas run check && npm -C agentic-graph/canvas test`, then `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to keep docs/schema aligned. | 2026-03-25 |
| Canvas / Interaction Modes / Workspace Sync | Keep Canvas Interaction Mode + Workspace Sync Mode SSOT across Toolbar, Settings, and AgenticRAG schema | Document `infiniteCanvasInteractionMode∈{static,interactive}` and `canvasWorkspaceSyncMode∈{manual,realtime}` as SSOT toggles shared by Toolbar and Settings. Static+Manual is the default: force-directed layout runs to stability then freezes; rich-media/markdown overlays forward wheel/pointer to Canvas; Graph Data Table and GraphRecordDb avoid position-only churn and only sync on content changes or explicit “Sync now”. Interactive+Realtime enables continuous D3 forces and fully interactive overlays/panels while still forbidding graph/layout/zoom mutations outside user actions. | `agentic-graph/canvas/src/hooks/store/{canvasSlice.ts,types.ts}`, `agentic-graph/canvas/src/features/settings/{registry-ui.graph-and-orchestrator.ts,useSettingsView.ts}`, `agentic-graph/canvas/src/components/{Toolbar.tsx,GraphCanvasRoot/hooks/useD3GraphScene2d.ts,GraphCanvasRoot/components/RichMediaOverlayLayer2d.tsx}`, `agentic-graph/canvas/src/features/{markdown-workspace/main/viewer/MultiDimTableSurface.tsx,graph-inspector/ui/GraphRecordSelectionInspector.tsx,graph-data-table/ui/GraphDataTableTable.tsx}`, `agentic-graph/todo-log.md`, `agentic-graph/docs/documents/{agentic-graph-canvas-document.md,agentic-graph-renderer-document.md,agentic-graph-local-storage-document.md,agentic-graph-infinite-canvas-interaction-performance-document.md,agentic-graph-graph-data-table-fast-grid-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{canvas.jsonld,panels.jsonld,settings.jsonld,README.md}` | Canvas interaction + sync SSOT | `setInfiniteCanvasInteractionMode`, `setCanvasWorkspaceSyncMode`, `useD3GraphScene2d`, `RichMediaOverlayLayer2d`, `ThreeGraph`, `useGraphRecordDbSync`, `MultiDimTableSurface`, `GraphDataTableTable` | GraphData + Schema + workspace view + Interaction Mode + Sync Mode | Stable interaction behavior across 2D/3D renderers, Editor/Graph Data Table, and HTML Canvas exports without duplicate paths or background churn | Default to `static` + `manual`; in static mode, keep force layout bounded and frozen post-stabilization, forward overlay wheel/pointer to Canvas, and gate GraphTable sync off `graphContentRevision` to avoid position-only churn. In interactive mode, keep D3 force layout running, allow embedded overlays to be fully interactive (no wheel forwarding when safe), and key GraphTable sync off `graphDataRevision` while still deduping writes via `noteGraphWrite` gates. Workspace Sync Mode `manual` disables auto GraphRecordDb sync but exposes a single “Sync now” action; `realtime` enables auto sync behind the same revision+viewKey gates. | Run `npm -C agentic-graph/canvas run test:ci -- toolbarButtons settings zoomStateEq graphRecord.cache graphDataTableTypography` then `npm -C agentic-graph/canvas run doc:lint` and `npm -C agentic-graph/canvas run doc:sanity`; finally run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to keep schema+docs aligned with implemented toggles and LS keys. | 2026-03-25 |

## 2026-03-28

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 2D D3 Bipartite super-groups | Keep Bipartite layout + group hierarchy docs aligned with implementation | Update cross-repo docs+schema for 2D D3 Bipartite: document left/right lane layout, nested super-group→group→member hierarchy built from kg:subgraphs, and visibility invariants for clusters/headers/edges; forbid domain-specific rules, duplicate pipelines, recompute loops, or renderer drift. | `agentic-graph/docs/documents/agentic-graph-renderer-document.md`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | 2D D3 Bipartite renderer docs + AgenticRAG canvas contract | Update Markdown + JSON-LD text | Implemented Bipartite layout + super-group/group hierarchy | Updated docs + schemaVersion bump + cross-repo invariants | Document only code-backed behavior; keep hierarchy neutral and renderer-agnostic; avoid duplicating layout pipelines; run `npm -C agentic-graph/canvas run doc:lint` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-28 |

## 2026-03-29

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Edge Types | Keep edge-type docs aligned with global renderer SSOT | Document global Edge Types (bezier, straight, step, smoothstep) as schema.layout.edges.type SSOT and describe how FloatingPanel Edge Types and AgenticRAG edge properties drive cross-renderer visuals without affecting graph semantics. | `agentic-graph/docs/documents/agentic-graph-renderer-document.md`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,edge-schema.jsonld,canvas.jsonld}` | Cross-repo Edge Types docs + AgenticRAG schema | Update Markdown + JSON-LD text | Implemented global edgeTypes utility + renderer wiring | Updated docs + schema text + sync_map alignment | Document only code-backed global edge-type behavior; keep schema fields structural-only; forbid renderer-specific edge semantics or duplicate per-surface type switches. | Run `npm -C agentic-graph/canvas run doc:lint`, then `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-29 |
| Docs / Canvas / 2D D3 + 3D Animation | Document radial orbit animation + toolbar Animation switch | Update cross-repo docs+schema to describe 2D D3 radial Animation modes (Force-directed vs Orbit) and 3D globe-based animation, including toolbar Animation toggle, applicability guards, and SSOT layout/derivation invariants; forbid per-renderer drift, duplicate pipelines, and animation-specific recompute loops. | `agentic-graph/docs/documents/{agentic-graph-renderer-document.md,agentic-graph-graph-canvas-layout-ssot.md}`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md}`, `fixtures/agentic-graph-animation-demo.md` | Cross-repo 2D/3D animation docs | Update Markdown text | Implemented 2D D3 radial orbit animation, toolbar Animation switch, and 3D globe-based animation | Updated docs + demo markdown | Treat Animation as view-only: preserve SSOT GraphData/layout; restrict Orbit mode to D3 radial with bounded motion and no re-derivation loops. | Run `npm -C agentic-graph/canvas run doc:lint` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-29 |

## 2026-03-30

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 3D Voxel Mode | Keep Voxel Mode docs aligned with D3 Bipartite XY parity | Document Voxel Mode as a 3D D3-Bipartite sub-mode: XY parity on the ground plane, Z columns forbidden, layout-mode keyed cache, and view-only toggles across 2D/3D/Voxel and semantic/layout modes. | `agentic-graph/docs/documents/agentic-graph-renderer-document.md`, `agentic-graph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | Cross-repo Voxel Mode docs + AgenticRAG canvas contract | Update Markdown + JSON-LD text | Implemented voxel XY parity + ground-plane Z=0 + cache-key fixes | Updated docs + schema text + sync_map alignment | Treat voxel as a view-only 3D sub-mode that reuses D3 Bipartite seeds and SSOT layout caches; forbid voxel-specific derivation pipelines. | Run `npm -C agentic-graph/canvas run doc:lint` then `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-30 |

## 2026-03-31

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Multi-dimensional Table / RxDB | Document Multi-dimensional Table and RxDB GraphRecordDb as JSON backbone | Update cross-repo docs to describe Multi-dimensional Table as RxDB-backed GraphRecordDb over JSON GraphData, clarify nodes/edges tables, property columns, and sync gating, and forbid alternate table backends or non-JSON persistence paths. | `agentic-graph/docs/documents/agentic-graph-graph-data-table-fast-grid-document.md`, `huijoohwee.github.io/schema/AgenticRAG/README.md`, `agentic-graph/todo-log.md` | Cross-repo GraphRecordDb + Multi-dimensional Table docs | GraphRecordDb + Graph Data Table workspace | GraphData + schema + workspace view + Interaction/Sync modes | Updated JSON-based Multi-dimensional Table docs + aligned schema text | Document only code-backed RxDB GraphRecordDb behavior; keep JSON table contract neutral; forbid additional persistent backends. | Run `npm -C agentic-graph/canvas run doc:lint`, `npm -C agentic-graph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-31 |
