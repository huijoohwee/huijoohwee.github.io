---
title: "Agentic Canvas OS Todo 2026-06"
doc_type: "Planning Ledger Shard"
schema: "todo-log/v1"
period: "2026-06"
scope: "cross-repository"
status: "append-only"
append_policy: "append-only"
date_heading_format: "YYYY-MM-DD"
source_contract: "../docs/TODO.md"
adoption_date: "2026-07-14"
historical_source: "$GITHUB_ROOT/agentic-graph/todo-log.md"
historical_policy: "preserve-row-bytes"
---

# Todo 2026-06

This shard is governed by `../docs/TODO.md`. Pre-adoption rows retain their imported source bytes; new rows append at EOF.

> Canonical identity normalization (2026-09-03): mutable product, path, and runtime references in pre-adoption rows use `agentic-graph` and `AGENTIC_OS_*`. The four dated deployed-route URLs remain verbatim because they are observed external delivery-receipt subjects, not current configuration or authority.

## 2026-06-04

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Shared response card/audio rendering | Keep AI response text and audio media renderer-neutral | Project generated `output` text into shared editable Card/Storyboard rows; route `audioUrl`/audio media through shared card, panel, overlay, preview, inventory, importer, and HTML-export owners. | `agentic-graph/canvas/src/{lib/{cards,canvas,render,graph,websites},components,features/storyboard-widget-manager,__tests__,tests/registry}` | Shared media/card response owners | `GRAPH_NODE_CARD_OUTPUT_PROPERTY_KEYS`, `buildStoryboardBoardModel`, `getNodeMediaSpec`, `buildRichMediaPanelPreviewSpec`, `CardMediaPreview`, `resolveWidgetCompactPreview` | Headless response fields plus `audioUrl`/audio media nodes | Editable output rows, audio cards/widgets/panels/overlays, audio inventory counts, audio HTML export | Audio is a normal media kind and generated text is a normal card field; shared schema paths and media-kind inference own projection. | Re-smoke FloatingPanel Chat -> Storyboard/Storyboard Widget with an audio URL response before deploy approval. | 2026-06-04 |
| MCP-shaped chat structured content | Make FloatingPanel Chat responses materialize like MCP tool results | Extract `response.structuredContent`, literal MCP `result.structuredContent`, and structured `result.content[]` text blocks from assistant YAML/JSON or accepted KGC frontmatter, normalize plain fields plus typed KTV envelopes, preserve declared widget-form records as Storyboard Widget nodes with document-scoped registry entries, preserve safe `flow:compute`, and project panels, cards, media, nodes, generated edges, authored edges, and overlay refs into canonical KGC flow data before workspace apply. | `agentic-graph/canvas/src/features/chat/{chatResponseStructuredContent.ts,chatResponseStructuredContentProjector.ts,chatHistoryWorkspace.kgc.baseFallback.ts,chatHistoryWorkspace.kgc.build.ts,chatResponseBaseContract.ts}` | Chat structured-content projection | `extractChatResponseStructuredSurface`, `projectChatResponseStructuredSurfaceIntoKgcFrontmatter`, `normalizeKgcAssistantBodyForStorage`, `unwrapFlowEnvelopeFieldValue`, `resolveWidgetRegistryEntry`, `computeFlowConnectedValuesBySchemaPath`, `buildStoryboardWidgetInlineComputeOutputPatch`, `buildGraphNodeCanonicalTextPatch` | MCP-style structured content with declared widget form/type records, literal MCP result envelopes, safe inline compute data, text, image, audio, video, HTML, edge fields, exact `{key,type,value}` envelopes, or `properties[]` KTV rows | Parsed Storyboard Widget nodes plus document-scoped registry entries for declared forms, Rich Media Panel endpoints otherwise, `n-deliver` response edges, authored handle-bearing edges, `widget_bundle.graph.nodes_ref` overlay refs, editable shared media/card render fields, active frontmatter writeback, provider-free inline-compute run writeback, connected-value recomputation, and run-all eligible dataflow widgets | Chat persists structured tool-result semantics as normal frontmatter-flow data; typed frontmatter values unwrap upstream before parser; literal MCP envelopes and content text parts resolve upstream; declared widget forms use shared Storyboard Widget registry and connected-value owners; authored `flow:compute` nodes write computed output schema paths before provider TextGeneration dispatch; neutral render records use shared Rich Media owners; inline edits keep flattened and native `properties` mirrors aligned through shared card patch/updateNode owners. | Live-smoke a FloatingPanel Chat response that emits typed `response.structuredContent.widgets[]`, literal MCP `result.structuredContent`, safe `flow:compute`, handle-bearing `edges[]`, run-all inline compute, and inline-edited output before deploy approval. | 2026-06-04 |
| MCP submit validation acceptance | Stop valid MCP structured results from entering legacy KGC retry | Finalize literal MCP structured responses at the shared attempt resolver when a renderable structured surface exists; do not synthesize KGC or retry for a KGC block. | `agentic-graph/canvas/src/features/chat/floatingPanelChat/{floatingPanelChatKgcAttempt.ts,floatingPanelChatSubmitCoordinator.ts}` | Chat submit validation owner | `resolveChatAgenticGraphAttempt`, `executeFloatingPanelChatSubmitCoordinator`, `extractChatResponseStructuredSurface` | Literal MCP `result.structuredContent` with renderable widgets, panels, cards, media, or edges | One provider attempt, raw assistant response finalized, `validatedKgc=null`, structured-response validation snapshot | The root validator owns acceptance; the coordinator keeps one finalize path and existing workspace/canvas projection materializes the response later. | Re-smoke a real provider or local runtime command emitting literal MCP structured content before deploy approval. | 2026-06-04 |
| Storyboard Widget-run workspace artifacts | Make widget LLM/transcript/image/video output land in Editor Workspace without renderer forks | Persist completed Text Widget and Video Transcriber Markdown outputs as sibling workspace artifacts; persist Image Generation and Video Generation binaries plus editable Markdown manifests; passively register text artifacts and media manifests in Source Files; carry workspace paths through shared widget/Rich Media Panel patches. | `agentic-graph/{canvas/src/features/chat,canvas/src/components/StoryboardWidgetCanvas/runtime,docs/documents,todo-log.md}` | Storyboard Widget workflow run + widget artifact helpers | `writeTextWidgetRunOutputArtifact`, `writeRichMediaWidgetRunOutputArtifact`, `buildTextWidgetOutputPatch`, `buildRichMediaWidgetOutputPatch`, `runWorkflowNode` | Final generated text/transcript/image/video output plus active workspace path | Workspace Markdown artifact or manifest, binary media artifact path, Source Files entry, widget/Rich Media Panel output metadata | Final text writes once; media writes one binary and one editable manifest; Source Files registration uses `applyToGraph: false`; no provider, renderer, or graph-apply branch owns local artifacts. | Live-smoke Storyboard Widget Text/Image/Video widget runs from a workspace-backed document and confirm the sibling artifact or manifest appears in Source Files before any deploy. | 2026-06-04 |
| Chat KGC workspace landing | Make LLM output render through shared Source Files owners | Route saved `chatAgenticGraph` KGC apply through workspace import before active markdown graph apply. | `agentic-graph/{canvas/src/features/chat,canvas/src/features/workspace-fs,docs/documents,todo-log.md}` | Chat KGC canvas bridge | `applyChatKgcWorkspaceDocumentToCanvas` | Saved canonical KGC markdown | Parsed Source Files entry plus active canvas document | Source Files import owns renderer-neutral materialization; active markdown actions own graph apply; no provider, renderer, or downstream patch branch. | Mirror schema docs next and re-smoke Storyboard on a live chatAgenticGraph response before any deploy. | 2026-06-04 |
| Canvas toolbar dock SSOT | Keep editor-workspace canvas controls reachable on mobile | Move canvas toolbar dock geometry into shared responsive owners and span the mobile safe viewport while desktop keeps the editor-pane boundary. | `agentic-graph/canvas/src/{pages/Canvas.tsx,lib/ui/responsiveElementClasses.ts,styles/responsive-canvas-toolbar.css,__tests__}` | Canvas toolbar dock | `workspaceToolbarBoundaryStyle` | Desktop, narrow, and coarse-pointer viewports | Bounded top desktop dock and bottom mobile dock | Focused shared CSS owns geometry and content clamps; React only suppresses the desktop pane boundary on narrow/coarse viewports; toolbar row-scroll remains the scroll owner. | Re-smoke `?openEditorWorkspace=1` after future toolbar, workspace split, or mobile breakpoint changes. | 2026-06-04 |
| Stripe Checkout payment acceptance | Accept hosted Stripe payments without browser secrets | Route checkout through the payment Worker, preserve same-window Session ID redirects, and keep Checkout URLs session-only. | `agentic-graph/{grph-shared/src/payments/stripePaymentSsot.ts,cloudflare/workers/agentic-graph-payment,canvas/src/features/payments,docs/documents,todo-log.md}` | Stripe payment Worker + Canvas return runtime | `resolveStripeCheckoutServerConfig`, `buildStripeCheckoutSessionCreateForm`, `createStripeHostedCheckoutSessionForWorker`, `handleStripeCheckoutStatus`, `handleStripeWebhook`, `StripeCheckoutReturnRuntime`, `PaywallOverlay` | Worker env, hosted Checkout returns, webhook events | One-time payment default, subscription mode via Price ID, visible Worker `[vars]` price/mode/return origin, Worker-secret-only Stripe credentials/webhook secret, locally-owned D1 row before live Stripe status lookup, minimal public status payload, payment-status-only return unlock, same-payload duplicate webhook acknowledgement, retryable failed/stale webhook processing state, hosted Session expiry on audit persistence failure | Server env owns price/mode/return origin; paywall always creates fresh Sessions; status reads D1 first, requires canonical `session_id`, retrieves Stripe only for existing open/unpaid rows, rejects unknown session ids and legacy `id` aliases before Stripe calls, keeps customer/metadata/url/workspace details in D1, returns only minimal payment state publicly, unlocks only paid/no-payment-required returns, clears transient URLs on return, skips same-payload duplicate side effects, retries failed processing, reclaims stale claims, and expires a hosted Stripe Session before returning if the Stripe audit row cannot be persisted. | Configure visible Worker `[vars]` checkout price authority, configure `STRIPE_WEBHOOK_SECRET` as a Worker secret, and apply the webhook processing D1 migration with `npx wrangler d1 migrations apply agentic-graph-storage --remote --config cloudflare/workers/agentic-graph-payment/wrangler.toml`, then deploy/payment-smoke when production checkout is authorized. | 2026-06-04 |
| Agentic Stripe Checkout ACP settlement | Accept agent-initiated fiat checkout through hosted Stripe | Let ACP create requests include `stripe_checkout`, return hosted Checkout URLs, and settle, fail, or cancel only from verified matching Stripe webhook/status refresh. | `agentic-graph/{grph-shared/src/payments/{agenticCommerceSsot.ts,stripePaymentSsot.ts},cloudflare/workers/agentic-graph-payment,canvas/src/features/payments,docs/documents,todo-log.md}` | Stripe payment Worker + ACP settlement | `createStripeHostedCheckoutSessionForWorker`, `expireStripeHostedCheckoutSessionForWorker`, `handleStripeCheckoutStatus`, `settleAgenticCommerceSessionFromStripeSession`, `failAgenticCommerceSessionFromStripeSession`, `cancelAgenticCommerceSessionFromExpiredStripeSession`, `handleCompleteSession` | `stripe_checkout`, `agenticCommerceSessionId`, `workspaceId`, expected amount/currency, Stripe idempotency, Stripe session metadata, delegate token, Stripe event id | `session.stripe_checkout.url`, `metadata[acp_session_id]`, `metadata[expected_amount_total]`, `metadata[expected_currency]`, `Idempotency-Key`, server-owned return origin, ACP proof settlement, `payment_failed`, `cancelled`, duplicate webhook acknowledgement, or 409 guard | ACP id owns Stripe client reference and idempotency; hosted return URLs and Stripe totals must match route origin/config and ACP amount/currency; Stripe audit or ACP persistence failures after Stripe creation expire the hosted Session before returning; Stripe settlement also requires `client_reference_id` to match `metadata[acp_session_id]`; terminal states reject later local completion. | Configure Worker checkout price and run the live create-and-expire payment smoke before Cloudflare deploy approval. | 2026-06-04 |
| Solana Pay ACP payment acceptance | Accept Solana Pay without a parallel payment service | Add Solana Pay as an ACP rail in the payment Worker, validate signatures through Solana RPC, and keep generic webhooks unable to settle unconfirmed on-chain sessions. | `agentic-graph/{grph-shared/src/payments,cloudflare/workers/agentic-graph-payment,canvas/src/__tests__,docs/documents,todo-log.md}` | Solana Pay payment Worker rail | `buildAgenticCommerceSolanaPayUrl`, `confirmAgenticCommerceSolanaPayTransfer`, `handleSolanaPaySettle`, `settleAgenticCommerceSession` | `payment_rail="solana_pay"`, recipient, SPL token, deterministic reference, memo, submitted signature | `session.solana_pay.url`, `pending_onchain`, Solana RPC confirmation, ACP proof, payment trace, or 409 webhook guard | Existing ACP session, D1, proof, and trace owners stay canonical; settlement validates generated reference, memo, recipient, amount, and SPL/SOL transfer deltas before proof write. | Configure `SOLANA_PAY_RECIPIENT`, `SOLANA_PAY_SPL_TOKEN`, and `SOLANA_PAY_RPC_URL`, then run a bounded live transfer smoke before deploy approval. | 2026-06-04 |
| x402 pay-to authority | Accept machine-native payments only to an operator-owned address | Keep zero-address `X402_PAY_TO_ADDRESS` as future-setup placeholder while rejecting fallback and placeholder `payTo` in x402 readiness. | `agentic-graph/{cloudflare/workers/agentic-graph-payment/wrangler.toml,grph-shared/src/payments/agenticCommerceSsot.ts,scripts/{agent-ready-commerce-checks,configure-x402-payment-worker}.mjs,canvas/src/__tests__,docs/documents,todo-log.md}` | x402 payment readiness | `assertAuthoritativeX402PaymentRequired`, `readAgenticCommerceX402PayToAddress`, `payment:x402:configure`, `payment:x402:readiness` | `PAYMENT-REQUIRED`, `X402_PAY_TO_ADDRESS`, fallback derived address, placeholder zero address | Pass/fail x402 readiness authority | x402 response syntax is insufficient for production if `payTo` equals the shared deterministic fallback or zero-address placeholder; readiness requires an ERC-20 asset, exact scheme, network, and configured EVM pay-to address. | Replace placeholder with `X402_PAY_TO_ADDRESS=0x... npm run payment:x402:configure -- --write-visible-vars --deploy-visible-vars --apply --yes --confirm=apply-stripe-payment-worker-config`, then rerun `payment:x402:readiness`. | 2026-06-05 |
| Combined payment readiness | Prove human and agentic payment gates together | Aggregate existing Stripe and x402 readiness scripts without moving their semantics. | `agentic-graph/{scripts/check-payment-readiness.mjs,canvas/src/__tests__/paymentReadinessScript.test.ts,todo-log.md}` | Payment readiness wrapper | `payment:readiness` | Stripe readiness JSON, x402 readiness JSON | Combined pass/fail summary | The wrapper only composes focused gates and passes `--live-checkout-create` through to Stripe when explicitly requested. | After configuring Stripe price authority and `X402_PAY_TO_ADDRESS`, run `npm run payment:readiness -- --live-checkout-create`. | 2026-06-04 |
| Stripe payment readiness gate | Make production payment blockers explicit without accidental Stripe mutations | Add a non-mutating Worker config and remote D1 schema check, surface it in Commerce, then require an explicit bounded live Checkout create-and-expire smoke flag. | `agentic-graph/{grph-shared/src/payments/stripePaymentSsot.ts,scripts/check-stripe-payment-readiness.mjs,canvas/src/features/panels/views/stripePaymentApiDocs.ts,docs/documents,todo-log.md}` | Payment readiness script + MainPanel Commerce Stripe rows | `payment:stripe:readiness`, `stripeApi.worker.d1_migrations`, `stripeApi.worker.readiness_gate` | Wrangler secret names, visible Worker vars, remote D1 table/column names, bounded optional live Checkout create-and-expire flag | Pass/fail readiness report and operator rows | Config check requires server key, visible Worker `[vars]` checkout price authority, webhook secret, visible checkout mode and return origin when set, payment D1 tables, webhook-processing columns, nullable webhook claim fields, and a timeout around the optional live smoke; readiness fails if Stripe credentials appear in visible Worker `[vars]`, fails if checkout price authority, checkout mode, or return origin is hidden as a Worker secret, and Commerce exposes the same readiness scope. | Apply pending payment D1 migrations, keep non-secret checkout price/mode/return origin in Worker `[vars]`, keep Stripe credentials as Worker secrets, then after Stripe webhook config run `npm run payment:stripe:readiness -- --live-checkout-create` for a bounded create-and-expire smoke; use `-- --live-checkout-timeout-ms=<ms>` only when intentionally adjusting the smoke timeout. | 2026-06-04 |
| Stripe payment Worker config helper | Apply operator-supplied Stripe config without source churn | Add a dry-run-first helper that validates env values and requires explicit confirmation before `wrangler secret put`. | `agentic-graph/{grph-shared/src/payments/stripePaymentSsot.ts,scripts/{configure-stripe-payment-worker.mjs,stripe-payment-script-runtime.mjs},package.json,canvas/src/__tests__,docs/documents,todo-log.md}` | Payment config script + Commerce settings docs | `payment:stripe:configure` | Process env, visible Worker `[vars]`, shared Stripe payment SSOT, Wrangler secret put | Secret-name apply plan or guarded apply | The helper reads visible Worker `[vars]` checkout price authority, rejects missing webhook/price authority, conflicting price sources, subscription inline pricing, and checkout price/mode/return-origin process input that would hide non-secret Worker `[vars]` config as secrets, and apply attempts missing confirmation. | Keep checkout price authority, `STRIPE_CHECKOUT_MODE`, and `STRIPE_CHECKOUT_RETURN_ORIGIN` in Worker `[vars]`, supply the real Stripe webhook secret through process env, dry-run, then apply with the confirmation flag when production config is authorized. | 2026-06-04 |

## 2026-06-05

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| MiroMind Pages server-managed key readiness | Prove MainPanel MiroMind API key setup without browser secrets | Keep `miromindApi.api_key` server-managed by default, own the focused contract locally through `npm run test:smoke:miromind:source`, and gate live Pages with the non-deploy readiness command. | `agentic-graph/{canvas/{src/__tests__,src/tests/subsetMiroMindSmoke.ts,package.json},scripts/check-miromind-readiness.mjs,docs/documents,todo-log.md}` | MainPanel Integrations + Pages chat proxy | `testMainPanelMiroMindApiKeyUsesServerManagedPagesSecretContract`, `test:smoke:miromind:source`, `miromind:readiness:check` | Toolbar -> MainPanel Integrations -> `miromindApi.api_key`, `joohwee` Pages `MIROMIND_API_KEY` | Focused local proof, secret-list proof, live no-BYOK proxy smoke | Pages secrets must be present before the deployment that reads them; local UI and proxy code can pass while the live route still needs a fresh Pages deploy. | After deploy approval, run `npm run pages:deploy-cloudflare`, then rerun `npm run miromind:readiness:check` against `joohwee.pages.dev` and `airvio.co/__chat_proxy`. | 2026-06-28 |
| Toolbar Theme and renderer palette consistency | Keep one compact Theme button and palette defaults source-owned | Restore the single Toolbar Theme cycle button while keeping System/Light/Dark order centralized; route renderer palette reads through schema helper. | `agentic-graph/canvas/src/{components/Toolbar.tsx,lib/ui/theme.ts,features/toolbar/hooks/{useToolbarActions,useRendererPalette}.ts}` | Toolbar Theme + renderer palette SSOT | `THEME_MODE_OPTIONS`, `getNextThemeMode`, `handleToggleTheme`, `getRendererPalette` | Toolbar theme click + `schema.metadata['renderer:palette']` | One compact Theme button cycles System -> Light -> Dark -> System; shared palette merge path remains schema-owned | Theme option order is centralized in `THEME_MODE_OPTIONS`; palette defaults are read by `getRendererPalette(schema)`; segmented Toolbar groups and local mode maps are forbidden. | Mirror the theme/palette contract into AgenticRAG docs after the next cross-repo schema-doc batch; keep Prod/Cloudflare deploy out of scope until requested. | 2026-06-05 |

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| 2D Dashboard renderer | Render graph-derived dashboard through existing D3 | Register canonical Dashboard surface, derive cards from active graph, and let Display Controls Grid own chart gridlines. | `agentic-graph/canvas/src/{components/DashboardCanvas,lib/config.render.ts,components/toolbar,__tests__}` | Dashboard renderer | `buildDashboardCanvasModel`, `DashboardCanvas`, `getCanvasViewRendererOptions` | Active `GraphData`, `GraphSchema.behavior.canvasGrid` | D3 dashboard canvas, generic cards, Grid On/Off marker | Dashboard is graph-derived and file-agnostic; no alternate chart runtime, fixture backfill, or external-demo data; minimap stays disabled. | Mirror AgenticRAG schema only when cross-repo doc batch is authorized; Dev→Prod/Cloudflare deploy remains blocked until instructed. | 2026-06-05 |
| 2D renderer centroid SSOT | Center graph elements consistently across 2D surfaces | Reuse one shared graph-element centroid/viewport helper for D3, Flow Canvas, Storyboard Widget, Design, SVG export, layout seed, post-fit, and arrange owners. | `agentic-graph/canvas/src/{lib/canvas/graph-elements,components/{GraphCanvas,FlowCanvas,DesignCanvas},lib/graph,__tests__,tests/registry}` | Graph element centroid utility | `computeViewportCenteredTransformForGraphElements`, `measureGraphElementCenterSet`, `computeLayoutRectSetViewportCenterShift` | Node/group/edge points, visual rects, viewport bounds, zoom transforms | Shared centroid metrics, viewport-centered transforms, layout rect recenter shifts | Graph-element centering is renderer-neutral; 2D renderers import the helper and no longer own duplicate local centroid sums or switching-specific center patches. | Re-smoke live Source Files switches across D3, Flow, Design, and Storyboard before deploy approval. | 2026-06-05 |
| Source Files Share URL context menu | Make right-click Share URL publish before copy and fail visibly | Hydrate metadata-only workspace entries through the shared active-document resolver before storage publish, reuse FS reads for path publishes, and make D1 document writes converge by canonical workspace/path. | `agentic-graph/{canvas/src/{features/source-files,features/markdown-workspace,__tests__,tests/registry},cloudflare/workers/agentic-graph-storage}` | Source Files Share URL | `publishWorkspaceEntryShareUrl`, `publishWorkspaceEntriesToAgenticGraphStorage`, `processDocumentMutation`, `buildMarkdownFileTreeContextMenuItems` | Metadata-only `WorkspaceEntry`, storage worker publish, seeded D1 document row, clipboard fallback | Published storage document plus copied/prompted URL or visible publish error | Storage publication owns public URL validity; D1 writes resolve `(workspace_id, canonical_path)` before insert so seeded docs and Share URL records do not create duplicate rows or UNIQUE failures. | Re-smoke live Source Files right-click Share URL when production storage configuration is authorized; no Dev->Prod or Cloudflare deploy was run. | 2026-06-05 |
| Generated chat artifact storage | Make New Chat KGC Markdown and binary outputs durable without a FloatingPanel uploader | Promote generated `/chat-log/{session}` Markdown to the server-owned GitHub write route first, then mirror to Source Files storage; upload generated binary bytes to R2 and promote a sibling Markdown manifest. | `agentic-graph/{canvas/src/{features/chat,features/source-files,__tests__,tests/registry},cloudflare/{pages,workers/agentic-graph-storage},scripts}` | Chat artifact storage promotion | `persistChatStreamArtifacts`, `writeKgcCompanionOutputBlob`, `publishGeneratedWorkspacePathsToGitHub`, `publishGeneratedWorkspacePathsToAgenticGraphStorage`, `handleGitHubWorkspaceWrite`, `configure-pages-github-write.mjs`, `readAgenticGraphStorageRuntimeSyncEnabled`, `e2e-github-canonical-storage-sync.mjs` | Canonical KGC path, trace/report paths, generated PNG/video Blob, GitHub write env, runtime sync env | GitHub repository file under `chat-log/{session}/...` as canonical write target; D1/R2 as secondary read/share/cache targets when runtime storage is enabled | Workspace-backed files remain skipped by background sync; generated chat artifacts opt into GitHub-first promotion once through the shared generated-artifact path, using canonical `chat-log/...` repository paths; binary bytes stay out of D1 and D1 stores only the searchable `.manifest.md`; if GitHub write fails, Cloudflare cache promotion is skipped. | Production Pages has repo, branch, and token bindings; `npm run e2e:github-canonical-storage:dev` and `npm run e2e:github-canonical-storage:prod -- --json` validate GitHub-before-Cloudflare ordering, custom-domain route aliasing, GitHub Contents readback, D1 doc/pull, and Share URL readback. Latest production proof wrote commit `e750ca7e1afa8bddc6b64fb28ed5d16060f8d99a`. Re-smoke with an actual FloatingPanel Chat -> New Chat turn when validating full assistant output UX. | 2026-06-06 |
| Source Files cache and switching performance | Reduce repeated active-document hydration during Editor Workspace Source Files switching and startup | Share same-key resolved-text promises, reuse active-path startup snapshots, and settle completed document switches without retry churn; keep ownership upstream. | `agentic-graph/canvas/src/{lib/markdown-workspace-runtime,features/source-files,__tests__,tests/registry}` | Source Files selection/startup cache | `readCachedWorkspaceSelectionResolvedTextForActivePath`, `isWorkspaceDocumentSwitchApplySettled`, `createWorkspaceStartupSourceRootEntriesReader` | Same active workspace path, duplicate switch/startup hydration, completed switch retry tick | One FS/storage read per same-key handoff, retained active text, settled retry timers, registered focused guards | Cache keys include active path, canonical-read intent, entry timestamp, and text hash; switch settlement requires selected name/text plus Canvas graph source convergence; no renderer-local fallback or document hardcode. | Mirror AgenticRAG schema note after the next cross-repo docs batch; re-smoke live Source Files reload on Storyboard Widget before deploy approval. | 2026-06-05 |
| Source Files 2D renderer switching | Keep selected-file renderer presets authoritative during Source Files switches | Include current 2D renderer in switch settlement and gate shared widget frontmatter handoff to the Storyboard renderer only. | `agentic-graph/canvas/src/{lib/markdown-workspace-runtime,components,__tests__}` | Source Files renderer switch authority | `isWorkspace2dRendererPresetStaleForDocument`, `isWorkspaceDocumentSwitchApplySettled`, `CanvasViewport` | Selected Markdown `kgCanvas2dRenderer`, current Canvas renderer, graph source | Matching active Design, GitGraph, or Storyboard surface with no stale shared-widget override | Frontmatter renderer preset must converge before switch settles; frontmatter-flow graph shape cannot override non-Storyboard 2D renderers while Editor Workspace is open. | Re-smoke Design, GitGraph, and Storyboard file switches before deploy approval. | 2026-06-05 |
| KGC session artifact consolidation | Keep canonical run and trace documents as the retained Markdown outputs | Write final KGC to `kgc_*`, keep `kgc-trace_*`, fold stream-log content into trace, fold markdown run/manifests into canonical, and repair invalid inline compute YAML at source. | `agentic-graph/canvas/src/{features/chat,lib/markdown,features/parsers,__tests__}`, `agentic-graph/docs/documents` | KGC workspace artifact policy | `appendChatHistoryWorkspaceFile`, `persistChatStreamArtifacts`, `persistAgenticGraphVdeoxplnRunManifestForChat`, `emitKgcRunOutput`, `repairFrontmatterYamlSyntax` | KGC finalization, stream SSE metadata, markdown run output, vdeoxpln manifests, malformed inline compute envelopes | `kgc_YYYYMMDDTHHmmssZ.md` plus `kgc-trace_YYYYMMDDTHHmmssZ.md` with consolidated sections and parseable frontmatter | Canonical KGC remains the graph/run source; trace owns observability; markdown companions become idempotent marked sections; syntax repair is generic and in-memory, not file backfill. | Re-smoke one live FloatingPanel Chat KGC turn and confirm no `chat-stream-log_*` or markdown `kgc-output_*` sidecar appears before deploy approval. | 2026-06-05 |
| Root config folder cleanup | Keep config artifacts under one data-owned SSOT | Move root config folders to `data/config/*`, remove duplicate shell delete shims, and keep parser, canvas, docs, and generated previews on canonical config paths. | `agentic-graph/{data/config,canvas/src,agentic_graph_parser,docs,CodeWiki.md,todo-log.md}` | Config path SSOT | `DEFAULT_SCHEMA_CONFIG_PATH`, `DEFAULT_GRAPHRAG_CONFIG_PATH`, `repo_path` | Former root-level config folders and duplicate shell delete shims | `data/config/{graphrag,schema,orchestrator,llm-chat}` plus clean consumers | Config artifacts are data-owned repo inputs; parser and canvas consumers derive default paths from shared constants; unreferenced shell `rm` overrides are removed instead of preserved as compatibility shims. | Mirror the `data/config` path in AgenticRAG schema docs during the next schema/API doc batch and run `sync_map.py --mode check` when that cross-repo update is authorized. | 2026-06-05 |
| Chat structured-content neutral dataflow | Make card-to-panel render payloads dynamic by default | When structured responses include source cards/widgets and panel targets without authored edges, synthesize one neutral inline compute widget and explicit handle edges; preserve authored `edges[]` and `flow:compute` authority. | `agentic-graph/canvas/src/features/chat`, `agentic-graph/canvas/src/__tests__`, `agentic-graph/docs/documents/agentic-graph-renderer-document.md` | Chat structured-content projection | `extractChatResponseStructuredSurface`, `ensureStructuredSurfaceDataflow`, `computeFlowConnectedValuesBySchemaPath` | MCP-style `structuredContent.cards[]`, `widgets[]`, `panels[]`, `media[]`, authored edges, or safe inline compute | Dynamic frontmatter-flow compute node, connected Rich Media Panel values, and Storyboard card recompute path | Explicit assistant dataflow wins; missing dataflow is repaired once upstream in the extractor, not by fixture backfill, renderer aliases, or downstream panel patches. | Re-smoke FloatingPanel Chat with a real neutral card-plus-panel structured response before any Prod or Cloudflare deploy. | 2026-06-05 |

## 2026-06-13

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Nested documentation sync into AgenticRAG map | Keep newly added API-reference and companion docs visible in the cross-repo docs graph | Make the AgenticRAG docs-map sync recurse through `docs/documents/**`, preserve relative document paths, regenerate the map, and mirror the recursive-source rule in shared schema notes instead of hand-patching generated graph artifacts. | `agentic-graph/{todo-log.md,docs/documents/agentic-graph-api-reference/agentic-graph-ai-agents-memory-layer-prd-tad.md}`, `huijoohwee.github.io/schema/AgenticRAG/{sync_map.py,README.md,agentic-graph-documents-map.graph.jsonld}`, `huijoohwee.github.io/docs/documents/hjh-workspace-todo-log.md` | Docs sync map contract | `iter_doc_rel_paths`, `get_node_doc_rel_path`, `sync_map.py --mode write` | Nested docs under `docs/documents/**` including `agentic-graph-api-reference/*` and future sharded companions | Refreshed docs map with nested relative-path document nodes plus updated schema guidance | Fix missing nested docs at the sync root once; generated map artifacts stay derived from recursive canonical docs, and hand-added downstream nodes or filename-only assumptions remain forbidden. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future nested doc additions, sharding moves, or companion-folder growth. | 2026-06-13 |

## 2026-06-15

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Cloudflare Pages chat proxy hardening for authenticated collaboration | Replace the public server-managed proxy trust boundary with source-owned authz and audit owners before claiming true multi-user collaboration | Keep `__chat_proxy` infrastructure-only, add `/api/chat/relay` auth middleware plus D1 `users`, `auth_sessions`, `workspace_memberships`, `workspace_provider_policies`, and `chat_proxy_audit`, and migrate browser chat submit to workspace-authorized relay flows before enforcing production collaboration/auth claims. | `agentic-graph/{docs/documents/agentic-graph-api-document.md,docs/documents/agentic-graph-storage-schemas-document.md,docs/documents/agentic-graph-multi-user-collaboration-prd.tad.md,todo-log.md}`, `huijoohwee/functions/__chat_proxy/[[path]].js` | Cloudflare Pages chat relay and future auth boundary | `pages:deploy-cloudflare`, `/api/chat/relay`, `/api/chat/policies/{workspaceId}`, `/api/chat/audit/{workspaceId}` | Current public `airvio.co/__chat_proxy/*` deploy chain, Cloudflare Pages project `joohwee`, and provider secret variables plus collaboration/storage planning docs | Canonical API deploy-chain map, concrete D1 auth/relay schema, and migration steps for authenticated multi-user collaboration | The Pages proxy remains the shared provider transport, but true multi-user collaboration needs D1-backed identity, membership, policy, and audit owners; do not overload the public relay as the browser trust boundary. | Implement the Worker or Pages auth middleware and D1 migrations first, then move MainPanel chat submit from direct `__chat_proxy` browser calls to authenticated `/api/chat/relay` before tightening server-managed access. | 2026-06-15 |
| Chat skill prompt modularization release follow-up | Record the Dev -> Prod -> Cloudflare release trail and keep downstream docs-map indexing canonical | Keep chat skill prompt modularization source-owned in `agentic-graph`, publish only through generated mirror sync, log paired source/publish commits plus live route proof in canonical docs, and regenerate the AgenticRAG docs map from authored release-note sources only. | `agentic-graph/{canvas/src/features/chat,docs/documents,todo-log.md}`, `huijoohwee/{content/agentic-graph,agentic-graph}`, `huijoohwee.github.io/schema/AgenticRAG/{sync_map.py,agentic-graph-documents-map.graph.jsonld}` | Chat skill registry + release ledger | `pages:build-sync`, `pages:deploy-cloudflare`, `sync_map.py --mode write` | Source commit `fcd0ea5f`, publish commit `f0422135`, deployed route `https://airvio.co/agenticgraph/`, preview deploy `https://84f45986.joohwee.pages.dev` | Added release note, updated topology/ledger trail, regenerated docs map entry for the new release document | Release evidence belongs in canonical upstream docs and generated schema mirrors only; do not hand-edit publish artifacts or downstream graph nodes when the authored release note is the real source. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future release-note additions, publish-topology changes, or companion-doc sharding. | 2026-06-15 |
| Dev -> Prod -> Cloudflare release: chat relay auth + panel updates | Publish the current Dev state through the generated mirror into Cloudflare Pages without hand-editing publish artifacts | Build with `VITE_BASE_PATH=/agentic-graph/`, sync the mirror, rebuild Functions bundle, and deploy only via `pages:deploy-cloudflare`; treat `huijoohwee/content/agentic-graph` as generated output, and keep the release trail anchored by paired repo commits plus live route proof. | `agentic-graph/{canvas/src/features/chat,cloudflare/workers/agentic-graph-storage,docs/documents,todo-log.md}`, `huijoohwee/{content/agentic-graph,_worker.js,_routes.json}`, `huijoohwee.github.io/schema/AgenticRAG/{sync_map.py,agentic-graph-documents-map.graph.jsonld}` | Dev/Prod/Pages deploy chain + release evidence | `pages:build-sync`, `pages:check-sync`, `pages:functions:build`, `pages:deploy-cloudflare`, `sync_map.py --mode check` | Dev commit `aa76bc4b`, Prod commit `5bc6ce0d`, preview `https://d8d19ebc.joohwee.pages.dev`, live `https://airvio.co/agenticgraph/` | Live `/agentic-graph/` returns `200`, preview deployed, D1 docs seed `applied=37`, focused tests pass for `sourceFiles.storageSync.chatAuth`, `ui.floatingPanelChat.relayDiagnostics`, `ui.mainPanel.skillsCommands`, `dashboard.keywords`, and `mainPanel.historyView` | Use the source-owned release pipeline so drift is prevented: sync parity must pass before deploy, Functions bundle must be generated from source, and post-deploy proof must be based on live route behavior (not local artifacts). | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` and address the reported Dependabot vulnerabilities before the next production release. | 2026-06-15 |

## 2026-06-16

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Dev -> Prod -> Cloudflare release: storyboard + Strybldr + typecheck gates | Log the production release evidence and keep the Dev->Prod->Cloudflare pipeline drift-free | Release only via `pages:build-sync` + `pages:deploy-cloudflare`, keep storyboard renderer id `storyboard` (not `strybldr`), preserve Strybldr `mediaKind` parsing, and require `npm --prefix canvas run test:ci:unit -- strybldr` + `npm --prefix canvas run typecheck` before deploying. | `agentic-graph/{canvas/src/{features/chat,features/strybldr,components/StoryboardCanvas.tsx},cloudflare/workers/agentic-graph-storage,todo-log.md}`, `huijoohwee/{content/agentic-graph,agentic-graph}` | Pages publish pipeline + storyboard/Strybldr contract owners | `pages:build-sync`, `pages:check-sync`, `pages:deploy-cloudflare` | Dev commit `36dd25e7`, Prod commit `af9f48a5`, preview `https://ea647ff5.joohwee.pages.dev` | Live `https://airvio.co/agenticgraph/` updated with sync parity; seeded docs `applied=37, conflict=0, rejected=0` | Publish artifacts are generated-only; downstream mirrors and schema graphs remain derived from canonical docs and validated pipelines. | Run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after doc changes and address Dependabot alerts before the next production release. | 2026-06-16 |

## 2026-06-17

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Storyboard + Editor Workspace/Kanban shared-utils doc sync | Keep canonical authored docs and cross-repo guidelines aligned with the new shared toolbar/data-view ownership | Update only canonical authored docs and guidelines, record shared Storyboard toolbar reuse plus shared Workspace/Kanban `New Record` and settings ownership, and forbid renderer-local utility forks or alias renderer wording. | `agentic-graph/{docs/documents/{agentic-graph-*-document.md,agentic-graph-strybldr-prd-tad.md},todo-log.md}`, `huijoohwee.github.io/{guidelines/{agentic-graph-pipeline-guidelines.md,yaml-frontmatter-guidelines.md},schema/AgenticRAG/README.pipeline-and-renderer-guidelines.md}` | Canonical docs + cross-repo guideline SSOT | authored-doc updates, guideline sync, ledger entry | Recent Storyboard toolbar helper extraction, shared Viewer/Workspace/Kanban utility reuse, and canonical renderer id `storyboard` | Updated authored docs, updated guidelines, and one planning-ledger row capturing the shared-utils sync boundary | Canonical authored docs remain upstream truth; schema/guideline mirrors describe validated ownership only, and generated or renderer-local notes must not become parallel authority. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after the next related docs batch and continue mirroring only from canonical authored sources. | 2026-06-17 |

## 2026-06-18

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Toolbar Zoom menu consolidation | Keep canvas zoom commands native, discoverable, and non-duplicated | Consolidate zoom in/out, pin, fit, selection focus, and percentage presets into one Toolbar Zoom menu while preserving shared zoom request and viewport-center transform owners. | `agentic-graph/{canvas/src/{components/Toolbar.tsx,components/toolbar/{ZoomModeSelect.tsx,toolbarZoomMenuModel.ts},__tests__/storyboardWidgetOverlayZoomDirectionRegression.test.ts,lib/config-copy/uiCopy.ts},docs/documents/agentic-graph-frontend-document.md,todo-log.md}` | Main Toolbar Zoom menu | `ZoomModeSelect`, `TOOLBAR_ZOOM_PRESETS`, `computeToolbarZoomPresetTransform`, `requestZoom`, `requestZoomTransform` | Toolbar click, active 2D zoom state, viewport dimensions, user-selected percentage preset | One Zoom menu, live percent readout, center-preserving preset transform, no separate zoom in/out toolbar buttons | Toolbar owns controls; shared zoom helpers own math; renderers consume requests; Dev-only until release is explicitly approved. | Keep future zoom additions in `toolbarZoomMenuModel.ts` or shared zoom libs, and do not deploy to Prod/Cloudflare without operator instruction. | 2026-06-18 |
| FloatingPanel Strybldr Storyboard layout controls | Let users select wide/portrait card ratios and flexible/fixed board layout without renderer forks | Keep Strybldr layout selection in shared persisted UI settings; Storyboard consumes settings only, preserves 9:16, defaults to 16:9 and Flex, and avoids copied vendor layout code. | `agentic-graph/{canvas/src/{features/strybldr/StrybldrFloatingPanelView.tsx,components/StoryboardCanvas.tsx,hooks/store,lib/config.ls.keys.ts},docs/documents/agentic-graph-frontend-document.md,todo-log.md}` | Strybldr FloatingPanel + Storyboard renderer UI settings | `setStrybldrStoryboardCardAspectMode`, `setStrybldrStoryboardBoardLayoutMode`, `StoryboardCanvas` ratio/layout classes | FloatingPanel user selection, persisted local UI settings, active Storyboard graph | Visible 16:9/Flex defaults, explicit 9:16/Fixed options, bounded right-side reference grid, source-contract guards | Strybldr owns the control surface; Storyboard owns rendering; graph/card data stays unchanged, provider-neutral, and Dev-only until release is explicitly requested. | Mirror schema notes only when the next approved cross-repo docs sync runs; do not deploy to Prod/Cloudflare without operator instruction. | 2026-06-18 |
| FloatingPanel Media, `@ Upload Media`, and Cloudflare collaboration storage docs | Keep media insertion and persistence contracts source-owned across canonical docs and schema mirrors | Document one shared Media upload/inventory owner, keep `@` insertion as inline chips, and split Cloudflare duties: R2 bytes, D1 metadata/provenance, KV access cache, Durable Objects room sync. | `agentic-graph/{docs/documents/{agentic-graph-artifact-media-storage-architecture.md,agentic-graph-storage-sync-document.md,agentic-graph-storage-schemas-document.md,agentic-graph-multi-user-collaboration-prd.tad.md},todo-log.md}`, `huijoohwee.github.io/{guidelines/agentic-graph-pipeline-guidelines.md,schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}}` | Media storage/collaboration docs SSOT | authored-doc updates, guideline sync, schema note sync | FloatingPanel Media upload, card-field `@ Upload Media`, R2/D1/KV/Durable Object runtime responsibilities | Updated canonical storage docs, collaboration extension boundary, cross-repo guideline note, AgenticRAG panel contract, and one ledger row | Media persistence is durable only after R2 and D1 confirmation; KV and Durable Objects are runtime accelerators/sync channels, not canonical blob or provenance stores. | Re-run `sync_map.py --mode check` after any new media doc is added, and keep Dev-only until a separate commit/push/deploy instruction. | 2026-06-18 |

## 2026-06-22

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Timeline video sequence editor | Make Timeline useful for imported video sequencing without copying vendor UI or adding renderer forks | Render video on Canvas; keep BottomPanel transport editing and FloatingPanel row lists source-backed. | `agentic-graph/{canvas/src/{components/{MediaCanvas.tsx,MermaidGanttCanvas.tsx,timeline/*},features/gitgraph/{GanttTimelineTransportPanel.tsx,TimelineBottomPanelView.tsx,TimelineFloatingPanelView.tsx},features/markdown-workspace/{workspaceImport/*,useWorkspaceFileActions/importActions.ts},lib/{mermaid/mermaidGanttBarInteraction.ts,url.ts},__tests__/*},docs/documents/agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md,todo-log.md}` | Timeline + Media Canvas playback + Mermaid Gantt transport + import projection | `MediaCanvas`, `registerVideoSequenceSourceFiles`, `GanttTimelineTransportPanel`, `MermaidDiagramPanelView`, `materializeVideoSequenceTimelineImportDocument`, `splitMermaidGanttCodeRowAtOffset`, `insertMermaidGanttVideoSequenceOperationRow` | Active Timeline tab, selected Mermaid Gantt row, playhead position, local files/folder videos, direct video URL imports, local absolute video paths | Media Canvas video playback, one visible `kgVideoSequenceTimeline` workspace file, manifest-only source units, BottomPanel sequence lanes/tools, FloatingPanel row list, cut/splice/mask/grade writeback | Keep Canvas playback in Media renderer and Timeline edits source-owned by Mermaid Gantt frontmatter; never persist stale blob URLs or branch on validation paths. | Add compiled video export only after a separate runtime/tool contract is approved; keep Dev-only until explicit Prod/Cloudflare release instruction. | 2026-06-22 |
| 2D Renderer Media surface | Make imported rich media inspectable on Canvas without duplicating Timeline or panel owners | Add canonical `2D Renderer: Media` that reuses shared rich-media inventory and `RichMediaPanel`. | `agentic-graph/{canvas/src/{components/{MediaCanvas.tsx,CanvasViewport.tsx,RichMediaPanel.tsx},components/toolbar/canvasViewMenu.ts,lib/{config.render.ts,config-copy/uiCopy.ts,command-menu/commandMenuRichMediaInventory.ts},__tests__/rendererPipelineNeutrality.test.ts},docs/documents/agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md,todo-log.md}` | Media Canvas renderer + shared rich-media viewer | `MediaCanvas`, `RichMediaPanel`, `useCommandMenuRichMediaInventory`, `readVideoSequenceTimelineModelFromMarkdown`, `getCanvas2dRendererMenuLabel` | Active document media, playable video-sequence sources, Canvas View Mode menu click | `2D Renderer: Media` menu entry, media canvas source grid, direct video/audio/image/iframe previews | Media renderer previews playback sources only; Timeline remains editor, and Gantt flow diagrams stay in BottomPanel/FloatingPanel diagram surfaces. | Add export/render pipeline only after a separate runtime contract; do not deploy to Prod/Cloudflare without operator instruction. | 2026-06-22 |

## 2026-06-23

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| HTML video renderer | Make coding-agent HTML-to-MP4 jobs runtime-selectable without adding deploy artifacts | Keep Render_Spec validation, engine resolution, Flow node, MCP descriptor, and MP4 manifests source-owned in Dev. | `agentic-graph/{canvas/src/features/html-video-renderer,canvas/src/features/chat/richMediaRun.ts,canvas/src/lib/config.storyboard-widget.ts,mcp/local-tool-contract.js,mcp/server.js}` | HTML Video Renderer pipeline | `validateRenderSpec`, `resolveHtmlVideoEngine`, `runHtmlVideoRenderJob`, `runHtmlVideoFlowNode`, `buildAgenticGraphLocalMcpToolDefinitions` | HTML, CSS, data JSON, duration, fps, dimensions, engine hint or `AGENTIC_OS_HTML_VIDEO_ENGINE` | Structured errors, render job semantic key, video/mp4 artifact metadata, vdeoxpln route, Flow widget registry entry | Engines are caller/runtime registered; no hardcoded fallback renderer exists, and artifacts flow through the shared rich-media writer once. | Register a real FOSS adapter module next, then smoke an actual MP4 write in Dev before any Prod/Cloudflare release. | 2026-06-23 |

## 2026-06-24

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Media renderer + Timeline docs/schema sync | Keep 2D Renderer: Media, BottomPanel/FloatingPanel Timeline, media-canvas video sync, and source-backed cut/splice/mask/grade ownership aligned across canonical docs and AgenticRAG mirrors | Update canonical docs first: Media previews only, Timeline edits source-backed Mermaid Gantt, shared transport/metadata owners stay upstream, and local `@fs` video imports must not persist stale blob URLs or probe-abort noise. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Media renderer + Timeline panel split + schema mirror | doc updates, schema note sync | Recent Dev-only media/timeline transport refactors, local absolute video URL import fixes, and video-sequence editing contracts | Updated canonical docs, updated schema guidance, one ledger row preserving Media/Timeline SSOT and import-stability rules | Source-owned docs remain upstream truth; schema mirrors describe validated ownership only, and generated registry surfaces stay derived rather than hand-edited. | Add a focused doc note or companion only if future video export/runtime contracts exceed current canonical sections; keep Dev-only until explicit Prod/Cloudflare instruction. | 2026-06-24 |
| Edited-media export/runtime boundary docs sync | Keep Media preview, Timeline editing, and edited-media export on one source/metadata boundary without reintroducing probe-abort or blob-revoke churn | Document export as a derived surface: it must reuse the compiled sequence plan, shared source/metadata helpers, neutral browser media APIs, and delayed blob download revocation; it must not introduce alternate runtime source registries, fixture paths, or success-path probe cleanup. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence preview/edit/export contract | doc updates, schema note sync | Recent export-path probe fix, shared `downloadBlob()` revoke-delay fix, compiled export plan ownership | Updated canonical docs and schema guidance preserving one preview/edit/export SSOT for source timing and browser-media export behavior | Export stays downstream of the same source-backed plan used by preview/edit; docs describe validated runtime behavior only and must not imply a parallel export pipeline. | Add a focused companion only if future native/FOSS export backends diverge from the current browser `MediaRecorder` path; keep Dev-only until explicit Prod/Cloudflare instruction. | 2026-06-24 |
| Edited-media export cleanup contract docs sync | Keep edited-media export teardown deterministic so recorder failures cannot skip track, Web Audio, or video cleanup | Document the shared export finalizer: even if recorder stop/error rejects, export must still stop stream tracks, disconnect the media source, close `AudioContext`, and clear the video element before returning or failing. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export shutdown/finalizer contract | doc updates, schema note sync | Recent export cleanup finalizer and focused shutdown regression coverage | Updated canonical docs and schema guidance preserving deterministic browser-runtime teardown for edited-media export | Cleanup order is a runtime contract, not an implementation detail; docs should make it explicit anywhere export ownership is described so future changes do not regress release sequencing. | Add a focused companion only if future export backends introduce backend-specific shutdown rules beyond the current browser `MediaRecorder` path. | 2026-06-24 |
| Edited-media export progress/cancellation docs sync | Keep long edited-media exports observable and cancelable without fragmenting export ownership across panels | Document shared export progress/cancel semantics: staged preparing/rendering/finalizing progress labels, shared abort detection, active-button cancel behavior, and guaranteed cleanup on cancellation all belong to the shared export helper rather than panel-local implementations. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export progress/abort contract | doc updates, schema note sync | Recent shared export progress helper, `AbortSignal` threading, cancelable BottomPanel export controls, and focused abort/progress regression coverage | Updated canonical docs and schema guidance preserving one SSOT for export progress labels, cancellation, and abort-safe cleanup | Progress/cancel behavior is part of the runtime contract, not UI sugar; docs should describe it wherever export ownership is defined so future surfaces reuse the same shared boundary. | Add a focused companion only if future native/FOSS export backends require backend-specific progress or cancellation semantics beyond the current browser path. | 2026-06-24 |
| Edited-media export structured progress docs sync | Keep export progress reusable across surfaces without label parsing or panel-local recomputation | Document the structured progress payload: shared export progress events must expose `kind`, `phase`, `percentage`, `completedSegments`, and `totalSegments`, while rendering labels remain derived presentation over that upstream data. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export structured progress contract | doc updates, schema note sync | Recent shared progress payload upgrade and focused segment-count regression coverage | Updated canonical docs and schema guidance preserving one SSOT for field-level export progress data across preview/edit/export surfaces | Structured progress data is a runtime contract, not a UI-only label; docs should make the explicit fields discoverable so future surfaces/logging reuse the same payload instead of parsing text. | Add a focused companion only if future backends require richer progress payloads beyond the current segment-count contract. | 2026-06-24 |
| Edited-media export degenerate-plan docs sync | Keep empty or non-renderable export plans from drifting between BottomPanel UI and shared runtime export behavior | Document shared export-plan validation: export must reject plans with no segments or no positive-duration source ranges before recorder/runtime work starts, and active export controls must reuse that same upstream validator instead of inventing panel-local heuristics. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export degenerate-plan contract | doc updates, schema note sync | Recent shared export-plan validator and focused degenerate-plan regression coverage | Updated canonical docs and schema guidance preserving one SSOT for empty/non-renderable export rejection across preview/edit/export surfaces | Plan validity is a runtime contract, not just button-state polish; docs should make the shared validator discoverable so future surfaces fail early and consistently instead of drifting from the export helper. | Add a focused companion only if future export backends require backend-specific plan validation beyond the current empty/non-renderable contract. | 2026-06-24 |
| Edited-media export error-taxonomy docs sync | Keep abort, capability, plan, source, and runtime export failures upstream-defined instead of drifting into panel-local toast copy | Document the shared export error taxonomy: edited-media export must expose one SSOT for failure codes/messages and shared feedback mapping so cancellation stays neutral, known export failures reuse canonical messages, and surfaces avoid trimming or inventing local fallback copy. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export error feedback contract | doc updates, schema note sync | Recent shared export error-code/message helpers, focused error-feedback regression coverage, and action-hook adoption | Updated canonical docs and schema guidance preserving one SSOT for edited-media export failure classification and surface feedback | Export failure messaging is part of the runtime contract, not panel polish; docs should make the shared error taxonomy discoverable so future surfaces/backends reuse the same classification boundary. | Add a focused companion only if future native/FOSS export backends need backend-specific error codes beyond the current browser-runtime taxonomy. | 2026-06-24 |
| Edited-media export outcome/result docs sync | Keep success, cancel, and failure export feedback on one upstream result boundary instead of mixing structured failures with panel-local success toasts | Document the shared export outcome contract: edited-media export should expose a structured downloaded/cancelled/failed outcome with canonical message, toast kind, filename, and error-code fields so surfaces/logging can consume one SSOT for post-export feedback. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export outcome/result contract | doc updates, schema note sync | Recent shared export outcome helper, structured download result, focused outcome regression coverage, and action-hook adoption | Updated canonical docs and schema guidance preserving one SSOT for downloaded/cancelled/failed export outcomes across preview/edit/export surfaces | Post-export feedback is a runtime contract, not toast-local presentation glue; docs should make the shared outcome shape discoverable so future surfaces and telemetry hooks reuse the same boundary. | Add a focused companion only if future export backends require richer structured outcome fields beyond the current downloaded/cancelled/failed contract. | 2026-06-24 |
| Edited-media export event-envelope docs sync | Keep in-flight progress and terminal result on one upstream event stream instead of split callback shapes or surface-local telemetry wrappers | Document the shared export event envelope: edited-media export should emit a single structured event stream that carries progress events and outcome events with canonical message, kind, percentage/phase or status/toast-kind fields so surfaces, logs, and history can subscribe once without toast-coupled event shaping. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export event-envelope contract | doc updates, schema note sync | Recent shared export event helper, unified `onEvent` callback adoption, and focused event-envelope regression coverage | Updated canonical docs and schema guidance preserving one SSOT for edited-media export progress/outcome telemetry | Export telemetry is a runtime contract, not panel glue; docs should make the single event envelope discoverable so future surfaces and logging pipelines do not fork progress and terminal event shapes. | Add a focused companion only if future export backends require richer event metadata beyond the current progress/outcome envelope. | 2026-06-24 |
| Edited-media export session-history docs sync | Keep each export run discoverable as one bounded shared snapshot instead of transient toast-only state or surface-local bookkeeping | Document the shared export session/history contract: each edited-media export run should materialize one structured session record keyed by run id, filename base, kind, timing, progress, and terminal outcome fields, and surfaces should keep a bounded upserted history fed by the shared event envelope. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export session/history contract | doc updates, schema note sync | Recent shared export session reducer/history helpers, hook-level bounded session history adoption, and focused session-regression coverage | Updated canonical docs and schema guidance preserving one SSOT for export run snapshots across preview/edit/export surfaces | Export run history is a runtime contract, not a later UI convenience; docs should make the bounded session snapshot model discoverable so future history/retry/audit surfaces reuse the same record shape. | Add a focused companion only if future export backends require persisted cross-session history beyond the current in-memory bounded run snapshots. | 2026-06-24 |
| Edited-media export retry-replay docs sync | Keep retry behavior stale-plan-safe and upstream-defined instead of replaying from panel-local state or toast text | Document the shared export retry/replay contract: failed or cancelled export sessions should resolve a retry request only when a matching current compiled plan is available, no other export is active, and the retry preserves prior run linkage via shared run ids rather than local UI heuristics. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export retry/replay contract | doc updates, schema note sync | Recent shared retry-request helpers, hook-level retry entrypoint, and focused stale-plan retry regression coverage | Updated canonical docs and schema guidance preserving one SSOT for edited-media export replay eligibility and request shaping | Export replay is a runtime contract, not a button-only concern; docs should make the stale-plan-safe retry boundary discoverable so future retry/history surfaces reuse the same validation and run-linkage model. | Add a focused companion only if future export backends require persisted replay payloads beyond the current in-memory plan/session retry contract. | 2026-06-24 |
| Edited-media export retry-control docs sync | Keep BottomPanel retry UI neutral and shared instead of deriving button copy/state locally from transient sessions | Document the shared export retry-control contract: BottomPanel should surface one retry control from the latest retryable export session through shared control helpers, preserving generic disabled copy when unavailable and session-kind-specific retry copy only when a stale-plan-safe retry request exists. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export retry-control contract | doc updates, schema note sync | Recent shared retry-control helper, BottomPanel retry button surfacing, and focused retry-control regression coverage | Updated canonical docs and schema guidance preserving one SSOT for retry control copy/state across export surfaces | Retry UI is a runtime contract boundary, not panel polish; docs should make the shared retry-control helper discoverable so future surfaces do not fork retry button heuristics. | Add a focused companion only if future surfaces require richer retry menus beyond the current single latest-session retry control. | 2026-06-24 |
| Edited-media export session-surface docs sync | Keep recent export run rendering neutral and shared instead of formatting status/history rows inside BottomPanel | Document the shared export session-surface contract: BottomPanel should render recent export runs from one upstream surface model derived from bounded session history, preserving shared detail labels, empty-state copy, and per-run retry affordances instead of panel-local status formatting. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export session-surface contract | doc updates, schema note sync | Recent shared session-surface helper, BottomPanel recent-export strip surfacing, and focused session-surface regression coverage | Updated canonical docs and schema guidance preserving one SSOT for recent export run rendering across BottomPanel and future surfaces | Export history rendering is a runtime contract boundary, not panel polish; docs should make the shared session-surface helper discoverable so future surfaces do not fork per-status copy or retry-row formatting. | Add a focused companion only if future surfaces require richer paging/filtering beyond the current small recent-run strip. | 2026-06-24 |
| Edited-media export session-tone docs sync | Keep recent export row styling semantic and shared instead of mapping success/cancel/failure/running states to panel-local classes | Document the shared export session tone/style contract: recent export rows should derive tone and style mode from one upstream helper so running, downloaded, cancelled, and failed runs expose stable semantic styling data for BottomPanel and future surfaces. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export session-tone contract | doc updates, schema note sync | Recent shared session tone-style helper, BottomPanel semantic data-attribute surfacing, and focused tone-style regression coverage | Updated canonical docs and schema guidance preserving one SSOT for semantic export-row styling across recent-run surfaces | Export row styling is a runtime contract boundary, not CSS-only polish; docs should make the shared tone/style helper discoverable so future surfaces do not fork status-to-style mappings. | Add a focused companion only if future surfaces require richer theming tokens beyond the current shared tone/mode contract. | 2026-06-24 |
| Edited-media export session-selection docs sync | Keep recent export ordering/filtering explicit and shared instead of relying on incoming array order or panel-local status rules | Document the shared export session selection contract: recent export surfaces should choose visible runs through one upstream ordering/filter helper that can filter statuses, prioritize the latest retryable run, keep active runs visible, and then fall back to recency. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}},huijoohwee.github.io/schema/AgenticRAG/{README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Video-sequence export session-selection contract | doc updates, schema note sync | Recent shared session selection helper, focused ordering/filter regression coverage, and BottomPanel surface-model reuse | Updated canonical docs and schema guidance preserving one SSOT for recent export ordering/filtering across BottomPanel and future surfaces | Export session selection is a runtime contract boundary, not panel data plumbing; docs should make the shared selection helper discoverable so future surfaces do not fork retry-priority or status-filter behavior. | Add a focused companion only if future surfaces require user-configurable sorting/filter presets beyond the current shared priority order. | 2026-06-24 |

## 2026-06-25

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Native media/timeline animation surfaces | Keep Media Canvas, BottomPanel Timeline, FloatingPanel Timeline, and FloatingPanel Media on one motion contract | Route media/timeline motion through `timelineAnimationEngine.ts`; expose semantic animation attributes and CSS/object/SVG frame data without importing anime.js or creating panel-local animation owners. | `agentic-graph/{canvas/src/components/timeline,todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md}}` | Media timeline animation engine | `buildTimelineAnimationState`, `easeTimelineAnimationProgress` | Playback progress, active source count, panel surface, target taxonomy | Native `data-kg-animation-*` attributes, CSS custom properties, object frames, SVG frames | Anime.js informs target coverage only; runtime state stays native, shared, deterministic, and surface-neutral across media/timeline panels. | Mirror AgenticRAG schema notes only when the next docs-map sync is approved; keep Dev-only until explicit Prod/Cloudflare instruction. | 2026-06-25 |

## 2026-06-26

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Timeline preview animation/runtime release + docs/schema sync | Record the released preview-sync/animation contract and the refreshed settings-flow provenance mirrors after Dev -> Prod -> Cloudflare | Keep preview sync, native animation timing, release evidence, and settings-flow artifacts source-owned in canonical docs first; mirror only validated boundaries into AgenticRAG and treat generated publish metadata as derived release output. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-2d-renderer-enhancement-design.md,agentic-graph-renderer-document.md,agentic-graph-yaml-mermaid-gitgraph-frontmatter-prd-tad.md,agentic-graph-cross-repo-publish-topology.md,agentic-graph-settings-document-schema-and-specs.md}},huijoohwee.github.io/schema/AgenticRAG/{README.md,README.pipeline-and-renderer-guidelines.md,panels.jsonld,settings-flow.jsonld}` | Timeline preview/runtime + release topology + settings-flow provenance | doc updates, schema note sync, release record | Released timeline preview animation/runtime commits, Cloudflare deploy proof, and refreshed settings-flow artifacts | Updated canonical docs, updated schema mirrors, and one ledger row preserving preview-sync/runtime, release, and settings-flow SSOT | Canonical authored docs remain upstream truth; publish mirrors and AgenticRAG notes describe validated release boundaries only, and generated settings-flow/public-route metadata must not be hand-patched downstream. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future renderer/panel or settings-flow doc changes and keep the next release record anchored by paired repo commits plus preview/live route proof. | 2026-06-26 |
| Rich-media browser smoke docs/schema sync | Record the Dev-only rich-media browser smoke harness, coverage, and current verification boundary across canonical docs and AgenticRAG mirrors | Keep rich-media browser verification source-owned: deterministic smoke route + browser script validate text/iframe/overlay/direct-media and storyboard-widget chrome visibility, while drag/resize callback semantics stay in focused component regressions. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-renderer-document.md,agentic-graph-testing-document.md}},huijoohwee.github.io/schema/AgenticRAG/{README.md,README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | RichMedia browser smoke harness | `RichMediaBrowserSmokePage`, `run_rich_media_browser_smoke.mjs`, `verify_rich_media_browser_smoke.py` | Dev-only smoke route, shared `RichMediaPanel` surfaces, browser assertions, focused component regressions | Passing browser smoke, screenshot artifact, updated docs/schema notes | Browser smoke proves deterministic runtime surfaces only; storyboard-widget drag/resize counters stay covered by focused source-level regressions because browser callback instrumentation is still flaky. | Re-run `npm run test:smoke:rich-media:browser` after future `RichMediaPanel` or media-shell changes and widen storyboard-widget browser assertions only when a deterministic drag harness exists. | 2026-06-26 |
| Storyboard rich-media drag smoke docs/schema sync | Record the Dev-only storyboard drag-to-panel smoke harness, command, and verification boundary across canonical docs and AgenticRAG mirrors | Keep storyboard drag verification source-owned: deterministic browser smoke mounts the real `2D Renderer: Storyboard` route, drags one image and one video through the shared media payload contract, and proves new `Rich Media Panel` nodes do not mutate existing storyboard/widget world positions. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-renderer-document.md,agentic-graph-testing-document.md},canvas/src/{__tests__/storyboardRichMediaDropBrowserSmokeContract.test.ts,tests/registry/postParserCases3.ts}},huijoohwee.github.io/schema/AgenticRAG/{README.md,README.pipeline-and-renderer-guidelines.md,panels.jsonld}` | Storyboard drag smoke harness + source contract registry | `StoryboardRichMediaDropSmokePage`, `run_storyboard_rich_media_drop_browser_smoke.mjs`, `verify_storyboard_rich_media_drop_browser_smoke.py`, `testStoryboardRichMediaDropBrowserSmokeContract` | Dev-only storyboard smoke route, shared media drag payload, browser assertions, source contract registry | Passing storyboard browser smoke, screenshot artifact, updated docs/schema notes, and tracked registry contract | The browser smoke proves the real storyboard drag path only; generic storyboard-widget drag/resize callback counters outside this path still belong to focused component regressions rather than broader browser instrumentation. | Re-run `npm run test:smoke:storyboard-rich-media-drop:browser` after future storyboard drag, rich-media drop, or overlay placement changes and widen browser assertions only when additional drag paths become deterministic. | 2026-06-28 |
| Storyboard readiness command sync | Bundle storyboard source smoke, browser drop/no-shift proof, and publish drift into one operator-facing local readiness command before any future Dev -> Prod -> Cloudflare approval | Keep release gating upstream: the canonical publish-topology checklist and AgenticRAG pipeline guidance should point to `npm run storyboard:readiness:check`, which runs `npm run test:smoke:storyboard-rich-media-drop:source`, the storyboard browser smoke, and `pages:check-sync` whenever storyboard drag, rich-media drop, or overlay/layout placement changes land. | `agentic-graph/{package.json,scripts/check-storyboard-readiness.mjs,todo-log.md,docs/documents/{agentic-graph-cross-repo-publish-topology.md,agentic-graph-testing-document.md,agentic-graph-renderer-document.md},canvas/{package.json,src/tests/subsetStoryboardRichMediaDropSmoke.ts}},huijoohwee.github.io/schema/AgenticRAG/README.pipeline-and-renderer-guidelines.md` | Local readiness command + source smoke bundle + release checklist + pipeline mirror | `storyboard:readiness:check`, `test:smoke:storyboard-rich-media-drop:source`, validation-command table update, pipeline release-gate note | Storyboard drag smoke harness, source-owned seam regression bundle, publish sync drift gate, future Pages deploy approval | One local readiness command, one reusable source smoke command, updated canonical checklist, and mirrored pipeline guidance | Release gating belongs in one operator entrypoint backed by one canonical source-smoke owner, not in downstream publish artifacts or duplicated inline eval wiring. | Re-run `npm run storyboard:readiness:check` before any future storyboard/rich-media placement release and keep `pages:deploy-cloudflare` blocked until it passes. | 2026-06-28 |
| Storyboard live-route media-panel verification docs sync | Document the canonical real app-route Rich Media panel retention and SSOT restoration proof across source docs and AgenticRAG mirrors | Keep live-route verification source-owned and explicitly separate from the Dev-only smoke route: operators should use `npm run test:live:storyboard-media-panel-retention:browser` when validating image/video panel stability plus markdown-reapply cleanup on the normal Canvas route. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-testing-document.md,agentic-graph-renderer-document.md,agentic-graph-cross-repo-publish-topology.md}},huijoohwee.github.io/schema/AgenticRAG/{README.md,README.pipeline-and-renderer-guidelines.md}` | Live-route verifier docs + checklist + pipeline mirror | `test:live:storyboard-media-panel-retention:browser`, live-route SSOT restoration note, readiness/pipeline doc update | Real-route workspace runtime command injection, image/video panel creation, edge retention, markdown reapply cleanup | Canonical docs and mirror guidance name the same live-route verifier and explain when to use it | Live-route SSOT restoration is a distinct verification boundary from the Dev-only smoke route and should stay documented as a source-owned operator command instead of ad hoc browser work. | Re-run `npm run test:live:storyboard-media-panel-retention:browser` after future storyboard Rich Media panel regressions, especially when validating transient edge/panel cleanup on markdown reapply. | 2026-06-28 |

## 2026-06-29

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| BottomPanel Timeline horizontal scroll | Make zoomed source-backed timelines physically scrollable | Move zoom/append width ownership to shared workspace layout so the scroll container gets real layout overflow instead of visual-only viewport flex expansion. | `agentic-graph/{canvas/src/components/timeline,canvas/src/__tests__,todo-log.md}` | Video sequence ruler workspace layout | `resolveVideoSequenceTimelineWorkspaceLayout`, `VideoSequenceTimelineRuler`, `testVideoSequenceTimelineEditorEnhancementContracts` | Zoomed source-backed video sequence timeline | `scrollWidth` can exceed `clientWidth` at zoom while viewport and append-space keep proportional spans | Shared workspace geometry owns scroll extent; viewport remains timeline content and append-space remains explicit editing room. | Re-run focused editor contract, TypeScript, hygiene, and live BottomPanel zoom-scroll proof after future ruler zoom changes. | 2026-07-04 |
| BottomPanel Timeline mixed-media time axis | Keep the ruler stable when image, audio, and video media are added | Derive ruler-axis duration from authored timeline minutes, not selected-source duration or summed media-source duration. | `agentic-graph/{canvas/src/features/gitgraph,canvas/src/__tests__,todo-log.md}` | Gantt timeline transport surface + preview sync contract | `useGanttTimelineTransportSurfaceModel`, `resolveVideoSequenceTimelineScaleMaxMinutes`, `testVideoSequenceTransportReadoutUsesPreviewSourceTime` | Mixed source-backed image/video/audio timeline with selected short media | Stable authored timeline ruler labels while selected-source readout remains source-aware | Ruler geometry is timeline-authoritative; source durations remain readout/playback context and must not mutate the global time axis. | Re-run focused preview-sync/media-drop tests, TypeScript, hygiene, and live BottomPanel ruler DOM proof after future mixed-media changes. | 2026-07-04 |
| BottomPanel Timeline placeholder media add | Prevent starter empty bars from duplicating when media is added | Treat Source video scaffold rows as consumable placeholders; replace them for video add, remove them for image/audio add, and hide stale scaffold rows when real video rows exist. | `agentic-graph/{canvas/src/lib/mermaid,canvas/src/components/timeline,canvas/src/__tests__,todo-log.md}` | Mermaid media-drop helper + video-sequence render projection | `appendMermaidGanttVideoSequenceMediaDrop`, `appendTaskToCode`, `appendSourceToFrontmatter`, `resolveRenderableVideoSequenceTimelineSpans` | Starter/source-backed Source video scaffold plus FloatingPanel Media Image, Audio, or Video add | One real typed media row and one real source entry; no lingering empty Source video clip | Media-drop source ownership consumes placeholder rows upstream; render projection suppresses stale scaffold rows instead of adding BottomPanel visual patches. | Re-run focused media-drop/render tests, TypeScript, hygiene, and live BottomPanel DOM proof after future add/import changes. | 2026-07-04 |
| BottomPanel Timeline lean append lanes | Keep source bars compact and video/image additions obvious | Render compact source media bars leaner; route image/scene clips through shared visual source-media bar styling; resolve source-aware thumbnails so image bars show one still and video bars fit the full strip; reserve one semantic append video display lane after occupied multi-video lanes. | `agentic-graph/{canvas/src/components/timeline,canvas/src/features/gitgraph,canvas/src/__tests__,todo-log.md}` | Video sequence display lane projection + compact media bar CSS + source thumbnail sets | `resolveVisibleVideoSequenceTimelineDisplayLanes`, `isCompactSourceMediaSpan`, `findSourceForSegment`, `useTimelineMediaReaderSummaries`, `VideoSequenceTimelineRuler`, `VideoSequenceTimelineDenseFbf.css` | Source-backed multi-video and image Mermaid Gantt sequence | `V1,V2,V3,Audio` display lanes with append metadata; image compact bars resolve one still; video bars distribute all sampled thumbnails inside 42px chrome | Ruler projection owns visual addition lanes and compact source classification; timeline plan sync resolves unmatched media rows by lane-compatible source kind; no repo fixture/backfill is added. | Re-run focused timeline scale/surface/editor contracts, TypeScript, browser DOM proof, and hygiene after future timeline lane/drop changes. | 2026-07-03 |
| BottomPanel Timeline UI declutter | Keep transport controls scan-friendly without losing editor commands | Show primary playback, sync, zoom, and context only when useful; move secondary edit, clip, fit/center, and utility actions into compact menus. | `agentic-graph/{canvas/src/components/timeline,canvas/src/features/gitgraph,canvas/src/__tests__,docs/documents,todo-log.md}` | Timeline transport chrome + context controls | `GanttTimelineTransportHeaderTools`, `GanttTimelineTransportContextControls`, `TimelineTransportControlsMermaidGantt.css` | Source-backed BottomPanel Timeline with imported video sequence | Compact `0:00 / total` row, no empty context copy, visible zoom label, grouped secondary tools | Primary state remains visible; secondary actions stay native and accessible through semantic menus rather than disabled toolbar clutter. | Re-run focused timeline editor/surface/responsive contracts, TypeScript, browser DOM proof, and hygiene after future BottomPanel chrome changes. | 2026-07-02 |
| BottomPanel Timeline tool chrome consolidation | Remove stale duplicate timeline tool chrome while preserving active editor affordances | Keep active Timeline tools in the Gantt transport header; share button/icon rendering through one neutral component and forbid ruler-local disabled tool strips. | `agentic-graph/{canvas/src/components/timeline,canvas/src/features/gitgraph,canvas/src/__tests__,docs/documents,todo-log.md}` | Timeline transport header + video-sequence tool button | `GanttTimelineTransportHeaderTools`, `TimelineVideoSequenceToolButton`, `TimelineVideoSequenceEmptyState` | Source-backed video sequence timeline header tools and empty-state ruler | One active tool strip, no disabled ruler duplicate, contract tests guarding ownership | Transport chrome owns commands; ruler owns scale, lanes, bars, ticks, and empty-state layout only. | Re-run focused timeline surface/editor contracts, responsive transport contract, TypeScript, docs sanity, hygiene, and browser DOM proof after future toolbar work. | 2026-07-02 |
| BottomPanel Timeline pinch zoom | Make trackpad pinch zoom-in/out responsive while preserving source-backed edits | Use bounded multi-step pinch deltas and native gesture anchors; keep zoom state upstream and editing math duration-based. | `agentic-graph/{canvas/src/components/timeline,canvas/src/features/gitgraph,canvas/src/__tests__,docs/documents,todo-log.md}` | Timeline transport zoom + Gantt transport view | `resolveTimelineTransportGestureZoomStepCount`, `resolveTimelineTransportNextZoomIndex`, `handleRulerWheelZoom`, `gesturechange` | Ctrl/meta wheel pinch, native gesture scale, ruler cursor anchor | Responsive zoom-in/out, deeper append workspace, stable cursor anchoring | Pinch modifies only shared zoom index and scroll anchor; clip positions, scrub, drag, and trim commits continue to use real duration rail. | Re-run timeline editor/surface tests, TypeScript, hygiene, and browser pinch/zoom proof on the BottomPanel Timeline. | 2026-07-02 |
| BottomPanel Timeline zoom workspace | Keep zoomed media editing readable and append-ready | Derive rounded scale-domain ticks, bar/lane scale parity, and right-side append workspace upstream; keep scrub, drag, and trim commits source-backed. | `agentic-graph/{canvas/src/components/timeline,canvas/src/__tests__,docs/documents,todo-log.md}` | Video sequence zoom projection + ruler rail | `resolveVideoSequenceTimelineScaleMaxMinutes`, `buildVideoSequenceTimelineZoomTicks`, `VideoSequenceTimelineRuler` | Source-backed video sequence duration, timeline zoom level, existing Gantt task spans | 52s media shows a 60s ruler at 100%, with proportional bars and zoomed append workspace | Visual scale can round upward for editing room; source-backed runtime duration still owns transport, scrub, drag, and trim commits. | Re-run timeline editor/surface tests, TypeScript, hygiene, and local BottomPanel browser zoom checks after future timeline zoom work. | 2026-07-02 |
| Video-sequence import timeline activation | Keep OpenCut-inspired import/editor behavior native and source-backed | Generate stacked video/audio sequence rows with source ranges only; open Timeline panels on activation; forbid default Mask/Grade backfill. | `agentic-graph/{canvas/src/features/markdown-workspace/workspaceImport,canvas/src/features/parsers,canvas/src/__tests__,docs/documents,todo-log.md}` | Video sequence import projection + frontmatter preset landing | `buildVideoSequenceTimelineImportMarkdown`, `applyCanvasFrontmatterPreset`, `testWorkspaceImportLocalMultipleVideosCreatesStackedVideoAudioSequenceDocument` | Operator-supplied local video files or direct video URLs | One visible Media sequence document, stacked video/audio rows, `kgsrc_*` ranges, BottomPanel/FloatingPanel Timeline landing | Import owns source/media projection only; timeline operation lanes belong to explicit editor tools and stale `storyboardWidget` panel state must not survive video-sequence activation. | Re-run local/URL video import tests, timeline surfaces/editor contracts, TypeScript, hygiene, and browser Launch import smoke before future media timeline work. | 2026-07-02 |
| Storyboard Card/Widget renderer consolidation | Keep Storyboard as the sole 2D renderer while preserving shared widget runtime behavior | Route Card/Widget through Display Controls; remove the retired renderer id, presets, fixtures, and aliases; keep shared widget utilities headless. | `agentic-graph/{canvas/src/components/toolbar,canvas/src/lib/{config.render.ts,canvas/canvasStoryboardDisplayControls.ts},docs/documents,todo-log.md}` | Canvas View Mode display controls | `getCanvasViewRendererOptions`, `applyCanvasViewSelection`, `testStoryboardCardWidgetDisplayControlsUseSingleStoryboardRenderer` | Toolbar Display Controls, persisted Storyboard display state, imported renderer presets | Card and Widget both retain `canvas2dRenderer=storyboard`; only presentation changes | Card and Widget differ only by UI; Storyboard owns renderer identity while shared Storyboard Widget modules remain implementation utilities rather than a renderer contract. | Re-run focused display-control/import tests, TypeScript, Storyboard source smoke, browser menu proof, `git diff --check`, and hygiene after future renderer changes. | 2026-07-02 |
| Shared 2D pointer-drag performance | Smooth pan, drag, zoom-adjacent overlay movement, and Rich Media resize across 2D renderers | Coalesce shared pointer move handlers on animation frames and flush the latest move before end/cancel; forbid renderer-local drag throttles. | `agentic-graph/{grph-shared/src/dom/pointerDrag.ts,canvas/src/__tests__,canvas/src/tests/runners,todo-log.md}` | Shared pointer drag owner | `startPointerDrag`, `testPointerDragCoalescesMoveHandlersOnAnimationFrames` | Pointer move streams from Storyboard Card/Widget, Rich Media panels, floating panels, resize handles | Frame-batched move delivery with no stuck pointer-drag body state | Drag smoothness belongs in `grph-shared/dom/pointerDrag`; renderers and panels consume the same helper without aliases, hardcoded fixtures, or compatibility backfill. | Re-run pointer-drag regressions, Rich Media interaction source-contracts, TypeScript, `npm run hygiene:check`, and local Storyboard Card/Widget browser drag checks after future interaction changes. | 2026-07-01 |
| Spatial capture fileset import | Ingest, parse, and render local XR capture bundles without fixture hardcodes | Detect complete `.ply`/`.spz`/panorama/collider-GLB folder filesets generically, create one XR model manifest, keep side members as source units, and forbid duplicate workspace artifacts. | `agentic-graph/{canvas/src/features/markdown-workspace/workspaceImport,canvas/src/features/workspace-fs,canvas/src/__tests__,docs/documents,todo-log.md}` | Local folder import + source mirror format owners | `resolveSpatialCaptureFilesets`, `buildSpatialCaptureFilesetManifestMarkdown`, `importWorkspaceLocalFolder`, `shouldEncodeWorkspaceSourceMirrorAsBase64`, `testWorkspaceImportLocalFolderSpatialCaptureFilesetCreatesRenderableXrManifest` | Operator-supplied spatial capture fileset folder with point cloud, gaussian splat, panorama, and collider | Manifest-first XR workspace import plus pending collider GLB payload and source-unit coverage | Fileset identity is inferred from role/extension semantics, not directory or basename literals; renderer activation reuses the GLB asset manifest path while side members remain external validation inputs. | Re-run the spatial capture focused import test, source mirror guard, hardcode scan with external runtime inputs, TypeScript, and hygiene after future folder-import or XR manifest changes. | 2026-06-30 |
| XR Mode panel surfaces | Make Surface Mode XR available from BottomPanel and FloatingPanel without copied external runtime | Add one shared semantic `XrPanelView` and `xrPanelModel`, route BottomPanel `XR` and FloatingPanel `XR` through existing shell state, activate native `canvas3dMode: "xr"`, surface WebGL/WebGPU/WebXR/glTF/PLY readiness plus shared physics controls, and expose manifest import/render cache identity. | `agentic-graph/{canvas/src/{features/three,features/strybldr,lib/toolbar,hooks/store},canvas/src/__tests__,docs/documents,todo-log.md}` | XR panel projection + panel routing + native graphics stack model + hardcode guard | `XrPanelView`, `resolveXrPanelSourceProfile`, `resolveXrPanelRuntimeStack`, `SpatialCaptureManifestStage`, `plyPointCloudWorker`, `parseStandaloneSpatialCaptureManifest`, `setCanvas3dMode`, `setBottomSurfaceTab`, `setFloatingPanelView`, `isStandaloneSpatialCaptureManifestText`, `resolveLaunchDropdownVisibleExportItems`, `testXrModeMountsSharedBottomAndFloatingPanels`, `testWorkspaceImportXrStandalonePlyLocalUsesSourceManifestCache`, `testWorkspaceImportXrStandalonePlyUrlUsesCachedManifestWithoutPayloadFetch`, `testWorkspaceImportXrStandalonePlyDoesNotAdvertiseGltfGlbExports`, `testWorkspaceImportXrStandalonePlyUsesSpatialRendererInsteadOfGraphFallback`, `testWorkspaceImportXrStandalonePlyRuntimeUsesTransferBackedWorkerParser`, `testForbidHardcodedRuntimeValidationInputInRepo` | Toolbar Surface Mode XR, BottomPanel XR tab, FloatingPanel XR view, Import URL / Import local files with operator-supplied `.ply` or `.spz` validation asset | Shared Bottom/Floating XR status and controls with semantic markers, PLY source profile, native graphics-stack readiness, manifest-first standalone PLY/SPZ import, source-only spatial renderer stage, transferable worker-backed PLY parsing, camera-direction Gaussian resorting, Sphere/Box/Orbit HUD state, no legacy GLTF/GLB export actions for source-only captures, and no committed runtime asset path | XR panel chrome is a projection of existing XR state, not a renderer fork; Import URL/local files write source-owned manifests with cache keys instead of payload bytes; standalone spatial manifests render through their own stage instead of graph/model fallback; PLY payloads parse off-thread when browser workers are available; covariance splats resort only on meaningful camera direction changes; SuperSplat/PlayCanvas remain inspiration-only references and validation assets remain external runtime inputs. | Re-run `workspace.import.xrSpatialCapture.*`, `canvas.xrMode.panelSurfaces`, native XR policy/physics tests, worker-parser transfer guard, external `.ply`/`.spz` hardcode guard, TypeScript, browser screenshot/mobile HUD check, `git diff --check`, and hygiene after future XR panel or import work. | 2026-07-05 |
| XR Mode native session policy | Keep Surface Mode XR immersive and interactive while provider-neutral | Prefer native `immersive-ar` over `immersive-vr`, share the WebXR session-init policy, render bounded native roll/thrust/velocity/collision/camera-anchor cues from shared controls, and scan runtime validation `.md`/`.gltf`/`.glb`/`.ply`/`.spz` assets through the hardcode guard. | `agentic-graph/{canvas/src/{lib/three,features/three},canvas/src/__tests__,canvas/src/tests/runners/runSchemaTests.ts,docs/documents/agentic-graph-renderer-document.md,todo-log.md}` | XR session policy + physics playground model/control owners + hardcode guard | `chooseXrSessionMode`, `buildXrSessionInit`, `resolveXrPhysicsPlaygroundState`, `readXrPhysicsPlaygroundControls`, `setXrPhysicsPlaygroundMode`, `installXrPhysicsKeyboardControls`, `CanvasXrEntryPanel`, `XrGraphStage`, `testForbidHardcodedRuntimeValidationInputInRepo` | Operator-supplied XR model/demo validation assets, native browser WebXR support, elapsed render time, Roll/Thrust panel buttons, keyboard movement/thrust/stabilize input | AR-first support check, AR/VR switch when both modes are supported, provider-neutral session init, bounded native roll/thrust states, velocity/collision/camera-anchor stage markers, shared controls, `.glb` hardcode scan coverage | External XR references are inspiration only; runtime stays native, dependency-free, copied-API-free, physics-engine-free, and hardcode-free. | Re-run `canvas.xrMode.nativeSessionPolicy`, `canvas.xrMode.physicsPlaygroundModel`, `policy.boundary.forbidHardcodedRuntimeValidationInput`, focused ESLint, and `npm run hygiene:check` after future XR surface changes. | 2026-06-30 |
| Visual annotation renderer reuse | Keep Storyboard and Media on one native dataset E2E contract without copying external CV runtimes | Keep renderer reuse in `VISUAL_ANNOTATION_E2E_CANVAS_2D_RENDERERS`; validate shared load/split/merge/save/convert, track ids, masks, zone filtering, zone counts, and compact media timeline routing through focused video-agent contracts. | `agentic-graph/{canvas/src/lib/config.render.ts,canvas/src/features/visual-annotation-engine/annotationDataset.ts,canvas/src/features/video-agent/videoAgentPipeline.ts,canvas/src/__tests__/{videoAgentValidationInput.test.ts,visualAnnotationEngine.test.ts,videoAgentPipeline.test.ts,videoAgentTimelineScale.test.ts},docs/documents/{agentic-graph-renderer-document.md,agentic-graph-vdeoxpln-prd-tad.md},todo-log.md}` | Renderer config + visual annotation dataset runtime + video-agent media timeline bridge | `isVisualAnnotationE2eCanvas2dRenderer`, `buildVideoAgentDatasetRuntime`, `buildVideoAgentPipeline`, `loadVisualAnnotationDataset`, `splitVisualAnnotationDataset`, `mergeVisualAnnotationDatasets`, `saveVisualAnnotationDataset`, `convertVisualAnnotationDataset`, `filterVisualAnnotationDatasetByZones`, `countVisualDatasetZones` | Operator-supplied validation docs/URLs, frame boxes, masks, track ids, annotation datasets | Shared renderer contract, dataset artifacts, deterministic splits, merged dataset, saved/converted JSON, polygon-zone filters, frame-ordered zone counts, compact VIDEO/FBF/AUDIO timeline tracks with semantic FBF samples | Reference Roboflow/Supervision-style E2E only as inspiration; runtime stays native, dependency-free, provider-neutral, hardcode-free, keeps source-frame imagery in Rich Media frame analysis, and keeps video-agent workflow rows in Flowchart panels. | Re-run `videoAgent.validationConfig.userConfiguredImportUrls`, `videoAgent.importUrl.materializesCompleteParsedGraph`, `videoAgent.timeline.processFlowchartMediaTimelineSplit`, `visualAnnotationEngine`, `videoAgentPipeline`, browser Launch -> Import URL, and `npm run hygiene:check` after future renderer/dataset changes. | 2026-06-30 |
| Video-agent URL transcript enrichment | Make Launch -> Import URL materialize video, frame-by-frame, and transcript artifacts without repo-hardcoded validation URLs | Preserve YouTube transcript JSON through `WorkspaceUrlContent`, align transcript segments to frame boxes, and expose `sourceTranscript` plus `frameByFrameTranscript` on Storyboard Widget ports. | `agentic-graph/{canvas/src/features/markdown-workspace/workspaceImport,canvas/src/__tests__/videoAgentValidationInput.test.ts,todo-log.md}` | Video-agent URL import materializer | `fetchWorkspaceUrlContent`, `materializeVideoAgentUrlImportDocument`, `buildVideoAgentUrlImportMarkdown` | Operator-supplied YouTube URL, transcript markdown/JSON, generated frame boxes | `.video-agent.md` graph with HtmlVideoRenderer, RichMediaPanel routes, dataset artifacts, source transcript, frame-aligned transcript rows | Transcript structure belongs in the shared Import URL materializer; validation URLs remain runtime inputs and never committed fixtures. | Re-run `videoAgent.importUrl.completeParsedGraph`, `videoAgent.validationConfig.userConfiguredImportUrls`, TypeScript, hygiene, and browser Launch -> Import URL smoke after future importer changes. | 2026-06-30 |
| Video-agent configurable validation imports | Let operators configure external validation documents and URL sets without repo hardcodes | Keep validation doc path and import URL set in `videoAgentValidationConfig`; Launch -> Import URL may prefill or import the configured set through the existing import bridge. | `agentic-graph/{canvas/src/features/video-agent,canvas/src/lib/toolbar,canvas/src/__tests__,docs/documents/agentic-graph-api-document.md,todo-log.md}` | Video-agent validation config + Launch Import URL | `readVideoAgentValidationConfig`, `writeVideoAgentValidationConfig`, `LaunchDropdownImportUrlItem` | Operator-configured validation doc path and URL list | Persisted browser/runtime config plus shared Import URL ingestion for each configured video source | Validation sources are runtime inputs; repo source owns only parsing, storage-key, env-key, and Import URL bridge contracts. | Re-run `videoAgent.validationConfig.userConfiguredImportUrls`, `videoAgent.pipeline.externalValidationInputsNoRepoHardcodes`, browser Launch Import URL config smoke, and `npm run hygiene:check`. | 2026-06-29 |
| Video-agent Director inspiration boundary | Keep reference-project inspiration neutral without copying code or adding external runtime dependencies | Preserve `VIDEO_AGENT_REFERENCE_BOUNDARY` as the SSOT for native inspiration-only orchestration and validate external URL sets only through Import URL. | `agentic-graph/{canvas/src/features/video-agent,canvas/src/__tests__,docs/documents/agentic-graph-api-document.md,todo-log.md}` | Video-agent reference boundary | `buildVideoAgentPipeline`, `importWorkspaceUrl` | External validation URL set and sibling vdeoxpln demo contract | Native pipeline/render data exposes boundary policy with no external dependency | The external demo doc may name the inspiration source; repo runtime must stay provider-neutral and dependency-free. | Re-run `videoAgent.pipeline.externalValidationInputsNoRepoHardcodes`, `htmlVideoRenderer.validationInput.vdeoxplnAnimatedMp4`, and `npm run hygiene:check`. | 2026-06-29 |
| Video-agent frame analysis Rich Media sync | Record the Dev-only fix that keeps multiple Rich Media panels aligned with the shared frame clock | Project frame images and normalized boxes from shared media-spec resolution; preserve decimal `__video_frame` times, write frame images under sibling `/image/video-frame/YYYYMMDDTHHmmssZ`, and avoid authored-doc runtime backfill. | `agentic-graph/{canvas/src/features/video-agent,canvas/src/lib/canvas/graph-elements/mediaSpec.ts,canvas/src/lib/rich-media/server/videoFrameServer.ts,grph-shared/src/rich-media/providers.ts,todo-log.md}` | Rich Media frame analysis projection + remote frame provider | `projectVideoAgentFrameAnalysisSrcDoc`, `computeNodeMediaSpec`, `buildRemoteVideoFrameRequestUrl`, `buildRemoteVideoFrameDefaultCacheRoot` | Runtime `frameBoundingBoxes`, authored srcdoc thumbnail shell, external validation URL set, BottomPanel FBF selection | Five timeline-synchronized frame images plus matching boxes in Rendered MP4, Video Agent Frame Analysis, and FloatingPanel Media panels | Rich Media panels should consume one upstream projected srcdoc and one shared frame URL helper; do not patch each panel, floor subsecond frame times, move source-frame thumbnails into Timeline transport rows, or use project-nested frame cache roots. | Re-run `videoAgent.pipeline.externalValidationInputsNoRepoHardcodes`, `videoAgent.pipeline.providerFrameImagesIntoFrameAnalysis`, `ui.videoSequence.timelineSurfaces.runtimeReady`, `workspace.importUrl.videoFramePreview.{cacheFileName,timestampedImageFolder}`, and live localhost panel audit before publish approval. | 2026-06-30 |
| Storyboard live-route drop-authority docs/schema sync | Record the fresh-draft graph authority and workspace-prefixed verifier selector contract across canonical docs and schema mirrors | Prefer fresh draft graph state for runtime scene seeding and topology signatures; live-route verifier port selectors must accept exact ids or workspace-prefixed suffix ids for both source and target handles. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-renderer-document.md,agentic-graph-storyboard-widget-pan-zoom-overlay-failsafe-document.md,agentic-graph-testing-document.md,agentic-graph-cross-repo-publish-topology.md}},huijoohwee.github.io/schema/AgenticRAG/{README.md,README.pipeline-and-renderer-guidelines.md,README.pipeline-and-renderer-guidelines.companion.md}` | Storyboard Widget runtime scene + Storyboard live-route verifier + cross-repo schema mirrors | `useStoryboardWidgetRuntimeScene`, `buildOverlayTopologyLayoutSignature`, `verify_storyboard_live_route_media_panel_retention.py`, `verify_storyboard_rich_media_drop_browser_smoke.py` | Freshly dropped Rich Media panel nodes with authored graph `x/y`, workspace-prefixed live-route node ids, and the canonical docs/sync targets | Updated canonical docs, mirrored schema guidance, one companion split, and a stable live-route rule that keeps fresh drops anchored and verifier selectors prefix-safe | The runtime scene must read the freshest draft graph before stale render snapshots, and browser verifier selectors must follow canonical id suffixes so live-route workspace prefixes do not create false regressions. | Re-run `npm --prefix canvas run test:live:storyboard-media-panel-retention:browser`, `npm --prefix canvas run test:smoke:storyboard-rich-media-drop:browser`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future runtime-scene authority or storyboard verifier changes. | 2026-06-29 |
| Storyboard live-route same-text reapply draft reset docs/schema sync | Record the canonical same-path same-text markdown reapply contract that clears transient Storyboard Rich Media and edge residue on the live route | Treat explicit source-authority markdown reapply as a fresh apply epoch: Storyboard Widget draft lifetime must key off shared applied-document identity plus apply revision so same-text replays invalidate transient residue before the next render/drop cycle. | `agentic-graph/{todo-log.md,docs/documents/{agentic-graph-canvas-document.md,agentic-graph-renderer-document.md,agentic-graph-storyboard-widget-pan-zoom-overlay-failsafe-document.md,agentic-graph-testing-document.md}},huijoohwee.github.io/schema/AgenticRAG/{README.md,README.pipeline-and-renderer-guidelines.md,canvas.jsonld,markdown.jsonld}` | Storyboard Widget applied-document reset + Storyboard live-route SSOT cleanup | `setMarkdownDocument`, `setActiveMarkdownDocument`, `useCanvasAppliedMarkdownDocument`, `test:live:storyboard-media-panel-retention:browser` | Explicit `applyMarkdownDocument(...)` replay of the active starter markdown after transient Rich Media panel and edge authoring | Updated canonical docs and schema notes describing same-text reapply invalidation, live-route cleanup proof, and draft-reset authority | Source-authority reapply is a real reset event even when path and text match; document it once in store/runtime SSOT and mirror only the validated boundary into AgenticRAG. | Re-run `npm run test:live:storyboard-media-panel-retention:browser`, `npm run test:smoke:storyboard-rich-media-drop:browser`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future Storyboard Widget draft-lifetime or live-route reapply changes. | 2026-06-29 |
| Storyboard live-route retention release | Record the production release that fixed live-route media-panel retention and completed the Dev -> Prod -> Cloudflare path | Keep Storyboard retention fixes source-owned, publish only through `pages:build-sync` and `pages:deploy-cloudflare`, and anchor release proof by paired repo commits, live route checks, and D1 docs seed evidence. | `agentic-graph/{canvas/src/components/StoryboardWidgetCanvas/runtime,canvas/scripts/verify_storyboard_live_route_media_panel_retention.py,docs/documents/{agentic-graph-cross-repo-publish-topology.md,agentic-graph-testing-document.md},todo-log.md},huijoohwee/{content/agentic-graph,_worker.js}` | Storyboard live-route retention + Pages release topology | `test:live:storyboard-media-panel-retention:browser`, `storyboard:readiness:check`, `pages:build-sync`, `pages:deploy-cloudflare` | Source commit `530462d6`, publish commit `ec4dfa47`, preview `https://0d3c18ba.joohwee.pages.dev`, live routes `https://airvio.co/` and `https://airvio.co/agenticgraph/` | Released Storyboard retention fix, synced publish mirror, successful Pages deploy, live route proof, and D1 docs seed `applied=41 conflict=0 rejected=0` | Release evidence belongs in canonical source docs and generated mirror artifacts only; fix hygiene blockers at the source repo, never by patching downstream publish output or bypassing pre-push gates. | Re-run `npm run storyboard:readiness:check` and `npm run test:live:storyboard-media-panel-retention:browser` before the next Storyboard Rich Media release and mirror any further release-note changes into AgenticRAG only after the next approved docs sync. | 2026-06-29 |
