{
  "@context": "https://huijoohwee.github.io/schema/AgenticRAG/v1/context.jsonld",
  "@id": "rag:Pipeline",
  "@type": "rdfs:Class",
  "schemaVersion": "1.0.2",
  "rdfs:comment": [
    {
      "@value": "Defines the high-level, domain-agnostic GraphRAG pipeline surface. Components implement token linking, edge elevation, adaptive threshold tuning, multi-document unification, feedback loops, corpus-scale reasoning, and agentic query traversal. Configuration is expected to be externalized and validated structurally. Runtime import→render flows must be order-safe and batch-aware: bulk imports should coalesce change notifications, and late async completions must not override the current user-selected view. UI render surfaces must consume a single SSOT-derived active graph render view (keyword base → optional frontmatter filter → optional group collapse) to prevent cross-panel drift. Cross-repo modularity is enforced: each concern has a single owner (host vs plugins), cross-repo imports occur only through declared package entrypoints (no sibling source-relative imports), shared types are single-source-of-truth, and persistence keys are centrally defined and namespaced to avoid collisions; automated checks should guard these boundaries.",
      "@language": "en-us"
    },
    {
      "@value": "定义高层、领域无关的 GraphRAG 流水线表面。组件实现 token linking、edge elevation、自适应阈值调参、多文档统一、反馈回路、语料级推理与智能体查询遍历。配置应外置并进行结构性校验。运行时 import→render 必须具备顺序安全与批处理意识：批量导入需要合并变更通知，异步任务的迟到完成不得覆盖用户当前选择的视图。所有 UI 渲染表面必须消费单一 SSOT 推导的 active graph render view（keyword 基础图 → 可选 frontmatter 过滤 → 可选 group collapse）以避免跨面板漂移。跨仓库的模块化必须被约束：每个关注点仅允许单一所有者（宿主 vs 插件），跨仓库引用只允许通过声明的包入口（禁止 sibling 源码相对路径导入），共享类型必须单一真源，持久化 key 必须集中定义并做命名空间隔离以避免冲突；应通过自动化检查守护这些边界。",
      "@language": "zh-cn"
    }
  ],
  "rdfs:label": [
    { "@value": "Pipeline", "@language": "en-us" },
    { "@value": "流水线", "@language": "zh-cn" }
  ],
  "propertyDefinitions": {
    "config": {
      "@type": "@json",
      "required": false,
      "suggestedFields": [
        "phrase_boundary_threshold",
        "max_entity_span_tokens",
        "coreference_distance_limit",
        "edge_confidence_threshold",
        "max_syntactic_path_length",
        "temporal_marker_boost",
        "auto_tune_enabled",
        "tuning_sensitivity",
        "feedback_window_size",
        "entity_merge_threshold",
        "conflict_resolution_strategy",
        "cross_document_inference_depth",
        "feedback_loop_enabled",
        "quality_check_interval",
        "max_feedback_iterations",
        "min_pattern_support",
        "max_pattern_entities_per_block",
        "emergent_relationship_threshold",
        "corpus_centrality_algorithm",
        "default_traversal_depth",
        "min_result_count",
        "max_context_chunks",
        "follow_up_suggestion_count"
      ]
    }
  }
}
